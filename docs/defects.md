# Astrographer — Active Defect / Finding List

Maintained by the document-archival loop (AGENTS.md item 6). Open defects on
top; fixed rows below; superseded rows archived. A defect in the `provident-ssr`
package or the foundation is a HANDOFF item (feature request to the provident
dev agent — see `docs/HANDOFF.md`), never patched here. A host-side finding
(this project's `src/`) is fixed here.

## OPEN

| # | Severity | Symptom | Reproduction | Suspected root cause | Proposed fix shape (upstream-owned) |
| --- | --- | --- | --- | --- | --- |
| **HEAVY-OPS-FREEZE-THE-PAGE (2026-09-16, live — user report: "document uploading and other heavy JS code stops the page from responding to basic inputs like page resize")** | **high (HOST + ARCHITECTURE) — OPEN (NEW)** | Ordinary document interactions freeze the UI: opening a document from the doc-nav makes the page unresponsive for ~1s (two **439 ms** main-thread long tasks), and expanding a doc-nav folder alone costs a **505 ms** long task. During such a freeze nothing responds — including a window resize. | Live on the running app (226-doc corpus): (a) a REAL hit-tested click on a doc-nav document row → **first responsive probe 1 011 ms** and `PerformanceObserver` long tasks `[439, 439]`; (b) a REAL click on a folder row (`[".live-corpus"]`) → **one 505 ms long task** (39 DOM mutations) just to reveal two more rows; (c) idle resize round-trip is 6 ms, so the freeze is the WORK, not the resize path. | Every app-graph change (a folder toggle, a document open, any pane change) routes through a FULL envelope re-derivation + re-assembly (`reDerive`/`refresh` → fresh envelope → content-root reconcile → render of a huge DOM: the live page is ~12 700 px of scroll height per document view and the store holds 226 documents), so a trivial disclosure costs a full-graph pass; the renderer's main thread is blocked for the whole pass. The engine/MCP side is NOT the blocker (queries/imports are main-process); the RENDERER's synchronous re-derivation is. | (a) Stop re-deriving the whole app graph for a local UI change: a doc-nav folder toggle must re-render ONLY the doc-nav pane body (a scoped pane reconcile), a document open must mount only the stage payload, and a pane visibility/size change must not touch the document graph at all; (b) move anything O(store) off the interaction path (virtualize the doc-nav list, cap the rendered rows, chunk/async the reconcile and yield to the event loop so a resize is never starved); (c) keep the long-task budget asserted live (owed row: no pane/doc interaction produces a >100 ms task). NOTE the user's architectural point recorded below (`ARCH-GNOSIS-OFFLOAD`). |
| **CANVAS-DIMENSIONS-JS-DRIVEN (2026-09-16, live — user report: "the dimensions of the canvas/frame should be basic CSS and JS should not be involved in adjusting to a window resize at all")** | **medium (HOST) — OPEN (NEW)** | The canvas/frame dimensions are written by JS as inline CSS custom properties on `<html>` (`--zone-left-size: 220px; --zone-right-size: 220px; --zone-header-size: 48px; --zone-footer-size: 48px; --stage-weight: 640fr; --top-bar-size: 36px`), and the stage track is authored as a `fr` value derived from a JS "size" number — so layout geometry is JS-owned even though a window resize should be pure CSS. | Live: `document.documentElement.getAttribute('style')` = the variable list above; `#wiki-root` computed `grid-template-columns: 220px 1583.91px 0px` and `grid-template-rows: 0px 12698.7px 0px`; `--stage-weight: 640fr` is the app's own serialization (`layout-state.ts:221/282`) of the stage "size" number. Idle resize latency is fine (6 ms) because the browser re-lays-out from the variables, but every layout STATE write re-serializes the same variables from JS (each write costs a style recalc on the huge page), and the sizing model is a JS number rather than CSS. | The layout model keeps zone sizes + a stage "size" in `LayoutState` and projects them into CSS custom properties on every change (`layoutCssVars`, `layout-state.ts:217-222`). The user's requirement: the frame/canvas dimensions should be **basic CSS** (flex/grid with `minmax`/`fr` and the container's own size), with **JS involved in a WINDOW RESIZE at all** — JS may own USER-CHOSEN sizes (a gutter drag), but must not be re-deriving geometry per change or per resize. | Reduce the JS surface: author `#wiki-root`/the stage as CSS (`grid-template-columns: var(--zone-left-size, 220px) minmax(0, 1fr) var(--zone-right-size, 220px)`, i.e. the stage always `1fr` — no JS `--stage-weight` serialization), keep the JS-written custom properties ONLY for operator-chosen sizes (gutter drag/double-click reset), and add NO resize listener at all (the CSS tracks the container). Owed live row: after a resize, geometry follows with no JS layout write and no layout-state commit. |
| **ARCH-GNOSIS-OFFLOAD (2026-09-16, user statement — carry as an ACTIVE architectural requirement)** | **high (ARCHITECTURE REQUIREMENT) — OPEN (recorded)** | *"preventing the app from locking up under import load, search, document opening, etc. was one of the incentives behind splitting the document handling off to Gnosis."* | — (a design requirement stated by the user, not a repro) | The app today does document parsing/indexing/derivation IN ITS OWN main+renderer processes (the RAG store, the markdown importer, the traversal/envelope derivation), so heavy document work contends with the UI; the Gnosis engine exists precisely to take that work. | The Astrographer shell should treat **Gnosis as the document-handling owner** for import/parse/index/query traffic (the `SOURCE-SWITCHABLE` `RagStore` seam already anticipates a `createRemoteRagStore` — `docs/decisions.md`), leaving the shell with rendering + orchestration only: heavy document work must not block the shell's UI thread or its main process. Immediate corollaries: imports/queries go to the engine (async, chunked), the local store path stays as the offline fallback, and the UI keeps a long-task budget under load. Tracked against `docs/decisions.md` `SOURCE-SWITCHABLE` + the gnosis engine rows (`GNOSIS-ENGINE-QUERY-MODE-IGNORED`, `HOST-ENGINE-QUERY-POST-NO-ENVELOPE`, `GNOSIS-ENGINE-ENRICHMENT-SURFACE-ABSENT`). |
| **INLINE-OFFSET-STALENESS (2026-09-16, live — user report N5: the mcp-endpoint spec's opening **Phase C**) — RESOLVED (STALENESS, not a parser defect)** | **resolved — DATA staleness (no code change owed)** | The `mcp-endpoint` spec rendered its opening paragraph with the bolded "Phase C" APPENDED to the end of the paragraph instead of injected mid-sentence (the user asked whether this is staleness or a parser defect). **Answer: STALENESS.** | Evidence: the STORED node (pre-rework parse) was `p:1` `content` = the sentence with an EMPTY gap where the inline belongs (`"…parked  (cross-process…"`) and `children` = `[{type:"strong", content:"Phase C"}]` with **NO `offset`** — and `buildInterleavedChildren` (`src/main/traversal.ts`) appends an offset-less child AFTER the body by design (its documented legacy path), which is exactly the append-at-end symptom. The CURRENT parser on the SAME source produces `content` = the FULL text (inline included: `"…parked Phase C (cross-process…"`) + `children` `[{type:"strong", content:"Phase C", offset:237}]` (verified by `parseMarkdown` on `docs/specs/mcp-endpoint.md`). After **delete + re-import** from the current source (`edit.delete_node` + `edit.import_markdown`, 416 nodes / 806 edges) the stored node carries `offset: 237` and the LIVE render is `inlineInPlace: true`, `appendedAtEnd: false`, text `"…parked Phase C (cross-process MCP/Electron endpoint — ../Pre…"`. | Not a parser defect: the current parser emits the 0.5.0 inline-offset model correctly; the app store simply held a pre-0.5 node (no offset). | **Action taken:** deleted and re-added `docs/specs/mcp-endpoint.md` — and, because the app could not persist writes in this session (the sandbox denies `~/.config`; see `SETTINGS-PERSIST-WRITE-SILENT`), the app was relaunched with `--user-data-dir=/tmp/astrographer-demo-store` and the whole corpus re-seeded there (226 files) so the fix is DURABLE. **RELAUNCH-CONFIRMED (2026-09-16):** after a real restart the stored `docs/specs/mcp-endpoint:p:1` carries `offset: 237` and the live render is `"parked Phase C (cross-process …"` mid-sentence. **RECURRENCE NOTE:** a relaunch against the sandboxed `~/.config` (whose file the app cannot overwrite) brings the append back — that is the STALE on-disk data, not a parser regression; run with a writable `--user-data-dir` (or `HOME`) to keep imported data current. **No code change owed.** NOTE the two ADJACENT live defects this document still shows and which are NOT staleness: **DOC-HEAD-CONTAINS-FIRST-PARAGRAPH** (the opening paragraph is still a child of the `h1`, so `pFont == headFont == 32px`) and **INLINE-RUNS-SHARE-THE-RAG-NODE-ID** (an inline `<strong>` carries the parent paragraph's `data-rag-node-id`, so `[data-rag-node-id="…:p:1"]` matches two elements — the source of the earlier false duplicate counts). |
| **PANE-VISIBILITY-IRREVERSIBLE (2026-09-16, live — user report N1)** | **high (HOST) — OPEN (NEW)** | The pane-visibility control is a one-way door in practice: a pane can be unchecked, but there is no way to get it back — and the user's explicit instruction is to **remove the option** rather than fix the restore path. | Live: with the settings modal open, `[id^="operator-pane-visibility-"]` resolves to **0 controls** (the modal hosts `#operator-panes` = settings + gnosis-status, whose catalogs do not include the app-graph panes) while the app-graph panes are the ones that can be hidden; a hidden pane's frame is REMOVED from the DOM (`uf_settings_7` semantics), so nothing in the sidebar offers it again; the native View → Panes menu is the only route and it is the LIVE-12 ACCEPTED compromise (the submenu closes on the first click). Meanwhile the persisted `enabledPanes` list cannot be repaired from the UI (and in this session the settings write is denied — see `SETTINGS-PERSIST-WRITE-SILENT`). | Pane visibility is stored as an ENABLED-SET with no "show available panes" affordance: `PaneRegistry` lists all panes, but the operator surface only exposes toggles for the panes that are CURRENTLY ENABLED (the per-pane `[data-pane][data-enabled]` controls live on the enabled panes' own chrome), so hiding the last control for a pane removes the only handle back. | **User decision: remove the pane-visibility option from the UI** (the operator should not be able to hide app-graph panes at all), OR make the census list EVERY registered pane with an independent checkbox including the hidden ones. Recommended (smallest, matches the user's call): drop the visibility toggles + the View → Panes submenu and author the app-graph panes as always-enabled; keep `PaneRegistry.enabled` for the operator-scope panes only. Owed live row: a hidden pane is recoverable OR the hide affordance does not exist. |
| **STALE-MOUNT-PUSHES-CANVAS (2026-09-16, live — user report N2; FIXED + LIVE-CONFIRMED same day)** | **high (HOST) — FIXED + LIVE-CONFIRMED** | Adding panes scrolls the whole app half-way down the page and leaves the decorated canvas section: the app content starts far below the top of the page. | Live: two `#wiki-root` mounts — a STALE childless one (`y=-11041, h=11589, children=0, display=grid`) and the live one (`y=548, h=11589, children=6`); `#app` box `y=-11058, h=11622` at `scrollY=11094`; page `scrollHeight=23230` (≈2× the app's own height). The stale mount keeps `id="wiki-root"`, is a grid box with a real height, and takes layout flow, so it consumes the space ABOVE the live canvas. | The app-graph re-assembly never removes the previous `#wiki-root` mount (the F-2 leak): the new mount is appended and the old one stays in flow with its own height. | Remove/neutralize the previous mount on re-assembly (`destroyRoot`/reconcile the OLD root, or `display:none` it until removed) so exactly ONE `#wiki-root` exists in flow. Same fix as F-2; the live row must assert a single in-flow `#wiki-root` AND that `#app`'s top sits at the layout origin after adding panes. |
| **PANE-DRAG-TOP-ONLY (2026-09-16, live — user report N3)** | **medium (HOST) — OPEN (NEW; refines PANE-DRAG-NO-COMMIT)** | Click-drag for panes still does almost nothing: only `search` and `doc-nav` respond at all, and a dragged pane can only be moved to the TOP of its existing pane/zone — never to a chosen position, never between panes. | Live: the left zone's `.layout [data-zone]` projection contains the zone container ONCE plus the four **0×0 gutters** and the `is-empty` right/header/footer cells (`x=0,y=0,w=0,h=0`), so the only viable drop target is the left zone itself; the reorder path therefore resolves an insertion index of 0 (top) for any gesture, and the other panes' headers do not start a drag. | The drop projection uses the `.layout [data-zone]` rect set (the panes'/zones' real boxes are inside `#wiki-root`, not under `.layout`), and `insertionIndexForPoint` gets no usable sibling geometry → every drop lands at index 0; panes whose frame lacks the header surface at the grab point never start. | Project the REAL zone/pane boxes (from the `#wiki-root` grid + the `.pane-frame` rects) for `movePaneDrag`/`dropZoneForPoint`, and compute the insertion index from the hovered pane's midpoint so a drag can land between any two panes; make every pane frame's header start the drag. Owed live row: drag pane A between B and C → order becomes B,A,C. |
| **DOC-HEAD-CONTAINS-FIRST-PARAGRAPH (2026-09-16, live — user report N4)** | **medium (HOST) — OPEN (NEW; the residue of LIVE-UF6)** | With the duplication fixed, the opening paragraph is STILL rendered inside the document's `h1` heading, so it takes the header's formatting (32px/700) and reads as part of the title. | Live (`mcp-endpoint` focused): `H1#rag-mcp-endpoint:section:1` (`font-size 32px, font-weight 700`) has ONE child: `P#rag-mcp-endpoint:p:1` ("Status: LANDED (2026-08-21)…"); the H1 and the P are NOT siblings (`p:1` is a descendant of the head, and the head's parent holds only the section headings). | `buildSubtree` nests a section's `doc-child` blocks INSIDE the section's own element, so a section's first paragraph is a child of its heading element. The traversal's doc-child nesting is correct for list/table blocks but a HEADING must be text-only — a paragraph inside an `h1` is invalid HTML and inherits heading typography. | Either author a section's non-heading doc-children as SIBLINGS of the heading element (payload root = the section, with the heading + its blocks as siblings), or wrap a section's heading text in its own inline element so the block children cannot be descendants of the heading. Owed live row: for a doc starting `# Title` + paragraph, the first paragraph is a SIBLING of the `h1` (and `getComputedStyle(p).fontSize` is body-scale). |
| **WHOLE-PAGE-EDITING-REQUIREMENT (2026-09-16, user requirement — report #3 follow-up)** | **high (PRODUCT REQUIREMENT) — OPEN (NEW; not yet implemented)** | The document is editable only ONE FRAGMENT AT A TIME: each paragraph and table cell is its own `contenteditable` host (or its own textarea in textarea mode), so a user CANNOT select text across paragraph boundaries, the document title and table cells are not editable at all, and the editing model fights normal reading/copy behaviour. **User requirement (verbatim intent):** *the entire page should be editable, as a single block if possible*; and markdown mode should drop the HTML formatting and present the markdown DATA as plain text (no form-control editing UI). | Live census: the stage of a focused document carries N independent `[contenteditable]` hosts (e.g. `{P:10}` on a fresh `docs/defects`), 0 editable headings (see **DOC-TITLE-NOT-EDITABLE**), 0 editable table cells (see **TABLE-CELLS-NOT-EDITABLE**); in textarea mode the same nodes become 815 separate `<textarea>` editors (see **EDIT-MODE-TEXTAREA-UI**). Selecting across two paragraphs is impossible by construction (separate editing roots). | The editing surface is authored PER RAG NODE (a per-node control spliced into each subtree root) rather than per DOCUMENT — a design decision recorded as `FORM-CONTROL-EDITING` / `EDITING-MODE-SETTING` (docs/decisions.md). | **Decided direction (docs/decisions.md `WHOLE-PAGE-EDITING`, ACTIVE requirement, supersedes the per-node editing control once implemented):** make the document ONE editable block (a single editing surface over the whole rendered document, with the RAG store still authoritative), so selections, copy/paste and edits span paragraphs naturally; Markdown mode shows the markdown DATA as plain text. Requires a spec re-derivation (the per-node commit shape → ONE whole-document commit, or a text-node diff derived from the single block) + the owed live row "whole-page edit commits 1-1 to the store". Absorbs **DOC-TITLE-NOT-EDITABLE**, **TABLE-CELLS-NOT-EDITABLE** and the textarea half of **EDIT-MODE-TEXTAREA-UI**. |
| **TOP-BAR-NOT-FIXED (2026-09-16, live — user demo report #3 C1)** | **high (HOST) — OPEN (NEW; overlaps TABBAR-SCROLLS-AWAY)** | The page scrollbar runs under the top bar: the tab strip scrolls away with the content and the vertical scrollbar is drawn over/behind the bar instead of beside it. | Live: `#tab-strip` computed `position: static`, `z-index: auto`, box `[0,-1320,2032,36]` at the current scroll offset while `document.documentElement.clientWidth = 2032` (a 14px scrollbar) and `body`/`html` both `overflow: visible`; page `scrollHeight 46 851`. | The bar is in normal flow inside the scrolled page (no fixed/sticky positioning, no z-index) and the page — not the stage — is the scroll container, so the scrollbar belongs to the whole document and paints over the bar. | Make the top bar a fixed/sticky shell row (own stacking context) and move scrolling into the stage/zones so the document scrollbar no longer spans the bar. Same root as `PANE-NO-PLACEMENT-OR-SCROLLBOX` (report #2). Owed live row: report #1 §3 row 2 (scroll invariance) + a scrollbar-geometry assertion. |
| **NAMEPLATE-NO-SIDE-MARGIN (2026-09-16, live — user demo report #3 C2)** | **low (HOST) — OPEN (NEW)** | The "Astrographer" nameplate sits 4px from the window's left edge, visually cramped against the frame. | Live: `.app-nameplate` box `[4, -1320, 98, 36]` with computed `margin: 0px 8px 0px 4px`; `#tab-strip` `padding-left: 0px`; `gapFromWindowEdge = 4`. | The nameplate CSS uses `margin-left: 0.25rem` (4px) — authored for a compact bar, with no window-edge inset. | Give the top bar a real left inset (e.g. `padding-left: 0.75rem` on `.tab-strip`, or a 12–16px `margin-left` on the nameplate) so the name aligns with the body padding (`16px`) instead of hugging the frame. Owed live row: nameplate inset ≥ 12px. |
| **TABLE-CELLS-NOT-EDITABLE (2026-09-16, live — user demo report #3 C3)** | **medium (HOST) — OPEN (NEW)** | Table cells in a document cannot be edited, although the surrounding paragraphs can — so a table-heavy doc (defects/decisions) is effectively read-only. | Live (fresh `docs/defects` focused): 6 `#zone:main table`s, **116 `td`/`th` cells, 0 with `contenteditable`**, `editableInTable = 0`; cells carry `data-node-id`/`data-rag-node-id` (`docs/defects:th:1`…) so they are real RAG nodes, but the editing control is never spliced onto them. Only `<p>` hosts are editable (10 of them). | The editing-control splice (rich/text mode) targets paragraph-like/section roots only — table cells (th/td) are not in the eligible set, so a table's own RAG nodes get no commit-on-blur control. | Extend the editing eligibility to table cells (`th`/`td`) — or make the table an editable subtree — so cell text commits on blur like a paragraph (with the same dirty-edit/caret rules). Owed live row: a table cell edit commits to the store. |
| **FIND-IN-PAGE-NOT-WIRED (2026-09-16, live — user demo report #3 C4)** | **medium (HOST) — OPEN (NEW)** | Ctrl+F does nothing: there is no find bar, and the application menu has no Edit/Find entries at all. | Live: a real `Input.dispatchKeyEvent` Ctrl+F reaches the page (`document` keydown log = `["ctrl+f"]`) and the app has **no find UI** (`inPageFindUI=false`, `activeElement` stays BODY); `src/main/app-menu.ts` builds ONLY a `File` menu (Import…/Import folder…/Quit) and a `View` menu (Panes) — no `Edit` menu, no `find`/`findNext` role, no `CmdOrCtrl+F` accelerator, and nothing calls `webContents.findInPage`. | The forked shell replaced Electron's default application menu with its own minimal template (File + View), removing the default Edit/Find items and never authoring a find-in-page path. | Add a find-in-page surface: either an `Edit` menu with `find`/`findNext`/`findPrevious` roles/accelerators (`CmdOrCtrl+F`) wired to `webContents.findInPage`, or a provident-authored find bar in the shell chrome (the project-wide UI rule prefers the provident framework for non-shell UI; the OS-owned menu is the shell exception). Owed live row: Ctrl+F opens a find UI and highlights a match. |
| **DOC-NAV-NOT-A-TREE (2026-09-16, live — user demo report #3 C5)** | **medium (HOST) — OPEN (NEW)** | The document navigator renders as a flat bulleted list of 63 titles instead of a directory TREE, and the folder grouping is inert in practice. | Live (fresh boot, doc-nav expanded): `ulCount 1`, `liCount 63`, `listStyleType "disc"`, `ul padding-left 40px`, `::marker` normal bullets, **`[role=tree]/[role=treeitem]` = 0**, `[aria-expanded] = 0`, **`[data-folder-path]` = 0** — every row is a `li[data-document-id]` at the same indent (`indentOfDeepest 0`). Source (authoritative in principle): `pane-graph.ts` DOES author folder `li` rows with nested `<ul data-folder-children>` + `data-expanded`, so the missing piece is DATA: `rag.list_documents` for the current store returns **63 docs with `path: []` (0 non-empty)** — no path segments, so no folders exist to render. | Two layers: (a) the doc-nav is authored as nested `ul`/`li` (bullet list semantics) rather than a tree control (roles/chevrons/indent guides); (b) the store's documents carry NO `path` metadata, so even the folder branch never fires. | (a) Render the navigator as a tree (ARIA `tree`/`treeitem` + `aria-expanded`, chevron affordances, indent guides, no bullet markers) — presentation change in the pane authoring + shell CSS; (b) make the importer persist each document's path segments so a re-import yields real folders (see the `pathed import` gap: the fresh import of `docs/specs/*.md` SHOULD group under `docs` → `specs`). Owed live row: a doc with path segments renders nested tree rows with `role=treeitem`. |
| **PANE-TOGGLE-FULL-REASSEMBLY (2026-09-16, live — user demo report #3 C6)** | **high (HOST) — OPEN (NEW)** | Collapsing/minimizing a pane or the sidebar has a very noticeable lag and a CPU spike, although the visible effect is a single class flip — the app re-assembles the whole app graph for a pure presentation change. | Live instrumentation while clicking `#zone-minimize-left`: **2 556 DOM mutations** (`MutationObserver` childList+subtree+attributes+characterData on `body`), **2 long tasks (465ms + 52ms)**, gesture wall time 2.5–2.9s, page 1 728 element nodes / `scrollHeight 46 851`; `Profiler` self-time shows the JS is small (`translateNodeData` 13ms, `renderTree` 8ms, `enumPathWalks` 8ms) — i.e. the cost is the DOM **style/layout/paint** work the re-assembly forces, not the JS. The same 2 55x-mutation profile appears for a SINGLE pane collapse (`togglePaneCollapse('search')` → 2 556 mutations) and on expand. | Every pane gesture routes through the app-graph re-assembly (`refresh()`/`reDerive` → fresh envelope → destroy+re-attach content roots) instead of flipping a class + CSS on the affected element; the empty-zone/minimize state is authored as a re-render rather than a CSS state. So one gesture rebuilds/re-lays-out the entire (large) document. | Make pane/zone collapse and minimize PURE CSS state on a single element (a class on the pane/zone frame + `display`/size rules), never a graph re-assembly — the user's framing is correct. This also fixes `PANE-SLOT-INSTABILITY-ON-DISCLOSURE` (a disclosure re-render currently re-sorts the zone). Owed live row: a pane-gesture budget (≤ N mutations, no long task > 50ms). |
| **DOC-TITLE-NOT-EDITABLE (2026-09-16, live — user demo report #3 C7)** | **medium (HOST) — OPEN (NEW)** | A document's title / section headings cannot be edited: only paragraphs are editable. | Live (fresh `docs/defects` focused): the `[data-doc-head]` H1 (`rag-docs/defects:section:1`) has **`contenteditable = null`**; `[contenteditable]` census on the stage = **`{P: 10}`** — **0** editable `h1/h2/h3` (`editableHs = 0`), and the H2 `docs/defects:section:2` likewise has no control. | The editing-control splice is authored for paragraph-like roots; heading/section roots (which carry the document title and every section title) are excluded from the eligible set, so no commit-on-blur control is attached to a title. | Make section/heading roots editable (`h1..h6`, at least the doc-head title) with the same commit-on-blur + dirty-edit + caret rules as paragraphs, and reconcile the title change through the RAG store (a title edit updates the doc-head node's content). Owed live row: editing the H1 commits to the store and survives a re-derive. |
| **PANE-NO-PLACEMENT-OR-SCROLLBOX (2026-09-16, live — user demo report #2 B5)** | **high (HOST) — OPEN (NEW; the root of B5, I-1 and I-8)** | Panes cannot be "placed": they are plain flow boxes, no overlap rule exists, and a pane with lots of content grows to its full height instead of scrolling internally — so the PANE and then the PAGE scroll, which is also why the tab bar scrolls away. | Live: `#pane-doc-nav` **h = 6 271px** (63 list items), `overflow-y: visible`, `max-height: none`, body child `clientHeight 6 194 === scrollHeight 6 194`; every pane `position: static`, `overflow-y: visible`; page `scrollHeight 27 629`; no zone/pane `gap`. | The zones are grid cells and the panes inside them are an unconstrained flow column: no `max-height`/flex + `overflow-y:auto` on the pane body, no collision/placement enforcement, and the page (not the stage) is the scroll container. | Make the shell's scroll containers the ZONES/STAGE (not `body`): panes get a bounded height with an internal `overflow-y:auto` body, panes are a placed, non-overlapping list (order from `LayoutState.panes[].order`, never re-sorted by content — see `PANE-SLOT-INSTABILITY-ON-DISCLOSURE`), and the empty-zone/scroll behaviour is asserted live. Cover with the owed PLACEMENT + INTERNAL-SCROLL live row (`docs/specs/user-demo-bug-report-2-2026-09-15.md` §3 row 13). |
| **ZONE-NO-BOUNDARY-ASYMMETRY (2026-09-16, live — user demo report #2 B1)** | **medium (HOST) — OPEN (NEW)** | Panes have a visible boundary (border + radius + padding) but ZONES have none, so the layout reads as loose boxes on a single undivided surface. | Live computed: `.pane-frame` `border: 0.744px solid rgb(58,64,70)` + `border-radius 8px` + `padding 8px`; `#zone:left` / `#zone:right` / `#zone:main` ALL `border: 0px none`, `backgroundColor rgba(0,0,0,0)`, `box-shadow none`, `border-radius 0`, `padding 0`. Source: `src/renderer/index.html:163` styles `.pane-frame` only — there is no zone boundary rule. | The zone containers are unstyled grid cells; the only chrome ever authored was on the pane frames. | Give each zone a real boundary (a background distinct from its neighbours, a border, or a grid `gap` ≥ 8px) so a ZONE reads as a region; keep the pane boundary for the pane itself. Cover with the owed ZONE-BOUNDARY PARITY row (report #2 §3 row 10). |
| **STAGE-LEFT-EDGE-FLUSH (2026-09-16, live — user demo report #2 B3)** | **medium (HOST) — OPEN (NEW)** | The content stage has no margins: the document's first characters start flush against the side pane's border, and the pane's own edge occupies the last pixels of its zone — the left edge of the canvas looks overlapped. | Live at 1600 and 1400 wide: `#zone:left.right = 253`, `.pane-frame.right = 253` (border-right 0.744px), `#zone:main.left = 253` ⇒ **gap 0px**; `zone padding 0`, `main padding 0`; first `<p>.left = 253` ⇒ inset from the canvas = **0**; `elementFromPoint(zoneLeft.right-2, top+60)` = `pane-doc-nav.pane-frame`. | No zone padding, no pane margin, no grid `gap`, and the main zone carries no left inset — everything is flush. | Author a real inset: 8–12px between the side zone and the stage (zone padding or grid gap) plus the stage's own content padding, so text never touches the pane border. Cover with the owed STAGE INSET row (report #2 §3 row 11). |
| **LIST-INDENT-40PX (2026-09-16, live — user demo report #2 B4)** | **low/medium (HOST) — OPEN (NEW)** | List bullets in the document pane eat an outsized share of the pane: the UA default 40px list indent is never overridden, and in a 220px pane that is ~18% of the width gone before the text starts. | Live (doc-nav pane expanded, 220px zone): the pane's `ul` computed `padding-left: 40px`, `li` width 163px of the 203px list box; `#zone:main ul` likewise `padding-left: 40px`. `src/renderer/index.html` contains NO `ul`/`li` rule (the 40px is the browser default `padding-inline-start`). | The shell CSS never sets a list indent, so every list inherits the UA default regardless of the container's width. | Add a shell list rule (e.g. `ul { padding-inline-start: 1rem }`) so bullets take ~16px, and/or let a narrow pane scroll horizontally; keep the stage lists readable. Cover with the owed LIST-INDENT row (report #2 §3 row 12). |
| **HISTORY-NOT-DROPDOWN (2026-09-16, live — user demo report #2 B6)** | **medium (HOST) — OPEN (NEW; overlaps LIVE-UF5)** | The history surface is neither a dropdown nor in a pane: it renders as a plain heading + list inside the MAIN canvas, so it consumes document space permanently. The user wants it collapsed behind a dropdown. | Live: `#pane-history` is a `DIV` (`isDetails=false`), children `STRONG:"History"` + `P:"(no history)"`; `historyInMain=true`, `historyInPane=false`, `.pane-frame [data-role="history"]` = 0; `#zone:main` has 0 `details`/`select`; Undo/Redo are `#editor-toolbar` buttons inside the main canvas. | The history segment is authored into the stage assembly as a static section (the C16 history sub-pane was never given a pane home), and no disclosure control was authored. | Make history a DROPDOWN/disclosure (default-collapsed) and place it (with the editor toolbar) inside a `.pane-frame` — tied to `LIVE-UF5` and report #1 I-5. Cover with the owed HISTORY CONTROL SHAPE row (report #2 §3 row 14). |
| **SETTINGS-PERSIST-WRITE-SILENT (2026-09-16, live — user demo report #2 B2)** | **medium (HOST) + ENVIRONMENT finding — OPEN (NEW)** | Pane visibility (and every other operator setting) appears not to save. Root cause in this session is the app being UNABLE to write its userData directory (`EACCES`), and the app swallows that failure silently — so it looks like a product bug. The persistence logic itself is verified CORRECT. | App stderr: `[security-store] persist failed (…/provident-security.json): EACCES: permission denied` + Electron `Error writing DevTools active port to file …: Permission denied (13)`; all settings files stale (operator-settings 13:01, rag 17:12, template 16:58, security 03:14) while shell/node writes to the same dir also fail `EACCES` (session-level file restriction). **Product verified correct with a writable HOME:** real click on `#operator-pane-visibility-doc-nav` → `enabledPanes ['doc-nav','search']→['search']`, disk updated, and **after restart the app booted with `['search']`** (frames also `['search']`). | `persist()` in `src/main/operator-settings-store.ts` (and the sibling stores) catch and DISCARD write errors (`catch { /* non-fatal */ }`) — no user-visible signal, no diagnostics line; nothing verifies writability at boot. | (1) Surface write failures: log a diagnostics line AND warn once in the UI when the settings/store path is not writable; (2) document that the app must be able to write `userData` (the demo run must not be sandboxed away from it). Cover with the owed PERSISTENCE ROUND-TRIP (real userData) row (report #2 §3 row 15). |
| **TABBAR-SCROLLS-AWAY (2026-09-15, live — user demo report I-1)** | **high (HOST) — OPEN (NEW)** | The window top bar IS the browser-style tab strip, but it scrolls away with the document: after scrolling, the user cannot reach a tab or the window's own drag/move region without scrolling back to the top. | Live: `#tab-strip` computed `position: static` (its parent `.layout` also `static`); box y = `-720` at `scrollY=0` (inside a tall scrolled container) and `-1200` at `scrollY=1200`, `.layout` y = `-1073`; page `scrollHeight` 5 300–27 629 px. | The strip is in normal flow inside the scrolled `body`/`.layout` — no `position: fixed/sticky` on the top bar or the shell's outer container. | Pin the shell top bar (tab strip + drag region) out of the scrolled flow: a fixed/sticky top row on the shell chrome (`src/renderer/index.html` CSS), with the scroll container being the stage/zones rather than the whole page. Cover with a SCROLL-INVARIANCE live row (after `scrollTo(0, N)` the strip's box stays in the viewport) — owed, see `docs/specs/user-demo-bug-report-2026-09-15.md` §3. |
| **TABLE-OVERFLOWS-STAGE (2026-09-15, live — user demo report I-2)** | **medium (HOST) — OPEN (NEW)** | Wide tables (e.g. the Active Decisions page) run off the side of the page and give the whole document a horizontal scrollbar instead of scrolling inside the content area. | Live (CDP viewport emulation on the mounted `decisions` doc, 4 `#zone:main table`s): fits at ≥2100px, but overflow **670px @1440**, **830px @1280**, **1 086px @1024** (`document.documentElement.scrollWidth` stays 2 095 while `clientWidth` is 1425/1265/1009); no `overflow-x` container on any parent (`wrap:false`). | The content-stage CSS gives tables no width constraint or scroll container, so a 4-column decisions table with long cells sizes to `max-content` (scrollWidth 1 842) and the page widens. | Give the stage's table rendering its own horizontal scroll/wrap container (`overflow-x:auto` on a table wrapper, or `table-layout:fixed` + `word-break`) so the page never widens. Cover with a RESPONSIVE-WIDTH live row (1280 + 1024 → no table past `clientWidth`) — owed. |
| **EDIT-MODE-TEXTAREA-UI (2026-09-15, live — user demo report I-6)** | **high (HOST) — OPEN (NEW; SUPERSEDES the textarea half of `EDITING-MODE-SETTING`)** | The HTML/Markdown toolbar toggle switches the document into a textarea editing UI: 815 `<textarea>` hosts appear (with DUPLICATE ids), all `contenteditable` hosts vanish, and the "Markdown" view is the HTML-rendered body re-flowed rather than the markdown source as plain text. The user has ruled the textarea out as a pre-MVP testing UI, and Markdown mode should drop the HTML formatting and show the markdown data as plain text. | Live (real hit-tested `#editor-toolbar-toggle` click, label `HTML`→`Markdown`): `data-mode contenteditable→textarea`; stage `<textarea>` count `0→815` (e.g. `#textarea-decisions:section:1`, `#textarea-decisions:p:1` ×2 — the same id twice), `[contenteditable]` count `6→0`; the rendered "markdown" text has no markdown tokens (`"Astrographer — Active Decisions SummaryMaintained by the document-archival loop…"`). | The `editingMode: 'textarea' | 'contenteditable'` operator setting is implemented as an EDITING CONTROL swap (textarea host per RAG root), not as a VIEW mode — so "Markdown" re-renders the HTML body into form controls. The duplicated per-copy textareas come from the same double-materialization as **LIVE-UF6/I-3**. | Discard the textarea editing UI; make the toggle a VIEW mode: HTML = the current rich/contenteditable rendering, Markdown = the document's markdown source rendered as PLAIN TEXT (a `pre`/plain-text content root, no form controls, no HTML affordances, unique ids). Supersede the textarea half of the `EDITING-MODE-SETTING` decision row and re-derive the affected unit specs + tests (`unit-l1-editing-mode-setting`, `unit-l-textarea-editing-ui`). Cover with a MARKDOWN-MODE CONTENT row (zero `<textarea>`s + markdown tokens present) — owed. |
| **PANE-DRAG-NO-COMMIT (2026-09-15, live — user demo report I-7)** | **medium (HOST) — OPEN (NEW; the counterpart of the fixed F-1 header-surface work)** | A user dragging a pane by its header PAST the next pane gets no relocation: the simple documented gesture reliably does nothing (no reorder, no move). | Live (`doc-nav` header hit-tested in-viewport, `hit=pane-collapse-doc-nav` y=340, next sibling `search` top 358 / bottom 532): real down + 3 moves + up at y=526 → order unchanged `[gnosis-query, doc-nav, search, gnosis-documents, gnosis-wikis, crosslinks, template-editor]`; DOM event log `["pointerdown:pane-collapse-doc-nav:340","pointerup:pane-search:526"]` (the gesture starts on the header — the F-1 fix — but no drop commits). One probe DID reorder (a `doc-nav` drag moved `search` to slot 3), so the commit is order/geometry-dependent rather than universally dead. | The drop path depends on the projected `.layout [data-zone]` zone bounds + the insertion-index/snap rules; after the F-1 header-surface change the gesture starts but the drop resolution (zone projection vs the pane's own frame rect) does not produce a `commitPaneDrop` for a next-sibling drag. | Make a next-sibling header drag commit a relocation (project the LEFT zone container's real bounds, not the `.layout` rect; assert the new slot index survives the re-render). Cover with a PANE-DRAG COMMIT live row (real gesture → slot index changes) — owed. |
| **PANE-SLOT-INSTABILITY-ON-DISCLOSURE (2026-09-15, live — user demo report I-10; GENERALIZED by the doc-nav folder click 2026-09-16)** | **medium (HOST) — OPEN (NEW)** | **Generalized evidence (2026-09-16, batch 6):** a REAL click on a doc-nav FOLDER row (`[".live-corpus"]`) also triggers the full re-assembly — **505 ms long task, 39 mutations** — which is the reported "clicking in the documents pane / expanding the docs directory causes the panes to reorder". In the 2-pane configuration measured the order happened to stay `["search","doc-nav"]` while the rows went 9 → 11 (the reorder reproduces with the fuller app-graph pane set); the ROOT is shared regardless: **a pane-body content change must never re-assign zone slots**. |  Opening or closing the advanced-search disclosure MOVES the search pane to the end of the sidebar zone — the pane the user just used jumps position. | Live (real click on `#advanced-search-toggle`, `data-expanded false→true`): the `search` pane moved slot **2 → 7** (y 322 → 1181, h 173 → 718 with the 139×544 fieldset painted); zone order `[gnosis-query, search, gnosis-documents, doc-nav, gnosis-wikis, crosslinks, template-editor]` → `[gnosis-query, gnosis-documents, doc-nav, gnosis-wikis, crosslinks, template-editor, search]`; the follow-up collapse click then MISSED (`elementFromPoint=null`) because the control had moved. | The app-graph re-assembly allocates pane slots by a `seq`/order counter that a pane's content change (the disclosure's re-render) can perturb — the pane's `order` in `LayoutState` is not the slot authority for a re-derive, so a content change can re-sort the zone. | Make the zone slot order authoritative from `LayoutState.panes[].order` across re-derives, so a pane's content/disclosure change can never change its slot. Cover with a PANE-SLOT STABILITY live row (toggle a disclosure → the pane's slot index is unchanged) — owed. |
| **DEMO-ENGINE-START-GAP (2026-09-15, live — user demo report I-9)** | **low (HOST/ops) — OPEN (NEW)** | A plain app start has no Gnosis engine, so every gnosis surface reads unavailable until the operator starts `gnosis-server` by hand; there is no single demo-start command, and `--mode=gnosis`'s engine spawn fails SILENTLY (reports READY from a pre-existing server) if port 8080 is already bound. | Live: with no engine → `gnosis.status` `fetch failed`, gnosis panes empty. After `GNOSIS_SERVER_OLLAMA_URL=… GNOSIS_SERVER_OLLAMA_MODEL=embeddinggemma ../Gnosis/target/debug/gnosis-server --port 8080` + relaunch with `--gnosis` → `gnosis.status state:"Ready"` and the pane renders `"Gnosis engineState: Ready… all subsystems up"`. Observed during this pass: the launcher's spawned server PANICKED (`bind loopback 127.0.0.1: Address already in use`) yet the launcher still printed `gnosis-server READY` (it probed the pre-existing server). | `scripts/start-app.sh` only spawns the engine under `--mode=gnosis` and trusts the `/engine/status` probe without verifying it answered from the process it spawned. | Add a documented single demo start (engine + app) and make the launcher FAIL LOUD when its spawned server dies (check the child's liveness/exit, not just the port). Launcher-only; no JS contract change. |
| **GNOSIS-SIDEBAR-SEAM-MISSING (2026-09-15, live — gnosis enrichment pass)** | **high (HOST) — OPEN (NEW; an RCA-12 layer miss)** | The `gnosis-wikis` and `gnosis-documents` app-graph panes are INERT in the live app with a READY engine: the wiki list renders (from the host boot read) but a real click on a wiki row does nothing, and the document pane stays `"No documents — select a wiki"` — `li[data-document-id="doc-1"]` never appears. The IPC path itself WORKS: a direct `window.provident.gnosis.documents('gnosis.document.list',{wikiId:'wiki-0'})` resolves `total:1, items:[doc-1]` from the live engine. | Live (`docs/specs/gnosis-enrichment-live-report-2026-09-15.md` UF-GNOSIS-1/2/6, app MCP `:3790` + CDP `:9225`, engine `:8080` READY with `wiki-0` + `doc-1`): real hit-tested click on `li[data-wiki-id="wiki-0"]` → app-graph `reassembled=false`, no `"Wiki: …"`; the document list never renders; `[DIAG]` `provident.dispatch` on the same node runs the handler (`handlers=[{name:'gnosis-wikis-select',event:'click'}]`) but `results=[null]` — the handler fires and its seam no-ops. Delete/publish/unpublish/archive controls are also absent and make no wire call. | `src/renderer/renderer.ts:1012-1015` constructs `SidebarPanes({ gnosis: { status, query } })` — the `gnosis` seam object carries only `status` and `query`, so `src/renderer/sidebar-panes.ts:2879-2880` registers only `gnosisStatus`/`gnosisQuery` on the bridge. `gnosisDocuments`/`gnosisWikis` therefore stay at the preload's **no-op defaults** (`src/main/preload.ts:307-308`), and `GnosisCrudPanes` (instantiated at `renderer.ts:995`) is never connected to them — all 11 handler bodies in `src/renderer/gnosis-crud-panes.ts` are unreachable. The `AD-2026-09-14-2` preload-parity guard is ONE-DIRECTIONAL (it checks every renderer `methods` key exists on the preload proxy, never that every preload gnosis key has a renderer registration), so the node suites stayed green while the assembled surface was dead — the RCA-12 envelope-green≠app-green class. | Register the two missing seams: pass `gnosisDocuments`/`gnosisWikis` through `SidebarPanes`'s `gnosis` option (renderer.ts) and expose them in the `methods` object (sidebar-panes.ts) routed to `gnosisCrudPanes.documents(...)`/`wikis(...)`; AND make the parity guard BIDIRECTIONAL (every `gnosis*` key the preload exposes must be registered by the renderer, else FAIL LOUD at boot) so this class cannot silently recur. Add a node test that the renderer registers every gnosis seam the preload exposes. |
| **HOST-ENGINE-QUERY-POST-NO-ENVELOPE (2026-09-15, live — gnosis enrichment pass)** | **high (HOST + spec drift) — OPEN (NEW)** | The app's engine-backed `rag.query` POST path sends a BARE JSON body where the engine requires the F2 envelope, so every engine query over POST fails with a masked error: the host reports `malformed envelope: invalid JSON` while the engine actually answered **HTTP 400 `text/plain "request decode failed"`**. (The GET/stream path works.) | Live: `src/main/engine-rag-store.ts` `ragQuery` (~`:920`) `fetch(engineUrl + '/rag/query', {body: JSON.stringify({query, ...opts})})` → engine `Envelope::from_json` rejects → 400 `request decode failed` → the host's `decodeEnvelope` (~`:320`) `JSON.parse`s the text → `malformed envelope: invalid JSON`. Expected body: `{schemaVersion:1, idFormat:'opaque-string-v1', payload:{query, mode, topK}}`. | The engine's wire contract (§12/§4.2 envelopes) was never applied on this one call site; the sibling stream path builds the envelope correctly. Spec drift too: `docs/specs/unit-gn-engine-integration.md:454` pins the WRONG (bare) request body, so the contract doc would have blessed the bug. | Wrap the POST body in the F2 envelope (`{schemaVersion: CURRENT, idFormat: ID_FORMAT_OPAQUE_STRING_V1, payload:{query, mode?, topK?}}`), correct the spec §body at `unit-gn-engine-integration.md:454`, and make the error path SURFACE the engine's status/body instead of masking a deterministic 400 as a JSON-parse error (`HOST-ENGINE-ERROR-MASKING`). Cover with a test asserting the exact request envelope the engine expects. |
| **GNOSIS-ENGINE-ENRICHMENT-SURFACE-ABSENT (2026-09-15, live — gnosis enrichment pass)** | **medium (GNOSIS ENGINE — HANDOFF)** | The engine's graph/vector legs are not usable end-to-end over its own HTTP surface: `vector_index_unavailable` on the graph store (the vector index is never built for the server's store) and 9 probed traversal/community/enrichment paths answer **404** — there is no route for the enrichment surfaces (`declareCommunity`/`updateCommunitySummary`/`resolveEntities`/`mergeFacts`) that F4-LLM would drive. | Live: `GET /rag/stream?mode=vector` → `event: error / data:{"code":"vector_index_unavailable","message":"vector index is not built"}` while `/engine/status` reports `vector:true, embedding:true` (the provider is wired; the INDEX is not built); 9 probed enrichment/traversal/community paths → HTTP 404. | The P2 server wires an `OllamaProvider` but never builds/adopts a vector index for its store, and exposes only `/rag/query`, `/rag/stream`, `/engine/status`, `/documents*`, `/wikis*` — the enrichment surfaces are not routed. | (gnosis-repo — hand off) Build/adopt the vector index in the server store for the embedding provider, and route the enrichment surfaces the F4-LLM integration needs. Recorded in `docs/HANDOFF.md`. |
| **VECTOR-IMPORT-NO-BODY-VECTORS (2026-09-15, live — ollama/embeddinggemma vector-mode pass)** | **high (HOST) — OPEN (NEW)** | In vector mode, documents brought in by an import are NOT retrievable by their BODY text until each node is edited once: `rag.query` returns `[]` for body phrases right after the import, while a single `edit.set_content` on a node makes that node (and only it) retrievable. This is the "vector/graph enrichment after import" gap — the store has the docs, the engine is `promoted`, and the query still finds nothing. | Live (vector-mode app, `PROVIDENT_EMBEDDING_PROVIDER=ollama` + `PROVIDENT_EMBEDDING_MODEL=embeddinggemma`, fresh `HOME`, engine log `vector boot: build complete (embedded 0 …)` because the boot ran on an EMPTY store): (1) `edit.import_markdown` of `.live-corpus/alpha.md`+`beta.md` → `rag.list_documents` = `['.live-corpus/alpha','.live-corpus/beta']`, but `rag.query {query:'document alpha documents'}` → `results: []` and the cache file stays `{"version":1,"entries":[]}` (26 bytes); (2) `edit.set_content {nodeId:'.live-corpus/alpha:p:1'}` → 2.5 s later the SAME query returns the `p:1` snippet at score 0.64 and the cache grows to 9 756 bytes; (3) `edit.set_content` on `beta:p:1` → `rag.query {query:'vector enrichment probe'}` returns the beta snippet at 0.77. **Boot-time indexing DOES work** when the docs exist before boot: restarting with the corpus present logs `vector boot: build complete (embedded 3, cacheHits 1, skipped empty 2)` and all three body-phrase queries return ranked results (α:p:1 0.64 / 0.75 for `document alpha documents`). | `src/main/main.ts` import success path (~`:198`) calls `runtime.getDefaultEngine().onStoreChanged('structural', result.documentIds, [])` — the reconcile is handed ONLY the imported DOCUMENT ROOT ids. The engine forwards that list verbatim to the embedder hook (`src/main/retrieval.ts` `onStoreChanged`), and `src/main/embeddings.ts` `addToVectorIndex` embeds `node.content` — a document root's `content` is `''`, so it is recorded `skipped: 'empty'` and the body nodes are never embedded. (The LEXICAL index escapes this by accident: `nodeText()` = `content + children[].content`, so the root's inline children carry the text into BM25.) | Pass the imported node ids (not just the document roots) to the reconcile — e.g. widen `importMarkdownCorpus`'s result (or have the caller walk `store.getNode(docId).ownedNodeIds` + the doc-child/next-section closure) so every imported CONTENT node reaches `onStoreChanged`; alternatively make `addToVectorIndex` fall back to `nodeText(node)` (content + inline children) the way the lexical index does, since a section/div root legitimately carries its text in children. Cover with a vector-mode import test (import → query the body phrase → expect ≥1 result) — currently NO test covers import→vector retrieval (the W2/W5 suites cover boot-build and edit-reconcile only). |
| **GNOSIS-ENGINE-QUERY-MODE-IGNORED (2026-09-15, live — gnosis engine pass)** | **high (GNOSIS ENGINE — HANDOFF, not patched here)** | The gnosis-server's `POST /rag/query` IGNORES the request's retrieval `mode` and `topK`: `Flat`, `Vector`, `Graph` and `Hybrid` all return byte-identical results and a `Flat` trace with `top_k: 10`, so graph/vector/hybrid retrieval and the caller's topK are unreachable over the engine's own query endpoint. (The SSE endpoint `GET /rag/stream` DOES honor `mode`, which is how the two legs were confirmed to exist.) | Live against `gnosis-server` (started with `GNOSIS_SERVER_OLLAMA_URL=http://127.0.0.1:11434`, `GNOSIS_SERVER_OLLAMA_MODEL=embeddinggemma`, `/engine/status` `state:Ready` + `{store,graph,lexical,vector,embedding,reranker}=true`; wiki `wiki-0`, document `doc-1` with a valid Provident graph): `POST /rag/query` body `{"payload":{"query":"embeddinggemma vector leg","mode":"Flat|Vector|Graph|Hybrid","topK":5}}` → all four replies have `trace: {"Flat":{"mode":"Flat","engine":"gnosis","top_k":10,"source":"Local"}}` and the SAME two results (`n1` score 2.6384988372299447, `n2` 0.5897495348410585); the payload result signatures are equal across all four modes (Flat===Vector===Graph===Hybrid = true), and `topK: 3` vs `5` changes nothing (`top_k` stays 10). `GET /rag/stream?query=x&mode=vector` → `event: error … code:"vector_index_unavailable"`; `mode=graph` → `trace: {"Graph":[]}`; `mode=hybrid` → `trace: {"Hybrid"…}` — i.e. the modes are real and only the POST path drops them. | `../Gnosis/src/bin/gnosis_server.rs` `rag_query_handler` (~`:156-186`) decodes only `payload.query` and calls `store.rag_query(&query, &RagQueryOptions::default())` — the `mode`/`topK` fields are never read; the sibling `rag_stream_handler` (~`:188`) DOES map `mode`/`topK` into `RagQueryOptions`, so the fix shape already exists in the same file. | (gnosis-repo, upstream-owned — hand this off, never patch here) In `rag_query_handler`, mirror `rag_stream_handler`: read `payload.topK` (`topK`|`top_k`) and `payload.mode` (accept the same `flat|graph|vector|hybrid` strings, case-insensitively) into `RagQueryOptions` and pass them to `store.rag_query`. Add a server-level test that the four modes produce different traces/results on a populated graph. Recorded in `docs/HANDOFF.md`. |

| **LIVE-12 VISIBILITY-MENU-CLOSES** | low (HOST) — **ACCEPTED (2026-09-14, per the user — the native-menu-close is an Electron limitation; the compromise is fine)** | The native View → Panes visibility submenu CLOSES immediately when a checkbox is clicked, so the user must reopen the menu to toggle each additional pane. | Click a Pane-visibility checkbox in the native menu; the submenu closes after the first toggle. | The native Electron `Menu` submenu (U-SHELL-8 `buildMenuTemplate` checkbox items) closes on item click by default — there is no native "stay open" for a checkbox submenu. | Document as a native-menu limitation + add an in-pane (provident) visibility control (in the settings modal / pane headers) so multiple toggles can be done without reopening the menu. |
| **LIVE-UF1 NEW-TAB-INERT (re-confirmed 2026-09-15, live)** | high (HOST) — **OPEN (expected user-bug finding; contradicts the prior LIVE-3 "FIXED partial" row)** | The tab-strip new-tab button (`[data-tab-new]`) is a silent no-op live: no NEW `.tab` is created and no usable view (landing/`#stage`) opens — the active Search tab is merely re-titled `search-2`. | Click new-tab in the running app; `.tab` count stays 3→3, `#stage-landing=false`, `#stage=false`. | `TabStrip.newTab()` needs an explicit target; with the seeded store it does not mint a usable tab target (the LIVE-3 "FIXED" wiring opens only the landing/empty draft on a NO-target path, which does not fire for the seeded non-empty store here). | Wire new-tab to a real target for the current context (a new/empty document draft that commits on edit, or the landing) so a click is never a silent no-op. **Reproduced 2026-09-15 by `scripts/live-drive.mjs --connect user1_tab_new` (FAIL).** See `docs/specs/live-user-flow-scenarios.md` §1. |
| **LIVE-UF2 PANE-DRAG-INERT (2026-09-15, live)** | high (HOST) — **FIXED + LIVE-CONFIRMED (2026-09-15, user-flow-audit pass)** | A REAL pointer drag on a pane's header did NOT relocate or reorder the pane — slot order and the pane's `y` were unchanged. | The original reproduction dragged from an OFF-VIEWPORT header (the target `gnosis-documents` sat ~9 600px below the fold on the tall page), so the gesture never started — a precondition hazard, not the whole story. **Re-verified with the header scrolled into view (hit-tested `hit=pane-collapse-doc-nav`, real CDP down/move×4/up PAST the sibling): the left-zone order changed `doc-nav,search` → `search,doc-nav`** (`/tmp` probe 2026-09-15, and the §6.1 report's NEW-1). | The drag started on a BUTTON control (the collapse-toggle header) and the renderer `isInteractiveControl` guard refused to start a pane drag on a control, so a user grabbing the header got no relocation. | **FIXED (2026-09-15, the F-1 fix):** the pane HEADER is now the explicit pane-drag GRAB SURFACE and is EXEMPT from the `isInteractiveControl` guard, while the pane BODY is no longer a gesture surface at all — so the header drag reaches the U-SHELL-4 relocate seam AND a click on a body row reaches its own handler. Pointer capture is deferred past a 4px drag threshold so a header CLICK still toggles the collapse. **LIVE-CONFIRMED:** real header drag reorders the zone slots (`doc-nav,search` → `search,doc-nav`); real header click collapses/expands (`uf_panes_1` PASS). The block `user2_pane_drag` itself PARKS when its own precondition (an in-viewport sibling) is unmet, so the live evidence is the probe + NEW-1. |
| **LIVE-UF5 HISTORY-IN-MAIN-CANVAS (2026-09-15, live)** | high (HOST) — **OPEN (expected user-bug finding)** | The history/undo/redo segment (`#pane-history` / `[data-role="history"]`) renders inside the MAIN canvas `#zone:main`, NOT inside a pane frame. | Locate `[data-role="history"]`; its DOM ancestry is `#pane-history → #zone:main → #wiki-root → #app → MAIN.layout → BODY`. | The history segment is authored/attached into the main `#zone:main` stage rather than into a pane (e.g. a left-zone `[data-role]` pane that holds a C16 history sub-pane). | Render the history/undo/redo segment inside a `.pane-frame` in the side pane zone(s) (the U-EDIT-2 history sub-pane), not in the central canvas. **Reproduced 2026-09-15 by `scripts/live-drive.mjs --connect user5_history_in_pane` (FAIL).** See `docs/specs/live-user-flow-scenarios.md` §5. |
| **LIVE-UF6 DUPLICATE-EDITABLE-PARAGRAPH (2026-09-15, live; RE-CONFIRMED on fresh data; FIXED + LIVE-CONFIRMED 2026-09-16)** | high (HOST) — **FIXED + LIVE-CONFIRMED (2026-09-16)** | In the alpha document the RAG paragraph `.live-corpus/alpha:p:1` ("Settings modal (C3). The **document alpha** documents.") is materialized TWICE as a user-visible editable paragraph — once nested INSIDE the `<h1 data-doc-head>` section header (rendered with header styling) and once as a sibling content root directly in `zone:main`. Editing the TOP nested copy cascades DOWN to the bottom; editing the bottom does NOT cascade up. | Focus alpha (CDP probe 2026-09-15): `[data-rag-node-id=".live-corpus/alpha:p:1"]` matches **4** elements, of which **2 are editable `<P contenteditable="true">`** (the inline `<strong>` children also carry the parent's `data-rag-node-id`). Both P copies carry `id`/`rag-` `.live-corpus/alpha:p:1`. ANCESTRY of the nested copy: `P#rag-…:p:1 → H1#rag-…:section:1 → #zone:main → #wiki-root → #app.card → MAIN.layout → BODY`; of the sibling copy: `P#…:p:1 → #zone:main`. Editing the top copy (CDP click→focus + `Input.insertText` + blur/commit) put the marker into BOTH P copies (2 marker-copies; `provident.get_rendered_html` marker occurrences=4) — confirming the one-way TOP→BOTTOM cascade. Also: a single `edit.set_content` on the document root reset its markdown but LEFT the separately-stored `p:1` node holding the edit — further evidence the paragraph is standalone-materialized AND nested. **Reproduced 2026-09-15 by `scripts/live-drive.mjs --connect repro_dup_para` (FAIL on all 3 asserts: SINGLE-RENDER editable count=2 (expect 1); NOT-NESTED p:1 IS a descendant of the `[data-doc-head]` h1; EDIT-CASCADE primary = 2 editable p:1 paragraphs).**  **NOT A LEGACY-DATA ARTIFACT (2026-09-16, user hypothesis tested):** the whole store was deleted (65 docs) and RE-IMPORTED from the current markdown sources (225 files, `docs/` + `docs/specs/` + `.live-corpus/`), and the defect is UNCHANGED on the fresh data — the CURRENT importer itself stores each section→first-paragraph pair with BOTH a `doc-child` AND a `parent-child` edge (`rag.get_edges` on a fresh `.live-corpus/alpha:p:1` → `["doc-child:.live-corpus/alpha:section:1->.live-corpus/alpha:p:1","parent-child:.live-corpus/alpha:section:1->.live-corpus/alpha:p:1"]`), and the fresh `docs/decisions` renders `p:1..p:5` each TWICE (one copy nested in the `[data-doc-head]` H1, one a sibling). So the fix is in the traversal/materialization (and/or the importer's edge emission), not a data refresh. | The section's parent-child paragraph is standalone-materialized as its own content root AND emitted again nested inside `buildSubtree(section)` — a double emission. Candidate: the standalone materialization path in `src/main/traversal.ts:454-483` (the MULTI-PARENT-DUPLICATE loop) plus the per-section `content.push(buildSubtree(section))` at `:450-452`. The traversal's own finding notes (`:444-449`, `:454-462`) acknowledge that a node which is BOTH a section AND a doc-child of another section materializes TWICE — and this same double-emission applies to a section's direct child paragraph that is also a `parent-child` target of the document head/section, because it is emitted nested (as a child of the header subtree) AND treated as a standalone subtree root by the `:470-483` loop. The paragraph therefore shows nested-with-header-styling AND as a separate editable sibling. | (host / upstream-owned decision) Emit each non-section paragraph subtree EXACTLY ONCE: keep a section's direct child paragraph nested within its section subtree (never a standalone content root), so `:470-483` must skip nodes that are already direct children of an in-scope section subtree; only standalone-materialize a genuine section (`:450-452`) or a genuine multi-parent shared node — never duplicating a section's own child into its own content root. |
| **LIVE-UF7 ZONE-RESIZE-NO-GRIP (2026-09-15, live)** | high (HOST) — **OPEN (expected user-bug finding)** | The side zone CANNOT be resized by dragging — a real user gesture at the visible zone boundary leaves the width unchanged, because there is no visible/grippable gutter to grab. | Capture `[data-zone="left"]` width (`220px`), drive a REAL CDP coordinate drag at its right (boundary) edge `x=253` (+72px, release); width stays `220→220` (Δ0). The `.gutter[data-zone="left"]` element measures `0×0` and sits at `(0,0)` (grid-area `auto`, no track), so a real pointer at the boundary hits the contenteditable paragraph, NOT a gutter. **[DIAG]** the `startGutter` seam DOES resize when a synthetic `pointerdown` is dispatched directly ON the hidden `.gutter` element (`220→160`, changed) — but that element is unreachable by a real user, so the resize surface is effectively absent. | (observed) `.gutter[data-zone]` all `0×0` at `(0,0)`, `grid-area:auto`, `cursor:col-resize`; `#wiki-root` columns `220px 779.721px 220px` with `gap:normal`(≈0). A real CDP drag at the boundary (`elementFromPoint(253,343)=P.contenteditable`) triggers no gutter match → no resize. | The `.gutter[data-zone]` resize handles are authored but assigned NO grid track / zero grid area, so they are not rendered at the zone boundary; the document-delegated `.layout .gutter[data-zone]` wiring never sees a real user `pointerdown` near the edge. | Give each zone gutter a REAL grid track + a non-zero hit-area at the column boundary (author the resize edge in the `#wiki-root` grid or a visible handle) so a real boundary drag reaches `startGutter`. **Reproduced 2026-09-15 by `scripts/live-drive.mjs --connect --no-seed --display=0 user7_zone_resize` (FAIL: REAL boundary drag Δ0).** See `docs/specs/live-user-flow-scenarios.md` §7. |
| **LIVE-UF8 ZONE-NO-BOUNDARY (2026-09-15, live)** | high (HOST) — **OPEN (expected user-bug finding)** | The side zone and the central stage appear as ONE surface: no visible separator (border / grid-gap / background / gutter) divides `[data-zone="left"]` from `#zone:main`. | Computed: `zone:left` & `zone:main` both `background: rgba(0,0,0,0)`, `border-left/right: 0px none`, `box-shadow: none`; `#wiki-root` grid `gap:normal`(≈0px), columns `160px 839.721px 220px` (left cell transparent); `.gutter[data-zone]` all `0×0`, `grid-area:auto` (no drawn divider); `#app.card` bg `rgb(38,42,46)` floods both cells identically. | Assert `user8_zone_boundary`: boundary-visible=false — `zoneBorder=false bgDiff=false gutterVisible=false gridGap=0px>0=false`. | The side-zone and stage cells carry no background/border, the `#wiki-root` grid has no gap, and the gutters are 0-size, so no edge/divider is drawn between the two cells — they read as one continuous surface. | Author a visible boundary between the side zone and the stage: a background/border on the zone column, a real grid-`gap`, or a visible gutter, so the side pane is visually distinct from the central canvas. **Reproduced 2026-09-15 by `scripts/live-drive.mjs --connect --no-seed --display=0 user8_zone_boundary` (FAIL).** See `docs/specs/live-user-flow-scenarios.md` §8. |
| **LIVE-UF9 SEARCH-OPEN-IN-TAB-SHOWS-DOC (2026-09-15, live)** | high (HOST) — **OPEN (expected user-bug finding; HOST-5 `paneTabExpand`)** | The Search pane's "Open in a tab" (`#pane-search-expand-tab`) creates a NEW Search tab but the new tab's active content is the OPEN DOCUMENT body, NOT the search view. | Focus the open alpha tab, then drive a REAL click on `#pane-search-expand-tab`: `.tab` count `5→6`, the active tab becomes `"Search: search-6"`, but `#zone:main` renders `"AlphaSettings modal (C3). The document alpha documents…"` (`isDocumentBody=true`) with `searchInput=false` and `ragResults=false`. | The `paneTabExpand` handler mints a new tab but does NOT mount the search view into the new tab's stage — the stage keeps the last-focused document content. | The newly-created search tab must materialize its SEARCH view (input/results) in `#zone:main` (per HOST-5), not reuse the open document's stage content. **Reproduced 2026-09-15 by `scripts/live-drive.mjs --connect --no-seed --display=0 user9_search_open_in_tab` (FAIL).** See `docs/specs/live-user-flow-scenarios.md` §9. |
| **LIVE-UF10 COLLAPSED-ZONE-TAB-HORIZONTAL (2026-09-15, live)** | high (HOST) — **OPEN (expected user-bug finding)** | When a side zone is minimized/collapsed, its pane-label strips read the text HORIZONTALLY (left-to-right) — they should read VERTICALLY (bottom-to-top). | Minimize the sidebar (`#zone-minimize-left`): all 7 `#zone-tab-left-*` `.pane-tab` labels report `writing-mode: horizontal-tb`, `text-orientation: mixed`, and are sized short-and-wide (`96×21`, `82×21`, `128×21`, `57×21` "Search", …) — horizontal text, not a rotated/bottom-up label. | The collapsed `.pane-tab` label CSS applies no vertical writing-mode / rotation; the label keeps the horizontal default. | Add `writing-mode: vertical-rl` (or a `rotate(180deg)` / `90deg` transform with upright `text-orientation`) to the collapsed zone labels so they read bottom-to-top vertically. **Reproduced 2026-09-15 by `scripts/live-drive.mjs --connect --no-seed --display=0 user10_collapse_vertical_text` (FAIL).** See `docs/specs/live-user-flow-scenarios.md` §10. |
| **F-1 PANE-BODY-GESTURE-SWALLOWED (2026-09-15, live — user-flow-audit pass)** | **high (HOST) — FIXED + LIVE-CONFIRMED (2026-09-15, same pass)** | A REAL mouse click on ANY clickable provident row/control INSIDE an app-graph pane body was swallowed: the pane-drag gesture claimed the pointer and the `click` event retargeted to `.pane-frame`, so the row's handler never ran. The doc-nav document rows and the search result rows were inert to a real user. | Live (`docs/specs/user-flow-audit-coverage-2026-09-15.md`, blocks `uf_panes_12`, `uf_tabs_7`, `uf_panes_14`): a REAL click on `li[data-document-id=".live-corpus/beta"]` left the stage on the previous document; click-probe `[pointerdown:<li>\|in#pane-doc-nav, mouseup:pane-doc-nav, click:pane-doc-nav]`. A real click on a search result row opened no tab (tabs 4→4). **[DIAG]** a native `.click()` on the same rows WORKED (stage switches; tabs 4→5) — the handler + dispatch seam were intact; the REAL gesture was what died. | `renderer.ts` `GESTURE_SELECTOR = '.gutter[data-zone], .pane-frame[data-pane-id]'` + `onDocumentPointerDown` started a pane drag from a `pointerdown` ANYWHERE inside the frame (the frame BODY included) and called `setPointerCapture` on `.pane-frame` — an interaction control the guard missed (a provident-compiled handler element carries only `data-node-id`/`data-wire`/class `is-clickable`, not `on:click`), AND capture on the frame retargets the gesture's `click` to the frame. **Second layer found during the fix:** capturing at the pointerdown retargeted the pane HEADER's own `click`, so the collapse/expand toggle became inert too. | **FIXED (2026-09-15):** the pane-drag gesture surface is the pane HEADER only (`GESTURE_SELECTOR = '.gutter[data-zone], .pane-collapse-toggle'`, the frame resolved from the header's parent for the pane id + the capture target), and pointer capture is **DEFERRED until the gesture travels past a 4px drag threshold** (`PANE_DRAG_CAPTURE_THRESHOLD`, claimed in `onGestureMove`) so the pane HEADER's own `click` (the collapse/expand toggle) still reaches its handler — F-1b, found during the live fix. **LIVE-CONFIRMED (2026-09-15, the same live pass):** the three rows the defect was recorded from now PASS on real gestures — `uf_panes_12` (U-1: a real doc-nav row click switches the stage AND marks `data-current`), `uf_tabs_7` (U-2: a real search-result row click opens the document tab while the search tab stays) and `uf_panes_14` (U-2/U-3: real type + submit + hover + row click → a new tab); the click-probe's event chain now names the ROW's own compiled node (`pointerdown/mouseup/click = preempt-node-* in#pane-*`) instead of retargeting to `.pane-frame[data-pane-id]`; and the header toggle that the old capture had also broken works again (`uf_panes_1`: doc-nav body `li=66`/h=9681px → real click → `is-collapsed=true`, `li=0`, h=37px header-only → second real click → `li=66`, h=9681px, same pane root `#pane-doc-nav`). Evidence the running bundle carries the fix: `dist/renderer/renderer.js` contains `.gutter[data-zone], .pane-collapse-toggle` and `PANE_DRAG_CAPTURE_THRESHOLD`. Pinned by `tests/renderer-pane-drag-surface.test.ts` + the re-pinned `tests/unit-u-shell-shell-wiring{,-adversarial}.test.ts`. See `docs/specs/user-flow-audit-coverage-2026-09-15.md` §4 F-1. | doc-nav body `li=66`/h=9681px → real click → `is-collapsed=true`, `li=0`, h=37px header-only → second real click → `li=66`, h=9681px, same pane root `#pane-doc-nav`). Evidence the running bundle carries the fix: `dist/renderer/renderer.js` contains `.gutter[data-zone], .pane-collapse-toggle` and `PANE_DRAG_CAPTURE_THRESHOLD`. Pinned by `tests/renderer-pane-drag-surface.test.ts` + the re-pinned `tests/unit-u-shell-shell-wiring{,-adversarial}.test.ts`. See `docs/specs/user-flow-audit-coverage-2026-09-15.md` §4 F-1.tureMove`) so a pure click never captures and always reaches its own control. Red→green: `tests/renderer-pane-drag-surface.test.ts` (new, 19) + the re-pinned `unit-u-shell-shell-wiring` (33) / `-adversarial` (16) = 68 green; `src/shared/dom-shim.ts` dropped its fabricated `pointerId: 1` default so the malformed/no-pointerId state is expressible. **LIVE-CONFIRMED:** real clicks now focus the clicked doc-nav document (`uf_panes_12` PASS: stage `rag-.live-corpus/alpha:section:1` → `rag-.live-corpus/beta:section:1`, `li[data-current]=true`) and open a new document tab from a search result row (`uf_tabs_7`/`uf_panes_14` PASS: tabs `5→6`/`6→7` with the click log showing `mouseup`/`click` on the row itself); the header toggle is live again (`uf_panes_1` PASS: 66 `li` → header-only 37px → 66 `li` on the same root). |
| **F-2 WIKI-ROOT-MOUNT-LEAK (2026-09-15, live — user-flow-audit pass)** | **high (HOST) — OPEN (NEW)** | Duplicate `#wiki-root` mounts LEAK on the app-graph re-assembly: a fresh app shows 1 mount, but after the pass's pane-set transitions the page holds **7 `#wiki-root` mounts (6 childless/stale)**, `#zone:main` is pushed to y≈89 608 and the page grows to ≈209 473px — the stale mounts are real scrollable page height the user sees as a giant blank region. | Live (`uf_mount_diag`, 2026-09-15 re-emission): `#wiki-root` **mounts=7 (childless/stale=6)** boxes `[-89814,29904,0] [-59910,29904,0] [-30007,29904,0] [-103,29904,0] [29801,29904,0] [59704,29904,0] [89608,29904,6]`, `#zone:main y=89608`, page `scrollHeight=209473` (viewport 721) ⇒ `singleRootMountAndStageInViewport=false`; a fresh boot shows 1 mount, so the leak accumulates with the pass's transitions (an earlier run reached 9 mounts / 106 215px). **TRIGGER NOW MEASURED (`docs/specs/user-flow-audit-coverage-2026-09-15.md` §4 F-2/NEW-2, reported as the §5.U U-4 row):** two REAL settings-modal toggle clicks that EMPTY the enabled-pane set move the count **7→8** (page 77 021→120 64px) and REFILLING it moves **8→9** (page →100 713px; `#zone:main` y 66039→2492→33035) — an explicit delta with NO reload, so the trigger is the empty↔filled pane-set transition. `uf_mount_leak_diag` shows the count is STABLE (7→7) across modal open/close, a doc-nav row click and the zone minimize/expand pair. **HARNESS CAVEAT (report NEW-4):** the `--connect` attach re-seeds `.live-corpus` (a pane-set refill), so the ABSOLUTE mount count is partly harness-accumulated and must not be quoted as a user-session number; the NEW-2 deltas (measured without a reload) are unaffected. | The app-graph re-mount path does not tear down the previous `#wiki-root` subtree when the pane set is re-assembled (the renderer content-root reconcile / `Runtime.destroyRoot` allocation vs the stage assembly). **The trigger is now measured (report §4 NEW-2: the empty↔filled pane-set transition, +1 mount on empty and +1 on refill) — no focused repro is owed; the fix + the assembled-layer regression test are.** | Tear down (or reuse) the previous `#wiki-root` mount on re-assembly so exactly ONE mount exists; add an assembly/reconciliation test asserting a single `#wiki-root` after a pane-set round-trip (RCA-12 assembled-layer test). |
| **F-3 EMPTY-ZONE-TRACK-NOT-COLLAPSED (2026-09-15, live — user-flow-audit pass)** | **medium (HOST) — FIXED + LIVE-CONFIRMED (2026-09-15, same pass)** | An empty side zone did NOT collapse its grid track and the stage did NOT reclaim the space. With ZERO enabled+placed panes in `left`, the zone got `is-empty` + `display:none` and 0 frames — but the `#wiki-root` grid columns stayed `160px 853.86px 220px` and the stage stayed `x=193, w=854` (unchanged). | Live (`uf_layout_10`): FILLED `zone:left` 160px @x=33, stage @x=193 w=854, grid `160px 853.86px 220px`; REAL modal clicks disabled both panes → `is-empty` + `display:none`, frames 0, grid STILL `160px 853.86px 220px` ⇒ `gridTrackCollapsed=false`, `stageReclaimed=false`. | `src/renderer/index.html` defined `--zone-*-track: 0px` (the intended collapsed track) but the `#wiki-root` grid consumed `--zone-*-size` — the collapse variable was DEFINED BUT NEVER CONSUMED, and the `is-empty` collapse rules targeted `.layout:has(...)` where the grid element's own declaration won the cascade. | **FIXED (2026-09-15):** `#app > #wiki-root` now declares `--zone-<z>-track: var(--zone-<z>-size, <default>px)` for all four zones and its `grid-template-columns`/`-rows` consume `var(--zone-<z>-track)`; the four collapse rules were re-targeted to the grid element itself (`#app > #wiki-root:has([data-zone='<z>'].is-empty:not(.is-revealed))`); the new pure `zoneTrackCssVars(registry, layout, opts?)` (`src/renderer/layout-state.ts`) + `applyZoneTracks()` (`src/renderer/sidebar-panes.ts`, wired at the 3 census/mount sites) write the collapsed `0px`. Red→green: `tests/renderer-empty-zone-track.test.ts` (new, 25) + the F-3 fixture repair (one-pass remand) → 25/25 green, `unit-u-shell-1-w2n3-zone-size-grid` (4) + `unit-u-shell-1-layout-zones` (39) stay green. **LIVE-CONFIRMED:** `uf_layout_10` PASS — grid `160px 741.209px 0px` (filled) → `0px 901.209px 0px` (empty) with the stage `x 193→33`, `width 741→901` (the reclaimed 160px) — `uf_layout_10` PASS on the §5.U U-5 row (`gridTrackCollapsed=true`, `stageReclaimed=true`), with `zone:left` `is-empty` + `display:none` + 0 frames; the FILLED sample's right track already reads `0px` (the fix's own projection) and `dist/renderer/renderer.js` carries `zoneTrackCssVars` / `--zone-left-track`. Restore: real clicks re-enabled both panes (frames 2, leftWidth 160, census `"doc-nav, search"`, all four clicks `path=cdp`). Pinned by `tests/renderer-empty-zone-track.test.ts` (25). See `docs/specs/user-flow-audit-coverage-2026-09-15.md` §4 F-3.ted size restored on refill. |
| **LIVE-1 BRANDING** | low (HOST) — **FIXED (2026-09-14); RE-SCOPED + RE-FIXED (2026-09-16, user report)** | The shell read the Provident-Electron DEMO title (fixed 2026-09-14 by rebranding the `<header>`). **2026-09-16 user report:** that nameplate + its description took ~90px of every page. Re-scoped: the NAME is now the `.app-nameplate` H1 in the top-bar row (`#tab-strip`) and the DESCRIPTION renders only on the landing page (`landingContent()`); the `<header>` block is removed. Live-verified: nameplate box `[4,0,98,36]` inside the strip, `headerPresent=false`, app top 126 → 36, landing children `H2:"Astrographer"` + `P:"Local graph-RAG wiki — document, link, and retrieve your knowledge base."`; nameplate survives a tab re-render. Decision `APP-BRANDING-IN-TOP-BAR` ACTIVE. | | The shell still reads the Provident-Electron DEMO (`index.html` `<header><h1>Provident-Electron — MCP endpoint demo</h1>` + the subtitle). | Boot the app; the top header is the demo title, not an Astrographer one. | The UI-overhaul never re-authored the `index.html` `<header>` (leftover from the fork demo). | Rebrand the header to Astrographer + a real landing intro; fold in LIVE-4. |
| **LIVE-2 TAB-PLACEMENT** | medium (HOST) — **FIXED (2026-09-14)** | The tab strip renders BELOW the header title, not in the window top bar (U-SHELL-9a intended browser-style tabs in the outer-shell top bar). | Boot the app; the title `<header>` is above the `#tab-strip` (which is inside `<main class="layout">`'s top-bar grid row). | The `<header>` sits BEFORE `<main class="layout">`, so visually the title is the top row and the tabs are under it. | Restructure the shell so the tab strip is the true top row (move/remove the title header, or put the tabs above it). **FIXED (2026-09-14 — the tab strip is the window OUTER-CHROME title bar: BrowserWindow `titleBarStyle:'hidden'` + `titleBarOverlay` (min/max/close overlay top-right), `#tab-strip` is the top drag region)** |
| **LIVE-3 NEW-TAB-INERT** | medium (HOST) — **FIXED (2026-09-14, partial)** | The tab-strip new-tab button (`.tab-new`) does nothing live. | Click new-tab in the running app; no usable tab opens. | `TabStrip.newTab()` requires an explicit target context; with no store/docs (and no C14-lite import root yet) it opens a null/empty tab (a no-op). | Wire new-tab to a real default target (the first doc / a landing), or disable it when no target exists. **FIXED (2026-09-14, LIVE-3 design): a no-target New Tab with NO wiki now opens the LANDING (first-run guide); with a focused store it would open a NEW/EMPTY document draft (commit-on-edit) — deferred to the create-document (U-D9) path.** |
| **LIVE-4 NO-LANDING** | medium (HOST) — **FIXED + LIVE-CONFIRMED (2026-09-15, U-LIVE4: `boot_landing` live battery PASS)** | No landing page: the stage starts on the empty placeholder envelope and there is no welcome/intro. | Boot the app with an empty store; the central stage is blank. | The stage mounts the placeholder bootstrap envelope; nothing authors a landing into the pane-inclusive stage assembly. | **FIXED (host, 2026-09-15, U-LIVE4):** `sidebar-panes.ts` `emptyStoreEnvelope()` now authors the landing stage body (the `applyStageBody` shape) into the empty-store traversal envelope so `assembleAppGraphEnvelope` yields landing+toolbar+panes in ONE graph at boot; `content-reconcile.ts` `asContentRoot` + `runtime.ts` `destroyRoot` admit `stage-landing` so the landing is reconcilable (no phantom after first import; adversarial-fix AD-2026-09-14-1..4). **LIVE-CONFIRMED (2026-09-15, `--no-seed boot_landing`):** empty-store boot shows `#stage-landing[data-stage=landing]` coexisting with `#editor-toolbar` + 7 `.pane-frame` panes; importing the first document removes the landing (`landingAfter=false`, no ghost). TDD: red `tests/unit-live4-empty-store-landing.test.ts` (9 red) → green 13 (incl. `unit-live4-adversarial-fix.test.ts` 4). |
| **LIVE-5 VISIBILITY-NOT-SAVED** | medium (HOST) — **FIXED + LIVE-CONFIRMED (2026-09-15)** | View-menu / operator pane-visibility toggles do not persist across restart. | Toggle a pane off, restart the app; it returns. | (Analysis) The write-through→store→boot-reload chain was complete (U-SHELL-8 `persistEnabledPanes`/`applyPersistedPaneVisibility`); the LIVE-11 preload-seam gap made the in-pane toggle inert at the boundary, and the dispatch-seam gap (handleDomEvent path-key wire) made real clicks fail — both now fixed. | **FIXED + LIVE-CONFIRMED (2026-09-15):** a full cross-restart round-trip via `scripts/live-drive.mjs` on a shared `--home` (two real relaunches): toggle pane OFF → its pane-frame is removed from the DOM and all operator settings populate (`#operator-enabled-panes:true`); on the second launch the pane stays hidden (`data-enabled=false` at boot, pane-frame absent) with the settings populated. The in-pane toggle is the `#settings-modal [data-pane][data-enabled]` control, driven via a native `.click()` (the panes sit below the viewport fold on display :0, so a CDP-coordinate click can't land — an environment artifact, the seam path is the valid driver). |
| **LIVE-6 PANES-NOT-IN-ZONES** | high (HOST) — **FIXED (2026-09-14, assembly: `#wiki-root` grid — panes render as a LEFT sidebar, the stage in the center; live-verified left.x<main.x)** | App-graph panes populate the MAIN CANVAS, not the side zones. | Boot the app; doc-nav/crosslinks render in the stage (`#app`), not `grid-area: left/right`. | The `.layout` grid places `#app` (the runtime mount holding ALL the `[data-zone]` zone containers) at `grid-area: stage`, so the left/right zones render INSIDE the stage cell; `#panes` (grid-area left) is the re-parented SecurePanels (in the modal) — the shell grid's separate left/right cells never receive the app-graph panes. | Restructure the shell/layout — **FIXED (2026-09-14): `#app #wiki-root` is now a grid (`header / left·main·right / footer`) laying the app-graph zone containers into sidebars; live-verified `zone:left` renders LEFT of the stage `zone:main`. Default distribution (all panes in `zone:left`) is a follow-up.** |
| **LIVE-7 SETTINGS-LAZY-LOAD** | medium (HOST) — **FIXED (2026-09-15): the old load-laziness is CLOSED; the fresh-store census drift is resolved by an explicit first-run default (USER-DECIDED)** | Operator settings don't display until the user attempts an edit, then recover to the last-saved state. | Open the app; the operator/settings panes are empty until an edit triggers a re-render. | (Analysis) The old load-laziness is CLOSED: `boot` loads the FULL settings state (`this.lastOperatorSettings` at `sidebar-panes.ts:1842`) before first render, so `#operator-topk`/`#operator-editing-mode` populate at boot. **LIVE finding (2026-09-15):** a FRESH store showed `#operator-enabled-panes` empty until a visibility write persisted `enabledPanes`, because the first-run default was "all enabled" applied to the REGISTRY but the persisted `enabledPanes` list (which the census renders) was empty. This is a one-time fresh-store default, not a recurrence after settings are persisted (live-proven by the LIVE-5 two-launch round-trip). **USER DECISION (2026-09-15): first-boot default enabled panes = `['search','doc-nav']`** (the Documents/`doc-nav` list + the Search pane) instead of all panes; this default is applied at first boot AND persisted through so the census and subsequent boots agree. TDD: re-derived `tests/unit-u-shell-8-view-menu-pane-visibility.test.ts` (state 1, H1, H4, H2 first-boot) to expect only search+doc-nav enabled on an EMPTY/first-run persisted set; `applyPersistedPaneVisibility` (`sidebar-panes.ts:1266`) now enables `FIRST_RUN_APP_DEFAULT = {search, doc-nav}` on the empty-first-run branch + writes it through via `persistEnabledPanes`. Operator-scope first-run default unchanged (all enabled). | Populate the operator-settings state at boot (read the persisted settings/UI-config before first render) — done; first-run default applied + persisted (search + doc-nav). |
| **LIVE-8 UNDO-INERT** | high (HOST) — **FIXED (2026-09-15, U-LIVE8)** | The editor-toolbar Undo/Redo `disabled` state was NOT refreshed on the CONTENT re-derive path: after an edit bumps `undoDepth` 0→1 the Undo button STAYED disabled (LIVE-8 UNDO-INERT). | Edit then click Undo in the live app; nothing reverts. | The C16 Undo control is disabled from `undoDepth`; the fresh `disabled:undoDepth<=0` authored by `applyEditorToolbar` on `reDerive('content')` was DROPPED because the pinned `editor-toolbar` root is neither `rag-` nor `pane-`, so `applyContentReconcile` never re-materialized it. | **FIXED (host, 2026-09-15, U-LIVE8):** the pinned `editor-toolbar` root joins the content-root allow-list in `extractContentRoots`/`asContentRoot`/`destroyRoot` (runtime + content-reconcile), so a `disabled`/`data-mode` flip lands it in `replaced` → destroy+attach re-materializes the fresh state. The AF2 hostile-bucket gate is preserved (only the pinned id joins the allow-list). TDD: red set `tests/unit-live8-toolbar-undo-refresh.test.ts` (3 failing: a-core, a-mirror, c) → green 7/7 + 3 AD regressions (`(Finding 1/b)` ×2, `(Finding 2/a)`) = **10/10**. |
| **LIVE-9 HTML-TOGGLE-INERT** | high (HOST) — **FIXED + LIVE-CONFIRMED (2026-09-15)** | The editor-toolbar markdown/html (editing-mode) toggle does nothing. | Click the HTML/Markdown toggle in the live app; the editor doesn't switch. | Same shared LIVE-8/9/11 root cause — the real-DOM-click→dispatch seam: `Runtime.handleDomEvent` resolved the event's node via `supervisor.getNode(wire)`, keyed by node.id, while the DOM wire is a path-key → real clicks dropped (MCP `provident.dispatch` and direct `sidebar.operatorSet` worked; data-mode did not flip on a real click). **FIXED (2026-09-15):** `handleDomEvent` falls back to the target's `data-node-id` when getNode(wire) is null (same fix as LIVE-11). **LIVE-CONFIRMED: a real CDP click on `#editor-toolbar-toggle` flipped `data-mode` `contenteditable`→`textarea` (toolbar_toggle battery PASS).** | Trace the editing-mode toggle → `operatorSet` → re-derive path (now live-working via the dispatch-seam fix). |
| **LIVE-10 NO-PANE-TITLES** | medium (HOST) — **FIXED (2026-09-14)** | None of the panes show a visible title. | Look at any pane frame; the header area is empty (only the collapse toggle). | `paneSubtreeRoot` (U-SHELL-3) authors the frame as a collapse-toggle + body with NO title chrome. | Author a visible title bar (the pane's `title`/def name) in the frame header. |
| **LIVE-11 COLLAPSE-INERT** | medium (HOST) — **FIXED + LIVE-CONFIRMED (2026-09-15)** | Panes do not collapse when the collapse toggle is clicked. | Click a pane's collapse toggle; the pane stays expanded. | Three layers, all resolved. (1, U-LIVE11): preload `contextBridge` `sidebar` proxy omitted `togglePaneCollapse` (+ 3 siblings) — exposed + parity guard. (2, **the shared LIVE-8/9/11 ROOT, FIXED 2026-09-15**): `Runtime.handleDomEvent` (`src/renderer/runtime.ts`) resolved the DOM event's node via `supervisor.getNode(wire)`, but `getNode` is keyed by **node.id** while the DomAdapter's `wire` is a **path-key** — so real DOM clicks were silently dropped. FIXED: `handleDomEvent` falls back to the event target's `data-node-id` when getNode(wire) is null. TDD `tests/unit-live-dom-click-dispatch.test.ts` red→green; final batch trio **4664/58/0, typecheck 0, build 0**. **LIVE-CONFIRMED:** native `el.click()` on `#pane-collapse-doc-nav` → `isCollapsed:true`, button `data-pane-collapse`→"true" (diag3 post-fix). NOTE: the CDP-coordinate `collapse` block is geometry-sensitive (elementFromPoint over the sidebar pane can be null in some window geometries), so the native-click probe is the reliable live check; the dispatch seam itself is proven by the passing toolbar toggle + native-click collapse. (3) host setLayout→re-render already correct. | Fix the host dispatch seam (done) so a real DOM click on a provident on:click control reaches the host method. |

## SHELVED

| # | Severity | Symptom | Reproduction | Suspected root cause | Proposed fix shape (upstream-owned) | Shelved reason |
| --- | --- | --- | --- | --- | --- | --- |
| **ENG-GAP-1** | a-big (shelved) | The `MarkdownAdapter` drops `data:*` props including the opt-in `data-node-id` (D7), so the agent-facing markdown output carries no element→node identity. | Render a graph with `renderOptions.nodeIdAttribute: true`, then re-emit through a fresh `MarkdownAdapter`; the markdown output has no node identity. | `MarkdownAdapter` (adapters.md §4.7 D7) drops `on:*` and `data:*` props; only `href`/`title`/`src`/`alt` are rendered. | A handoff feature request: a markdown-adapter option to preserve node identity (or a node-id-preserving markdown variant). | **Shelved 2026-08-26 (user ruling).** (1) Markdown is EXPORT-ONLY — no flow edits markdown output as input, so node identity in the export has no consumer today. (2) Comment IDs are fragile: an external editor could delete/alter them, corrupting the mapping. (3) The future markdown-parsing-to-storage feature will rely on **text-match diffing** instead of node-identity comments (see `docs/pending.md`). Revisit only if a markdown-as-input flow becomes supported. |

## FIXED

| # | Severity | Symptom | Reproduction | Root cause | Fix (host-side) |
| --- | --- | --- | --- | --- | --- |
| **HOST-SHELL-WIRING-DEAD-SELECTORS (+H2–H5)** | high (HOST) — **FIXED (2026-09-13, U-SHELL-N7)** | The `installShellPointers` shell pointer-wiring attached pointers only to elements found at INSTALL time — `document.querySelectorAll('.gutter[data-zone]')` / `.pane-frame[data-pane-id]` matched **NO authored DOM** (the frame root had no `data-pane-id`; index.html authored only `.gutter` CSS), so C4/C7/C11/C12 were unreachable live and the §2.8 source-pin was a false green. Related: HOST-2 (per-gesture `pointermove/up/cancel/dblclick` never removed → duplicate/stale commits), HOST-3 (`isInteractiveControl` no ancestor climb → a control's nested child mis-starts a drag, F9), HOST-4 (a reveal-driven re-mount detaches the captured element mid-gesture), HOST-5 (`toZoneBounds` coerces an unknown `[data-zone]` → `'left'`, fails open). | With the app rendered, C7/C4 gestures did nothing — no `startGutter`/`startPaneDrag` call, no `setLayout` write, the `.gutter` CSS was dead. | The wiring queried elements at install (before the graph rendered), registered per-gesture listeners that never teardown, inspected only the deepest node, captured onto an element a re-mount detaches, and coerced malformed zones into a real `left` drop target. | **FIXED (host, U-SHELL-N7, 2026-09-13):** `installShellPointers` (now a NAMED export of `src/renderer/renderer.ts`) is reworked as a document-level DELEGATED wiring — ONE delegated `pointerdown` resolved per event via `e.target.closest('.gutter[data-zone], .pane-frame[data-pane-id]')` (HOST-1/HOST-4); the per-gesture move/up/cancel registered exactly once + torn down at gesture end (HOST-2); ancestor-climb interactive guard incl. `select`/`textarea` + WRAPPER `label`/`fieldset`/`[contenteditable]` (HOST-3 + ADV5); fail-closed `isLayoutZoneNameValue` zone skip + gutter exclusion (HOST-5). Re-audit ADV1..5 also landed: permanent document-delegated `dblclick` reset; `pointerId`-scoped move/up/cancel + `lostpointercapture` teardown; cross-kind supersede revert; idempotent install; WRAPPER controls. The four `.gutter[data-zone][data-axis]` elements authored in `index.html`; the frame `data-pane-id` in `pane-graph.ts`; dom-shim extended (`innerHTML`/`outerHTML` getters + a delegated pointer dispatch + `closest`/`setRect`/`setPointerCapture`). Regressions: `tests/unit-u-shell-shell-wiring-adversarial.test.ts` 14 (HOST-1..5 + ADV1..5) + `tests/unit-u-shell-shell-wiring-pbt-generators.test.ts` 5. Trio green (**4527 pass / 58 skip**). Spec `unit-u-shell-shell-wiring.md` §3a/§3b. |
| **U-SHELL-9b-F1 (buildOwnersMap prototype-key throw)** | high (HOST) — **FIXED (2026-09-13, PBT audit)** | `buildOwnersMap` THREW on an edge whose `target` is an Object.prototype key — `buildOwnersMap([{ target: '__proto__', documentIds: ['A'] }])` / `'constructor'` → `TypeError: list.includes is not a function`. Surfaced by the U-SHELL-9b read-only PBT audit (F1) — the property generator's `target ∈ {'X',''}` could never sample it. | Drive `buildOwnersMap` with an edge object whose `target` is `'__proto__'`/`'constructor'`/`'toString'`/`'hasOwnProperty'`/`'valueOf'` and a non-empty `documentIds`. | `const owners = {}` is a plain object; `owners['__proto__']`/`owners['constructor']` resolve to the INHERITED non-array prototype value, so `?? ` never initializes and `.includes` throws — a totality violation of the register's "malformed edge is ignored / never throws / no prototype pollution" promise. | **FIXED (host, 2026-09-13):** `buildOwnersMap` now builds on a **null-prototype map** (`Object.create(null)`): a prototype-key `target` is an ordinary OWN key (never inherited, never pollutes, `.includes` never throws). Regression: `tests/props-cross-doc-shared.test.ts` (`P-IM-2 F1 companion`, magic-key targets). Trio green (195 files / 4469 pass + 58 skip). Spec §3a adversarial F1 recorded. |
| **U-STATE-1e-W2 (fork-echo swallows a sibling's genuine shared edit)** | medium (HOST) — **FIXED (2026-09-13, PBT audit)** | In one broadcast where doc A forks shared `rag-X → rag-X′` AND doc B independently EDITS its shared `rag-X`, B's content edit was silently classified `kept` (lost). Surfaced by the U-STATE-1e read-only PBT audit (W2) — the global `consumed` suppression, intended to leave B's UNCHANGED shared root kept (state 6), also suppressed B's real same-broadcast edit. | Broadcast: A forks `X→X′` (payload names `X`,`X′`) while B's `previous [rag-X]` → `next [rag-X edited]` (shape changed, subtree ids identical) in the same `change.nodeIds`. | `reconcileDocumentRoots` dropped the consumed fork ids from `effectiveChanged` GLOBALLY, so B's `payloadHit` is false and shape is only consulted when `useFallback||isPane` (false for B's content root) → B's edited X → `kept`. | **FIXED (host, 2026-09-13):** a root whose subtree carries a fork-consumed id (`carriesConsumedId`) now consults the SHAPE (`useShape`), so a genuine sibling edit flips to `replaced` while the pure fork-echo (unchanged shape) stays `kept` (state 6 preserved). Regression: `tests/props-reconcile-1e.test.ts` (`strat:nroot-fork-consumed-edit` + `strat:nroot-fork-echo-kept`). Trio green (195 files / 4469 pass + 58 skip). Spec §9 W2 (FIXED) recorded. |
| **HOST-SECURITY-STORE-GNOSIS-GROUPS** | medium (HOST — the security-UI toggle could not enable the Gnosis groups) | The manual-UI Security Settings pane rendered the `gnosis`/`gnosis-edit` toggles but a click could NOT enable them: the checkbox stayed unchecked and `enabled` never gained the group. The Gnosis MCP tools therefore never registered in the running app (they're gated on default-off groups). Verified live via CDP: a real DOM click on `toggle:gnosis` produced zero `security.set` calls that persisted — `enabled` stayed `["read","dispatch","rag","module","code","graph","edit"]`. | Click `toggle:gnosis` in the running app's Security pane (or call `window.provident.security.set({ groups: ['gnosis'] })`): the group is silently dropped and `enabled` is unchanged. Reproduced live (CDP) 2026-09-11 and by the TDD red set (`tests/security-store.test.ts`). | `src/main/security-store.ts` keeps its OWN `VALID_GROUPS` filter (line 45) that was NOT updated when the Gnosis tool groups landed — it lists only `read/dispatch/graph/code/module/rag/edit` and omits `gnosis`/`gnosis-edit`, while `src/main/security.ts`'s `ToolGroup` union and `VALID_GROUPS` (line 185) DO include them. `securityStore.set()` filters `patch.groups` against the STORE's stale set and silently drops the Gnosis groups, so `enabled` never changes — the UI checkbox cannot stick. The store's own L3 comment (lines 42-44) says its set "MUST match security.ts's ToolGroup union" — it drifted when the A2 `gnosis-edit` group landed. | **FIXED (host, 2026-09-11):** `src/main/security-store.ts` `VALID_GROUPS` now includes `gnosis` + `gnosis-edit` (lockstep with `security.ts`). TDD: red set `tests/security-store.test.ts` (2 new tests failed — the groups were dropped) → green 14/14 (enable + write-through + reload + disable symmetric). Also `src/renderer/secure-panels.ts` `GROUPS`/`GROUP_LABELS` now include the two Gnosis groups (the security-UI toggles render them). Full trio green: 3346 pass / 43 skip, typecheck clean, build clean. |
| **HOST-GUI-DOCS-PANE-DEADLOCK** | medium (HOST — **live-verified FIXED 2026-09-11**) | The `gnosis-documents` GUI pane was STUCK in whole-pane `data-gnosis-state="unavailable"` and rendered 0 clickable wiki `<li>` items / 0 doc `<li>` items, even after the `gnosis.wiki.list` bridge resolved (the `gnosis-wikis` pane rendered the same wiki list fine). A chicken-and-egg deadlock in the PURE helper `gnosisDocumentsContent` (`src/renderer/pane-graph.ts`): it returned the whole `unavailable` branch whenever `state.documents == null`, but the clickable wiki `<li>` items — the ONLY path that triggers `gnosis.document.list` (which sets `documents`) — were rendered only in the NON-unavailable branch. So with `documents` null the wiki selector never rendered → the pane could never leave `unavailable`. The module greens could not see it (they feed BOTH `wikis` AND `documents` directly to the pure helper). | Re-driven live on the relaunched app (`--remote-debugging-port=9222`): prior run click `gnosis-documents-refresh` → `pane-gnosis-documents` STILL `data-gnosis-state="unavailable"` with 0 `li[data-wiki-id]`; `pane-gnosis-wikis` rendered all wiki names; `[data-gnosis-pane="status"]` rendered `Ready`. 2026-09-11. | `gnosisDocumentsContent` gated the entire pane on `state.documents != null`; the wiki `<li>` selector (which would set `documents`) lived BELOW that gate, so it was unreachable while `documents` was null. The `gnosis-wikis` pane self-populates on `boot()` independent of document state, so only the documents pane deadlocked. | **FIXED (host, 2026-09-11):** `gnosisDocumentsContent` re-derived — the wiki selector renders whenever `state.wikis != null` (independent of `documents`); the Documents section shows `data-gnosis-docstate="empty"` ("No documents — select a wiki") when `documents` is null/empty. **Live-verified 2026-09-11 on the fresh relaunch (rebuilt `dist/`):** after a fresh `gnosis-documents-refresh` click the pane renders the wiki selector (10 `<li data-wiki-id>` live wiki names) AND the `data-gnosis-docstate="empty"` empty state — NO whole-pane `data-gnosis-state="unavailable"`, no `unavailable` anywhere; `pane-gnosis-wikis` (10 wikis) + `pane-gnosis-status` (Ready) unaffected. Module contract green: unit-a2 (91) + props-a2 (8) = 99/99. The full G29 doc-population slice (select a wiki → doc-19 `<li>` renders) remains PARKED in the A2 battery pending a provident-capable UI-drive (CDP-probe can't dispatch the provident handler-`select-wiki` binding) — not a defect. |

| **HOST-READ-GET-WITH-BODY** (HOST-GET-WITH-BODY-SSE-CRUD) | medium (HOST — **live-verified FIXED 2026-09-11**) | The document-CRUD **read** methods issued a **GET-with-a-body** using `globalThis.fetch`; under Node/undici a GET with a body is rejected (`TypeError: Request with GET/HEAD method cannot have body`), so `gnosis.wiki.list`/`get`, `gnosis.document.get`/`list` failed at the transport layer. The app itself (Electron running the same undici fetch) reproduced it. | Live `tools/call` on the read tools over the running app → the GET-body TypeError. | `createEngineFetch` returned `globalThis.fetch`, which rejects GET-with-body. | **FIXED (host, 2026-09-11):** `createCrudFetch()` added in `src/main/engine-transport.ts` (uses undici `request()` — GET-with-body-capable, applies TLS/dispatcher) and wired as the CRUD client default (`src/main/engine-crud-rag-store.ts` `opts.fetch ?? createCrudFetch`). TDD: red set `tests/engine-crud-real-transport.test.ts` (GET-with-body reads failed) → green 2/2; existing A1 suite 60/60 still green. **Live-verified 2026-09-11 on the relaunched app:** `wiki.list`, `wiki.get`, `document.list`, `document.get` all resolve (no TypeError). NOTE: `document.list` on a populated wiki previously still surfaced the separate `HOST-CRUD-LIST-SUMMARY-DECODE` decode finding — **now FIXED + live-verified 2026-09-11** (see its FIXED row below). |
| **HOST-CRUD-DELETE-RESULT-SERIALIZATION** | medium (HOST — **live-verified FIXED 2026-09-11**) | `gnosis.document.delete` (a successful engine delete returning the wire `void`/`null`) failed at the MCP boundary with `MCP error -32602: Invalid tools/call result` — a result-SERIALIZATION error, not an engine error. | `tools/call gnosis.document.delete` → `-32602`. | The A2 `handleGnosisTool` delete case returned `undefined`, which `text(undefined)` cannot serialize to a valid tool result. | **FIXED (host, 2026-09-11):** the `gnosis.document.delete` case in `src/main/mcp-server.ts` now `await engineCrud.deleteDocument(...)` then `return null` (the wire `result:null`). Pinned tests updated to the null contract (`unit-a2…` §5.8-7 80/80; `blind-unit-a2…` G7 49/49). **Live-verified 2026-09-11 on the relaunched app:** `document.delete` returns the wire result `null`, no `-32602`. |
| **HOST-CRUD-LIST-SUMMARY-DECODE** | medium (HOST — **live-verified FIXED 2026-09-11**; was OPEN/live-confirmed) | `gnosis.document.list` on a wiki with >=1 document previously returned the engine error `malformed document` instead of the typed `DocumentList {items,total,page,pageSize}`. The read transport fix (HOST-GET-WITH-BODY-SSE-CRUD) let the call through for the first time; the deterministic suite could not see it (the mock feeds a FULL document via `DOC_WIRE`). | `tools/call gnosis.document.list {wikiId:<wiki-with-a-doc>}` over the live app (engine Ready) previously → `malformed document`. Reproduced on a fresh wiki immediately after `document.create`. The raw engine `GET /documents` wire is CORRECT: `{items:[{document_id,wiki_id,title,state,revision,updated_at}],total,page,page_size}` — a lightweight `DocumentSummary` per Gnosis §4.1.3. | The A1/A2 pinned contract declared `DocumentList.items: Document[]` (full docs) and `decodeDocumentList` mapped every item through the strict `decodeDocument`, which requires `graph`/`tags`/`created_at`/`author`-key present — fields the engine's `DocumentSummary` does not carry → `malformed document`. The engine is CORRECT (it returns a documented summary); the host over-required. | **FIXED (host, 2026-09-11):** the A1 `DocumentList` contract was re-derived to `items: DocumentSummary[]` and the host decodes the summary list via the lenient `decodeDocumentSummary` (six fields, snake+camel; tolerates a full `Document` item / projects it; a malformed summary → `EngineError('malformed document')` 502). **Live-verified 2026-09-11 on the fresh relaunch:** `gnosis.document.list` on a populated wiki resolves to the typed `DocumentList` (`items` six-field `DocumentSummary[]` — `documentId`/`wikiId`/`title`/`state`/`revision`/`updatedAt`, no full-doc leakage — `total:1, page:1, pageSize:20`; no `malformed document`, no `-32602`); on an empty wiki → `items:[], total:0, page:1, pageSize:20`. See `docs/specs/unit-a1-crud-list-summary-decode-greens.md` §LIVE-LOCATION. |
| **HOST-GN-H11** | minor (documented limitation) | The loopback check (`assertLoopback`, INTERNAL in `src/main/engine-rag-store.ts`) accepts `localhost` unconditionally — it does not resolve the hostname to confirm it maps to a loopback address. | `createEngineRagStore({ baseUrl: 'http://localhost:8080' })` constructs without throwing (accepted per §5.8-14 / H11). | The factory is SYNCHRONOUS (no network I/O at construction — a pinned contract), so DNS resolution is not feasible in the sync factory. | **PARTIAL — DOCUMENTED LIMITATION (no code change):** the check now recognizes IPv4-mapped IPv6 (`::ffff:127.0.0.1`/`::ffff:7f00:1`); `localhost` remains accepted as the loopback alias per §5.8-14. A non-loopback address still throws at construction (`engine rag store: baseUrl must be loopback`). Found in the Unit GN adversarial pass (RCA-3, finding H11); regression-tested. **No package/upstream finding** — the Unit GN adversarial pass confirmed NO defect in the `provident-ssr` package or the Gnosis wire contract (spec §3b: NONE). |
| **HOST-LIVE-ZOD-SEAM** | medium | The SDK zod input-schema seam pre-empts the handler's documented M1/M2/documentId fail messages, so the batteries/specs overstate what is MCP-observable. Three manifestations (live-testing findings F2/F3/F4, 2026-09-08): **(F2)** a non-string `store` (`null`/`5`/`true`/`{}`/`[]`) is rejected by the `store: z.string().optional()` schema with `-32602 … expected string, received … at store` BEFORE the handler — only `''` reaches `resolveStoreArg` and yields M1 `store must be a non-empty string` (spec §5.1 M1 + battery §3.3 S19 overstate); **(F3)** an unknown `store` with a MISSING required arg (e.g. `create_node {store:'nope'}` without `type`/`content`) is rejected by the schema with `-32602` before the handler's store-resolution — M2 `unknown store` fires only when the call passes the zod schema (battery §3.3 S21 "every time" overstates); **(F4)** `rag.get_document {}` (missing `documentId`) is rejected by the required `documentId: z.string()` schema with `-32602 … expected string, received undefined at documentId` — only `''` reaches the handler's `documentId required` (V2 battery §3.3 C4 overstates). | Live MCP calls (2026-09-08 live-testing session, `archive/live-testing-findings/2026-09-08-findings.md`) with a non-string `store`, an unknown `store` + missing required args, and `rag.get_document {}`; each returns the SDK `-32602` error instead of the documented handler message. | The MCP SDK validates each tool's `inputSchema` (zod) BEFORE the handler runs; the schemas are type-strict (`store: z.string().optional()`, `documentId: z.string()` required), so type-invalid / missing-required-arg calls never reach the handler's documented fail messages. The spec's M1/M2/documentId contracts are correct at the PURE resolver/handler level (direct calls in tests) but overstate what the MCP surface observes. | Doc/battery erratum (NO code change — the SDK seam behavior is sound and matches the house "schema validates type, handler validates value" topK pattern): amend spec §5.1 M1 + §5.3 R6/R7 + battery §3.3 S19/S21 + V2 battery §3.3 C4 to note that non-string `store` / missing-required-arg / missing-`documentId` calls are rejected at the SDK zod seam with `-32602`, and only `''` reaches the handler's M1/M2/documentId messages. |
| **HOST-LIVE-UX-SETUP** | medium | The Unit X live-pending battery's setup is not buildable as written: it seeds its provenance graph with `edit.create_node {type:'content'|'fact'|'reference'}` (battery §2), but `edit.create_node` is a STRUCTURAL op (validates `type` against `RAG_NODE_TYPES` = h1-h6/p/ul/ol/li/blockquote/pre/code/strong/em/a/img/div/table…, `edit-ops.ts:173-174`) and rejects RAG kinds with `edit.create_node: invalid type`. RAG `nodeKind` ('content'|'fact'|'reference', `rag-store.ts:79`) is a SEPARATE field. **CORRECTED 2026-09-08 (verification):** markdown import does NOT mint `nodeKind` either (no `nodeKind` in `markdown-parse.ts`/`markdown-import.ts` — imported nodes are `nodeKind`-undefined ⇒ 'content'); the ONLY place fact/reference nodes are created is the module test suite via direct `store.putNode(makeNode(…, { nodeKind: 'fact' }))` (`tests/unit-x-rag-provenance-traversal.test.ts`). So there is **NO live MCP path that mints fact/reference nodes at all** — the graph-mode provenance probes (S9–S13) and the nodeKind-classified setup (S1–S4) are NOT live-exercisable, not merely "setup not buildable as written". The battery's recorded error string for a bad kind (`rag putNode: nodeKind required/invalid`, battery §3 S2) also does not match the live `edit.create_node: invalid type`. The runnable MCP subset (flat mode, audit log, rag-stream) IS live (live-testing finding F5, 2026-09-08). | Follow the Unit X battery §2 setup (`edit.create_node {type:'fact'}` etc.) against the live app → `edit.create_node: invalid type`; there is no live tool that produces a `nodeKind:'fact'`/`'reference'` node. | `edit.create_node`'s `type` param is the structural `RagNodeType`, not the provenance `RagNodeKind`; the battery conflated the two, and no live surface mints RAG kinds. | Doc/battery fix (NO code change — the design is sound: `create_node` is structural, `nodeKind` is a provenance category): rewrite the Unit X battery to (a) keep the runnable MCP subset (flat mode, audit log, rag-stream) live, and (b) PARK the graph-mode provenance (S9–S13) + the nodeKind-classified setup (S1–S4) as NOT-live-exercisable (no live path mints fact/reference nodes; covered by the module-level greens), the V2-battery A10/A11/B5/B6/C5 pattern. Extending `edit.create_node` to mint RAG kinds is a SEPARATE feature addition, not required to close this finding. |
| **HOST-F-DIST-STALE** | a-big (high) | Vector-mode boot (local ollama `embeddinggemma` as the embedding provider) crashed the app at startup with `ollama embed: malformed response` before any window/UI appeared. | Run the built app (`dist/main/main.cjs`, built 2026-08-31) with `PROVIDENT_RETRIEVAL_EMBEDDER=vector PROVIDENT_EMBEDDING_PROVIDER=ollama PROVIDENT_EMBEDDING_BASE_URL=http://127.0.0.1:11434 PROVIDENT_EMBEDDING_MODEL=embeddinggemma` against the real 62MB / 23,469-node store (6,629 empty/whitespace-content nodes): the first empty-content node POSTs `/api/embed` with `input: ""`, ollama returns 200 `{embeddings: []}`, and the stale bundle's `createVectorIndex` rejects → `main()` throws. A/B reproduced from git HEAD vs the fixed src (2026-09-05). | The `dist/` bundle was STALE: it predated the F9 empty-node-skip fix that exists green-tested in `src/main/embeddings.ts` (uncommitted working tree, modified 2026-09-05). The operator launched a build that did not contain the fix. **Coverage citation CORRECTED 2026-09-05 (F9 label-collision resolution, review amendment A2):** the skip is finding **UNIT-F-SKIP-EMPTY** (`docs/specs/unit-f-embeddings.md` §3a F10 + the §5.3 empty-content guard rule); NO test in `tests/embeddings.test.ts` covers the skip (the earlier citation was wrong) — the NAMED regression test is a W3 deliverable (`tests/embeddings-failure-policy.test.ts`). The greens row formerly labelled "F9 — Ollama provider malformed response" (`docs/specs/unit-f-embeddings-greens.md`) is RENAMED **GREEN-OLLAMA-MALFORMED** (it tests spec §5.9 #9, not the skip). | `dist/` rebuilt from the fixed src (`npm run build`; F9 skip logic verified present in `dist/main/main.cjs`). Trio green (2031 pass / 38 skip, typecheck clean, build clean). Process fix proposal P4 (launch via `npm start` which rebuilds; optional boot-time staleness check) is under the vector-boot proposal gate — see `docs/next-steps.md`. |
| **HOST-C1** | high | The coarse line→node map was fabricated (`{ startLine: i, endLine: i + 1 }` — a synthetic 1-line span per RAG object), not real markdown line ranges. | `buildTraversal` on a `ul` with 4 `li` doc-children returned a 1-line range for the `ul`. | The assembly step assigned a synthetic span instead of rendering the envelope to markdown and mapping each subtree's line span. | `src/main/traversal.ts` now renders the envelope to markdown (via `renderProducingProcess` + `MarkdownAdapter`) and maps each RAG subtree's real line span (0-based, inclusive start, exclusive end, COARSE — a `ul`+`li` chunk maps to the whole list; a doc-child's lines map to the doc-child). Regression-tested. |
| **HOST-C2** | medium | A parent's back-reference included its doc-children's node ids (`collectSubtreeIds` walked the whole subtree). | `backRefs.get('ul')` = `[ul, li1, li2, li3, li4]`. | `collectSubtreeIds` descended into every child, including doc-child subtree roots. | `collectSubtreeIds` now stops at each doc-child subtree root (a child carrying the stable `rag-<id>` id) — the parent's owned set EXCLUDES its doc-children's nodes (§5.2 rule 6 / §5.9). Regression-tested. |
| **HOST-C3** | medium | `docChildTargets` was scoped globally, not per document — a cross-document shared node that is a doc-child target in doc A but a multi-parent shared node in doc B was silently dropped in B. | `buildTraversal({ documentIds: ['docB'] })` did not materialize a node that is a doc-child target in doc A. | The doc-child exclusion set was built from ALL `doc-child` edges, not the current document's. | The doc-child exclusion is now scoped to the CURRENT document's doc-child edges (a `doc-child` edge belongs to the document whose node set contains its source). Regression-tested. |
| **HOST-C6** | low | Duplicate `documentIds` entries double-materialized content. | `buildTraversal({ documentIds: ['doc', 'doc'] })` emitted every section twice. | `documentIds` was not deduped. | `documentIds` is deduped (order-preserving) at the top of `buildTraversal`. Regression-tested. |
| **HOST-C7** | low | RAG node `props` (e.g. `href`/`src` for `a`/`img`) were not propagated to the subtree root. | An `a` node with `props: { href: '...' }` rendered without the href. | The subtree root's props were built from only `id` + `data-doc-head`. | The RAG node's own `props` are merged into the subtree root's props (`id` and `data-doc-head` take precedence). Regression-tested. |
| **HOST-UI1** | high | In dark mode (`prefers-color-scheme: dark`), the app shell is unreadable — white text on white backgrounds. | Set the OS/app to dark mode; the header/body/cards show white UA-default text over the hard-coded light `#f4f6f8`/`#fff` backgrounds. | `src/renderer/index.html` sets `:root { color-scheme: light dark }` (UA uses dark-scheme white text) but hard-codes light backgrounds with no dark-mode overrides. | `src/renderer/index.html` now has a `@media (prefers-color-scheme: dark)` block giving every hard-coded light background/text an equivalent dark value (body `.card` inputs `#status` etc.), honoring the `color-scheme: light dark` declaration. |
| **HOST-UI2** | high | The `SidebarPanes` renderer host NEVER boots in the real Electron app — `Uncaught TypeError: Cannot add property sidebar, object is not extensible` aborts `boot()` before it subscribes to `rag-store-changed`, so no RAG document ever materializes in the app view. | Launch the app with the default `contextIsolation: true`; the renderer console shows the throw and `get_rendered_html` stays on the empty placeholder envelope after an `edit.import_markdown`. | `installSidebarBridge` adds a `sidebar` property to `window.provident`, but in the real renderer `window.provident` is the `contextBridge`-exposed object, which is FROZEN — the property-add throws. The test dom-shim leaves `window.provident` a plain mutable object, so tests never hit it (a test-env vs. real-env divergence). | `src/main/preload.ts` now OWNS the `sidebar` object + exposes `installSidebar(methods)` (each exposed `sidebar.*` method DELEGATES to a holder the renderer installs); `src/renderer/sidebar-panes.ts` `installSidebarBridge` registers via `installSidebar` when present (real app) and falls back to a direct attach only for the non-contextBridge test shim. Boot now completes → subscribes → re-derives → materializes. Regression-tested (a frozen `window.provident` boot test). |
| **HOST-U2-F1** | a-big (high) | `String.fromCodePoint` threw a `RangeError` on out-of-range / lone-surrogate HTML character references, so `decomposeRichHtml` was NOT total (violating its §1.2/§2.2 fail-state 7 contract) — and `sanitizePastedHtml` also threw on the same hostile input. | `decomposeRichHtml('<a href="https://x?a=&#x110000;">link</a>')` (or a `&#xD800;` lone-surrogate ref) threw instead of returning `{ ok: true, ... }`. | `decodeHtmlRefs` (in `src/main/paste-sanitize.ts`, shared by `sanitizePastedHtml` + `decomposeRichHtml`) called `String.fromCodePoint(code)` unguarded for every numeric ref; `fromCodePoint` throws `RangeError` for a code point above `0x10FFFF` or in the surrogate range `0xD800–0xDFFF`. | `decodeHtmlRefs` now guards `code > 0x10ffff || (code >= 0xd800 && code <= 0xdfff)` and leaves the literal ref un-decoded. This makes BOTH `decomposeRichHtml` and `sanitizePastedHtml` total on such inputs (a host defect fix, additive-safe). Regression-tested (Unit U2 adversarial ADR-11 in `tests/unit-u2-rich-decompose.test.ts`). Found in the Unit U2 adversarial pass (RCA-3), recorded as §6 F1. |
| **HOST-U3-F3** | minor | A malformed payload (an empty/absent `content` array) threw a `TypeError` during the post-assembly host passes. | `loadAppGraph` on an envelope whose payload `content` is `[]` (a payload with no root node) threw instead of rendering. | `setTextareaReadOnly` (Unit L §5.3) and the new `applyEditingMode` (Unit U3 §1.3) both dereferenced `p.content[0]` without a guard — a payload with an empty `content` array made `content[0]` `undefined`, and the walk helper's first step threw on it. | Both passes now drive the walk with `walk(p.content?.[0])`, and the `walk` helper starts with `if (!n) return` (the guard lives inside the walk helper, equivalent to guarding the root before recursing — cheap hardening; no behavior change for valid envelopes). This covers BOTH `setTextareaReadOnly` and `applyEditingMode` (a host defect fix). Regression-tested (Unit U3 adversarial F3 in `tests/rich-splice.test.ts` — "a malformed payload does NOT throw in contenteditable mode"). Found in the Unit U3 adversarial pass (RCA-3), recorded as the spec §5 F3. |

| **ENG-JOURNAL-ENTRY-READ-API** — MOVED OPEN→FIXED (2026-09-14) | low (PACKAGE) — **RESOLVED upstream in `provident-ssr@0.5.0` (2026-09-12)** + host U-JR1 landed | No engine API enumerated journal entries / a numeric position; `Runtime.journal(action)` exposed only the `JournalResult` summary, so a C16 history sub-pane (W2-Q14 / U-EDIT-2) had no ordered entry list. | A host wanting a history panel had no read API. | `Supervisor.journalEntries(opts?)` did not exist before 0.5.0. | **Handled upstream — NEVER patched here.** RESOLVED in `provident-ssr@0.5.0` (2026-09-12): `Supervisor.journalEntries(opts?): JournalView` ships the sanitized JSON-safe journal reader (`JournalEntryView {index,kind,status}` + `fromIndex`/`totalEntries`/`truncated`/`undoDepth`/`redoDepth`/`basePresent`/`undoBaseBoundary`/`undoTopKind?`/`redoTopKind?`/`maxJournalLength?`; window `afterIndex?`/`limit?` clamped `[1,1000]`). Host consumption LANDED (2026-09-13) via the re-scoped **U-JR1** `provident.get_journal` host tool (`tests/unit-ujr1-get-journal.test.ts` 35/35, blind-greens 22/22). See `docs/HANDOFF.md`. |
| **HOST-APPLYCOMMAND-PLACEMENT-STATESLICE** — MOVED OPEN→FIXED (2026-09-14) | medium (HOST) — **FIXED (2026-09-11)** | `Runtime.applyCommand({kind:'state-slice',…})` was a **silent no-op for PLACEMENT-ROUTED content-root nodes** (the same op worked for a template-child node and via the handler `ClientAPI.apply` path). | (repro as originally filed). | `applyCommand` called `supervisor.apply` directly and its post-apply recompile hardcoded `node.compile(focusedSlice)`. | **FIXED (host):** `state-slice` now routes through `this.supervisor.clientAPI.apply(nodeId, mutation)` (mapped `ApplyResult` → `{status,dirtied,minted}`); the recompile mirrors `runPass2AndFlush` (`compilePath()` for placement-routed nodes else `compile(focusedSlice)`) + `recordResolved`. Regression: `tests/unit-runtime-applycommand-placement.test.ts` (3 tests). Trio green (170 files / 3950 pass + 44 skip). |
| **HOST-PANE-STALE-ON-CONTENT-CHANGE** — MOVED OPEN→FIXED (2026-09-14) | medium (HOST) — **FIXED (2026-09-11)** | The app-graph PANES (doc-nav/crosslinks/search) were NOT re-rendered on a RAG content change — `applyContentChange` reconciled only `rag-` document roots, so pane subtrees kept their load-time content until a full reload. | Edit a document so the doc-nav/backlinks should change; the `pane-doc-nav`/`pane-crosslinks` subtrees show the pre-change list. | `SidebarPanes.applyContentChange` reconciled the traversal envelope's `rag-` roots only; `pane-<id>` roots were excluded by `asContentRoot` and `extractContentRoots`/`destroyRoot`. | **FIXED:** `applyContentChange` runs `assembleAppGraphEnvelope` (pane-inclusive) + reconciles it; `asContentRoot` accepts `pane-` roots and shape-compares them always; `extractContentRoots`/`destroyRoot` accept `pane-`. Pinned by the amended **U-STATE-1a** §4 state 11. Trio green (159 files / 3804 pass). |
| **ENG-SUPERVISOR-HOOK-ACCUMULATION** — MOVED OPEN→FIXED (2026-09-14) | medium (PACKAGE) — **FIXED in `provident-ssr@0.4.1` (2026-09-11)** | Each `new Supervisor(...)` registered a MODULE-LEVEL `onNodeFinalized` hook never removed, so a discarded Supervisor (and its node map) was retained forever. | Create N Supervisors in a loop; the finalize-hook list grows by N and prior Supervisors remain reachable. | `provident-ssr` `dist/core/registry.js:312-315` appended the hook; `dist/core/supervisor.js:129-134` registered it; no deregistration/dispose surface. | **Handled upstream — NEVER patched here.** FIXED in 0.4.1: `Supervisor.dispose()` added (`supervisor.d.ts:97-99`) — releases the module-level finalize hook + node maps; idempotent; a disposed Supervisor is inert. Host upgrades to `^0.4.1` and calls `dispose()` on a discarded Supervisor. Trio green on 0.4.1. |
| **ENG-DESTROY-PLACEMENT-ANCHOR-RESIDUE** — MOVED OPEN→FIXED (2026-09-14) | low (PACKAGE, latent) — **FIXED in `provident-ssr@0.4.1` (2026-09-11)** | `Node.destroyLinks` dissolved only `child` anchors; `content`/`container` PLACEMENT anchors survived destroy on a reused hub, accumulating one stale anchor per destroyed root. | Destroy a placement-routed content root on a reused hub, then attach a replacement; the `main` placement Link retains the destroyed root's `content` anchor. | `provident-ssr` `dist/core/node.js:830-841` (`destroyLinks`) removed `child` anchors only. | **Handled upstream — NEVER patched here.** FIXED in 0.4.1: `destroyLinks` now detaches THIS node's `content`/`container` placement anchors from the SHARED per-name Link via `removeAnchor` (never `link.destroy()`, preserving other producers/consumers). Host upgrades to `^0.4.1`. |
| **HOST/U1-ENG** — MOVED OPEN→FIXED (2026-09-14) | a-big (high) — **RESOLVED upstream (`BOOLEAN-ATTRS`, 2026-09-12; present in `provident-ssr@0.5.0`)** | `DomAdapter.setProp` had NO boolean-attribute special-casing: `checked:false`/`selected:false` went to the generic `else` branch (`el.setAttribute(attr, bakeValue(val))`), leaving the boolean attribute PRESENT (an UNchecked/UNselected control was unrenderable via a `false` prop). | Author a radio `input` (or `select`/`option`) with `checked:false` (or `selected:false`); it rendered CHECKED/SELECTED. | `DomAdapter.setProp` generic `else` branch (`dist/core/adapters.js` ~196-202) routed every non-special attribute to `el.setAttribute(attr, bakeValue(val))`. | **Resolved upstream — NEVER patched here.** A closed `BOOLEAN_ATTRS` set + `booleanAttrValue`: DOM boolean attrs are presence/absence (OFF **removes** the attribute, never `="false"`) and reflect the DOM property when one exists; SSR omits when OFF. Verified `dist/core/adapters.js:300-315,515-528` (the code comment names HOST/U1-ENG). Consumed by U-EDIT-2's `disabled: undoDepth <= 0`. See `docs/HANDOFF.md` RESOLVED. |
| **ENG-BODYRUNS-WIRE-REF-PATHSTATE** — MOVED OPEN→FIXED (2026-09-14) | a-big (high) — **OBSOLETE / SUPERSEDED** | The `bodyRuns` `{ child: <authoredId> }` emit-boundary translation (provident-ssr 0.3.1) resolves the child wire correctly for a TEMPLATE-AUTHORED node, but NOT for a PLACEMENT-ROUTED (path-state) content root — and worse, emitting `bodyRuns` there DROPS the node's children (the inline `strong` + the `textarea` overlays) entirely instead of interleaving. This is the EXACT rendering model Astrographer uses (every RAG subtree root is a `LegacyContentPayload` with `placement.targetPlacement` into a zone producer). The ADAPTER run-child lookup (`adapters.js:121` DOM `this.wires.get(wireKey(run.child))` and the SSR contentHtml equivalent) looks up by a BARE `wireKey(run.child)`, but a placement path-state child is registered under the COMPOSITE key `pathKey\0forkKey` — so the child is not found and the run is dropped. No host-side `bodyRuns` rewrite fixes it (even composite-key refs drop; verified). | Render a placement-routed content root whose subtree carries `bodyRuns: [{ child: 'inline-0' }, { text: … }]` + a child authored `props.id: 'inline-0'`. Direct template-root node → `<strong>Proposal:</strong> Astrographer` (correct); placement-routed into a zone → only ` Astrographer` renders (children dropped). Verified with provident-ssr 0.3.1 AND 0.3.2 + the Astrographer dom-shim/Runtime + the real `buildTraversal` envelope (the placement-routed content-root case STILL drops the children in 0.3.2; 0.3.2 fixed ONLY the def-fill/component-prototype case). | **ROOT CAUSE (corrected 2026-08-31 by the M3 TestWriter — it is the ADAPTER run-child lookup, NOT the resolver).** The engine's `resolveBodyRunsChildWires` (0.3.2) DOES resolve the placement-routed `{ child: 'inline-0' }` to the child's correct PATH-KEY wire (`root/main/.../node-4`), which IS a member of the placement parent's `childOrder`. The drop happens in the ADAPTER. | **OBSOLETE / SUPERSEDED — the `bodyRuns` mechanism was discarded upstream in `provident-ssr` 0.4.0 (superseded by the bare-`text`-child + content-XOR-children model); the adapter run-child-lookup defect is therefore not applicable to any current upstream mechanism or consumer, and the host's `buildSubtree` uses the 0.4.0 XOR model.** Formerly kept OPEN strictly as an upstream-handoff (`docs/HANDOFF.md` OPEN handoff table) — now CLOSED (the mechanism no longer exists upstream). |
| **ENG-INLINE-ORDER** — MOVED OPEN→FIXED (2026-09-14) | a-big (high) — **FULLY RESOLVED (2026-08-31)** | Inline formatting ORDER was lost: a formatted span that PRECEDES plain text (e.g. `**Proposal:** …`) rendered AFTER the content text; the `RagNodeChild` model (`content` = all plain text + `children` = all formatted spans) had no interleaving position. | Import `- **Proposal:** Astrographer…`; `get_rendered_html` shows the `<li>` content text first, then the `<strong>Proposal:</strong>` child. | The framework rendered `escapeText(content) + children` (`contentHtml`) with no text/element interleaving; `RagNodeChild` (`content` + `children`) could not represent order, and `buildSubtree` (`src/main/traversal.ts`) emitted content first then children. | **FULLY RESOLVED — provident-ssr 0.4.0's bare-`text`-child XOR model SUPERSEDED `bodyRuns` (0.3.0-0.3.2):** text beside children is a `text` child in `childOrder`, so interleave is deterministic WITHOUT `bodyRuns`. Astrographer upgraded to `provident-ssr@0.4.0` and rewrote `buildSubtree` to the XOR shape (no `content`; interleaved `text` + inline-span children). Verified: `**Proposal:** Astrographer` → `<strong>Proposal:</strong> Astrographer` (see `docs/HANDOFF.md`). The former OPEN row's "HOST-SIDE RE-EXPRESSION … still OPEN here" note is **STALE** — no host follow-up remains. |

| **HOST-MS4-10** | info | The MCP `edit.import_markdown` handler SILENTLY DROPS non-string `files` elements: `src/main/mcp-server.ts:1488` pre-filters `(x): x is string => typeof x === 'string'`, so a mixed-type array (`['a.md', 42]`) imports ONLY the string elements and the importer's pinned non-string-element fail-state (`markdown import: empty file path`) is UNREACHABLE via the MCP surface — a silent partial ingest contrary to the importer's fail-state contract. | Drive `edit.import_markdown { files: ['a.md', 42] }` through the MCP surface with a valid `a.md` present: the call returns ok and imports only `a.md` (the non-string `42` is silently dropped); the SAME input passed directly to `importMarkdownCorpus` returns `markdown import: empty file path`. Found in the U-MS4 adversarial pass (finding F-MS4-10, registered in the U-MS4 spec §3a). | The import handler's caller-side `files` pre-filter (`mcp-server.ts:1488`) widens the importer's strict input contract BEFORE the importer can validate it — a PRE-EXISTING handler behavior (not introduced by U-MS4/U-MS2). | Host fix (in its owning unit, NOT this review pass): fail LOUD on a mixed-type `files` array by NOT pre-filtering — let the importer's byte-pinned `markdown import: empty file path` fire for a non-string element — OR document the filter as a sanctioned silent-drop behavior. Recorded OPEN for its owning unit by the U-MS4 doc review (2026-09-05); review record `archive/reviews/2026-09-05-unit-ms4-doc-review.md`. **Confirmed still OPEN at the U-MS5 doc review (2026-09-05):** the multi-store slice does NOT fix it — U-MS5 (read-only settings listing + the `RagQueryPayload.store` passthrough) touches neither the import handler's `files` pre-filter nor the importer's strict-input contract;  **Confirmed NOT changed by U-IMPORT-1 (2026-09-14):** U-IMPORT-1's `resolveImportSelection` rule-7 whole-poison (`src/main/import-directory.ts`) is the BROWSE surface and does NOT touch the import handler's `files` pre-filter (mcp-server.ts:1488) or the importer's strict-input contract — HOST-MS4-10 remains a later host-fix candidate. | | **FIXED (2026-09-14, host defect-resolution pass; regression `tests/unit-defect-resolution.test.ts`)** |
| **HOST-SNAPSHOT-COPY-NODEKIND** | low (HOST) | `adjacency.copyNode` (`src/main/adjacency.ts:44-46`) drops `nodeKind` when copying a `RagNode`, so a `createSnapshotStore` node view diverges from the JSON store for a `nodeKind`-bearing record (`getNode` returns `nodeKind` undefined). Found in the U-D1 adversarial pass (F2, 2026-09-11). | `createSnapshotStore([nodeWithNodeKind],[]).getNode(id).nodeKind` → `undefined`; the JSON store returns the kind. | `copyNode` is a field-by-field copy that was not updated when Unit X added the optional `nodeKind` field (U-D1 added `documentPath`/`tags` there). | Host fix: add `nodeKind: n.nodeKind` to `copyNode` (mirror `toPublicNode`). Deferred out of U-D1 (not a U-D1 field); fix in its owning context. | | **FIXED (2026-09-14, host defect-resolution pass; regression `tests/unit-defect-resolution.test.ts`)** |
| **HOST-JOURNAL-CHILD-DANGEROUS-KEY** | low (HOST) | `isValidChildren` (`src/main/rag-store.ts:506-518`) applies the prototype-pollution guard to a child's `props` but not the child object ITSELF, whereas `validateNodeShape` (F4) rejects a dangerous key on the child. A structural journal entry carrying a child with a `__proto__` own-key is retained at boot. Impact contained (`insertNode`→`deepCopy` strips dangerous keys). Found in the U-D1 adversarial pass (F3, 2026-09-11). | Craft a structural journal entry whose node's `children` contains a child with an own `__proto__` key; it survives boot (`journal().length === 1`) while `validateNodeShape` on the same node would reject it. | `isValidChildren` (Unit M) mirrors only the `props` guard, not the F4 child-own-key guard later added to `validateNodeShape`. | Host fix: add `hasDangerousKey(child)` to `isValidChildren` to mirror `validateNodeShape`. Deferred out of U-D1 (pre-existing journal hardening); fix in the owning context. | | **FIXED (2026-09-14, host defect-resolution pass; regression `tests/unit-defect-resolution.test.ts`)** |
| **HOST-NODEKIND-JOURNAL-INVERTIBILITY** | info (HOST, pre-existing, out of scope) | A `nodeKind`-only edit is not journal-invertible: `putNodeSync` takes the lightweight `content` branch (its `typeChanged`/`ownedChanged`/metadata conditions are all false, and the `content` snapshot carries only `content`/`children`/`props`), so `undo()` leaves the new `nodeKind` in place. Present since Unit X. U-D2 (metadata journal invertibility) did not fix it (out of contract) and strictly improves the combined case (a metadata+`nodeKind` edit now takes the structural `node-update` branch and inverts `nodeKind` too). Found in the U-D2 adversarial pass (F1, 2026-09-11). | `putNode` a node with `nodeKind` undefined, then `putNode` the same id with `nodeKind:'fact'`; `journal().at(-1).kind` is `'content'` and `undo()` leaves `getNode(id).nodeKind === 'fact'` (expected restored to undefined). | The metadata-invertibility fix extended the structural-branch condition to `typeChanged || ownedChanged || documentPathChanged || tagsChanged` but not `nodeKindChanged`; the `content` snapshot has no `nodeKind` field. | Host fix (future unit): add `nodeKindChanged` to the structural-branch condition (mirror the metadata delta), or extend the `content` snapshot with `nodeKind`. Recorded OPEN; not part of U-D2. | | **FIXED (2026-09-14, host defect-resolution pass; regression `tests/unit-defect-resolution.test.ts`)** |
| **HOST-DOCID-DOT-SEGMENT** | info (HOST, pre-existing, latent) | `sanitizeDocumentId` can mint a `.`/`..` documentId segment from a basename: root `...md` → id `..`; root `..md` → `.`; `a/...md` → `a/..`. Both import OK and store a root node. No current consumer treats `documentId` as a filesystem path (the parser interpolates it; the UI addresses it via `rag-<id>` over a Map/`getElementById`), so it is latent. Found in the U-D3 adversarial pass (ADV-3, 2026-09-11). | Import `...md` / `..md` at the corpus root; `documentId` is `..` / `.` and a root node is stored. | `sanitizeDocumentId` strips a trailing `.md` BEFORE the charset pass (`markdown-import.ts:69-76`), so `...md` → `..`; `deriveDocumentPath` never emits `.`/`..` (it uses `dirname(relative(...))`). Pre-existing (M4 kept the basename sanitizer unchanged). | Host fix (future unit, before any id-as-path use): reject a basename segment that sanitizes to `.`/`..` (an empty-ish/`.`-segment fail-state) or map `.`/`..` to `-`. Recorded OPEN; not part of U-D3. | | **FIXED (2026-09-14, host defect-resolution pass; regression `tests/unit-defect-resolution.test.ts`)** |
| **HOST-RAG-GROUP-LABEL-STALE** | info (HOST, pre-existing) | `src/renderer/secure-panels.ts` `GROUPS`/`GROUP_LABELS` describes the `rag` group as "rag.query, get_document, list_nodes, get_edges, backlinks" — already missing Unit X's `rag-stream`/`get_query_audit_log` and now Unit U-D5's `rag.list_documents`. The Security settings pane therefore under-describes the group's tools. Found in the U-D5 adversarial pass (F5, 2026-09-11). | Open the Security settings pane and compare the `rag` group label to `TOOL_GROUPS`'s 8 `rag` rows. | The label is a hand-maintained string that was not updated when `rag-stream`/`get_query_audit_log`/`rag.list_documents` landed. | Host fix (a later cosmetic/docs unit): derive the label from `TOOL_GROUPS` or update the string; low priority (no behavior change). Recorded OPEN; not part of U-D5. | | **FIXED (2026-09-14, host defect-resolution pass; regression `tests/unit-defect-resolution.test.ts`)** |
 **FIXED (2026-09-16):** the root cause was in `src/main/traversal.ts` `buildSubtree` — EVERY subtree root (each section AND every nested block) was authored with `placement: { targetPlacement: [zoneName] }`, so `translateLegacy` minted a `content` anchor for the section AND for each nested block; the placement-path enumeration then produced TWO routes for the same node (one through the section, one through the nested child) and the render emitted the child twice — nested inside the section and again as a zone-level sibling. The fix threads a `placed` flag through `buildSubtree`: only a PAYLOAD ROOT (a section, or a genuine multi-parent duplicate) carries the zone `targetPlacement`; a nested subtree is reached through its owner's route and announces none. `renderSubtreeMarkdown` re-adds the anchor on a CLONE for its line-map measurement, so the markdown ranges are unchanged. Tests: `tests/import-render-no-duplicates.test.ts` (NEW 3 tests: per-rag-id element census on a real `/docs/specs` import; the RENDERED doc has no duplicate element ids) — red→green. **LIVE END-TO-END (2026-09-16):** importing `docs/specs/user-flow-audit.md` (`edit.import_markdown`, 110 nodes / 213 edges) and focusing it → the rendered stage has **137 `[data-rag-node-id]` elements with ZERO duplicate rag nodes, ZERO duplicate element ids**, and all 12 ≥50-char source-text probes appear EXACTLY ONCE (0 missing, 0 doubled) — the user's 1-1 requirement holds. Trio 4746 pass / 58 skip / 0 fail.  **FIXED (2026-09-16, `src/renderer/runtime.ts` `tearDownGraph`):** the previous graph's `#wiki-root` ELEMENT outlives its node — `loadEnvelope` replaces the supervisor, so the old root is never destroyed and its element stays in the mount as an in-flow `display:grid` box. `tearDownGraph` now removes every element carrying a graph ROOT id (`#wiki-root`) from the mount BEFORE the new render (the diff-based render can only remove elements it tracks, and a root owned by a discarded supervisor is untracked by definition); fail-soft. **LIVE-CONFIRMED:** at boot and after TWO app-graph re-assemblies (a pane-visibility toggle each way) the page holds exactly **ONE `#wiki-root`** with `#app` top at **36px** and `scrollY 0` — previously a childless stale mount (h ≈ 11 589px) sat above the live one and pushed the canvas ~11 000px down. Tests: full suite 4746 pass / 58 skip / 0 fail. 
