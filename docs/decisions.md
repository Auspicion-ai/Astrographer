# Astrographer — Active Decisions Summary

Maintained by the document-archival loop (AGENTS.md item 6). ACTIVE =
governs the current project; SUPERSEDED = replaced (provenance only).
Decisions are recorded as `DECIDED:` / `ACTIVE` / `SUPERSEDED` rows and kept
in sync with `docs/specs/*.md`. Imported constraints from the foundation /
upstream project are marked **IMPORTED** (their canonical record is in
`../Provident-Electron/docs/decisions.md` / `../Preempt-Providence/docs/`).

## ACTIVE

| Decision | Date | What it pins | Source |
| --- | --- | --- | --- |
| **DECIDED: RAG-AUTHORITATIVE — the RAG layer is authoritative; the provident graph is a transient render materialization produced by a RAG traversal** | 2026-08-26 | The RAG store (RAG nodes + edges) is the persistent source of truth. The provident graph only exists after compilation by the RAG traversal (a `LegacyInitialData` envelope → `translateLegacy` → `renderProducingProcess`). The graph is a pure projection of the RAG store, re-traversed on ANY RAG-store change (content or structural — **CONTENT-EDIT-RE-TRAVERSAL** supersedes the earlier in-place state-slice content-edit path; the re-traversal is the renderer's response to the store change). Invariant: "the RAG store is always correct; the materialized graph is always re-derivable." The engine's graph-is-authoritative pin holds at the graph→HTML boundary. | user clarification 2026-08-26, `docs/specs/astrographer-review.md` §8.1/§9 |
| **DECIDED: SUBTREE-OWNERSHIP — a RAG object OWNS a subtree of Provident nodes (many-to-one back-reference `Map<ragNodeId, nodeId[]>`)** | 2026-08-26 | A RAG object maps to a subtree (a `contentNodes`-owned content root + its children), NOT a single node. The subtree root can link children and have no family parent, attached into a root-visible zone via placement/component logic. The RAG object's text = the markdown of its whole subtree, embedded as ONE chunk (a `ul` + its `li` children are one RAG object / one embedding). The back-reference is many-to-one, rebuilt per traversal. | user clarification 2026-08-26, `docs/specs/astrographer-review.md` §10 |
| **DECIDED: MARKDOWN-EXPORT-ONLY — markdown is an export-only surface; agent changes go through direct MCP updates** | 2026-08-26 | The `MarkdownAdapter` output is a read/export surface for the agent, NOT an edit surface (the ownership loss from D7 is accepted). Agent-driven changes go through direct MCP `edit`-group mutations to the RAG store, not markdown round-trips. Diffing markdown vs. generated source is a FUTURE SPECULATIVE feature (`docs/pending.md`). | user decision 2026-08-26, `docs/specs/astrographer-review.md` §11 |
| **DECIDED: FORM-CONTROL-EDITING — editing is commit-on-blur via a provident-rendered textarea/input, writing back to the source RAG object** | 2026-08-26 | Editing is a form control (textarea/input) committed on blur, writing back to the source RAG object (via the back-reference). NOT contenteditable (fights graph-is-authoritative; `DomAdapter.text` clobbers a live editor). NOT a zone-targeted state-slice (FS-10 blocks it — `placement-target-blocked`) — write back to the RAG store → re-traversal. A dirty-edit guard queues (not executes) a rebuild while a control is dirty. Caret/focus is host-side state keyed by RAG node id, restored after rebuild. | user confirmation 2026-08-26, `docs/specs/astrographer-review.md` §3b/§9 |
| **DECIDED: CONTENT-EDIT-RE-TRAVERSAL — a content edit writes back to the RAG store, then the renderer re-traverses in response to the store change** | 2026-08-26 | A content edit (MCP `edit.set_content` or UI commit-on-blur) writes back to the RAG store (via the back-reference), then triggers a re-traversal to re-materialize the graph consistently (including multi-parent duplicates). The `edit.set_content` tool updates the store; the re-traversal is the renderer's response to the store change. **Supersedes the "no re-traversal" note in Unit B §5.4 for `edit.set_content`** (the content op is still journaled as a `content` entry — Unit A §5.6; the re-traversal is the renderer's response, not a journaling concern). The renderer re-traverses on ANY RAG-store change (content or structural) — the strongest coherence guarantee. Re-traversal is the ONLY content-edit coherence mechanism (no in-place state-slice path). | `docs/specs/unit-d-editing.md` §4/§5.1.9 |
| **DECIDED: DERIVED-DOC-FLOW — doc-flow edges (doc-head/next-section/doc-end) are authoritative in the RAG store; the traversal maps them to family order + a doc-head marker prop** | 2026-08-26 | The RAG store carries doc-flow edges as authoritative. The traversal realizes them in the engine as family child-anchor order + a doc-head marker prop (no new engine roles — closed `Role` union). The traversal VALIDATES the edges (cycle/missing-node/missing-head/missing-end) and falls back to family pre-order on violation. | user clarification 2026-08-26, `docs/specs/astrographer-review.md` §3c/§9.2.3 |
| **DECIDED: LEXICAL-FIRST-RETRIEVAL — BM25/tf-idf behind an interface-swappable `Embedder`; graph traversal for context assembly** | 2026-08-26 (implemented 2026-08-27, Units E + F) | Retrieval is lexical-first (deterministic, offline, testable) behind an `Embedder` interface; vector embeddings implemented (Unit F, 2026-08-27). Selection scores all RAG nodes, takes top-k, then assembles context by graph traversal (bounded). The line→node map is a first-class assembly output (coarse: whole subtree → one RAG object). **Implemented (Unit E):** `src/main/retrieval.ts` — the `Embedder` interface + the lexical (BM25) v1 default, the maintained lexical index (incremental on `onStoreChanged`), `selectTopK`/`assembleContext`/`retrieve`/`createRetrieval`, and MCP/UI equivalence (the `rag.query` MCP tool and the `rag-query` IPC call the same maintained engine, `main.ts`). **Implemented (Unit F):** the `Embedder` interface is amended to ASYNC and the vector embedder (`src/main/embeddings.ts`) is a drop-in behind the SAME seam — `retrieval.embedder: 'lexical' | 'vector'` selects it in `main.ts`; the engine uses the passed embedder unchanged. | `docs/specs/astrographer-review.md` §3d/§9.2.10, `docs/specs/unit-e-rag-index.md` §4/§5, `docs/specs/unit-f-embeddings.md` §4/§5 |
| **DECIDED: SINGLE-WRITER-STORE — the RAG store is the lock point; the main process owns all writes; MCP and UI both route through it** | 2026-08-26 | A main-process `createJsonRagStore` (module-store pattern) serializes writes through a queue/mutex. MCP `edit` tool handlers and the renderer's commit-on-blur IPC both call the same main-process store methods. No renderer-side writes to the RAG store. This is the concurrency model for MCP↔UI equivalence. | `docs/specs/astrographer-review.md` §9.2.6 |
| **DECIDED: RAG-EDIT-MCP-GROUPS — new read-only `rag` + mutating `edit` MCP tool groups, both default-off, through the five-seam gate** | 2026-08-26 | `rag` (read-only, default-off): `rag.query`, `rag.get_document`, `rag.list_nodes`, `rag.get_edges`, `rag.backlinks`. `edit` (mutating, default-off): `edit.set_content`, `edit.create_node`, `edit.delete_node`, `edit.split_node`, `edit.merge_node`, `edit.set_edge`. Editing is NEVER a `code`-group op. Registered through the five-seam gate (`security.ts` TOOL_GROUPS + `mcp-server.ts` ALL_TOOLS + `shared/types.ts` RpcMethod + renderer switch + MUTATING_METHODS). | `docs/specs/astrographer-review.md` §9.2.7 |
| **DECIDED: MULTI-PARENT-DUPLICATE — a multi-parent RAG node is materialized as duplicate subtrees (distinct content roots sharing the RAG id via the map), respecting SI-1** | 2026-08-26 | The engine's single-parent family model (SI-1) cannot represent a multi-parent RAG node. Duplicate-per-parent (each a distinct provident content root sharing the same RAG object id via the back-reference map) is the only option that respects the engine model. Content edits must update ALL materialized duplicates (state-slice every duplicate), or explicitly accept cross-duplicate staleness until the next structural rebuild. | `docs/specs/astrographer-review.md` §9.2.8 |
| **DECIDED: PROJECT-JOURNAL — undo/redo lives in the RAG store's own project journal, not the engine Supervisor journal** | 2026-08-26 | The engine Supervisor journal dies on rebuild (a re-traversal mints a fresh graph). Undo/redo lives in the RAG store's project journal, which records INVERTIBLE entries for both content and structural ops (undoing a structural edit re-traverses). | `docs/specs/astrographer-review.md` §9.2.1 |
| **DECIDED: ENGINE-GAP-HANDOFF — a missing capability is an engine gap (handoff) iff it cannot be implemented by composing existing engine primitives in host code** | 2026-08-26 | Transient graph, back-reference, and RAG→subtree traversal are all project-specific (compose `translateLegacy`/`loadEnvelope`/`renderProducingProcess`/managed channel/five-seam gate). The ONE engine gap: `MarkdownAdapter` `data-node-id` preservation (D7) → handoff doc, non-blocking (the host-side line→node map covers it). The RAG layer is project-specific, not a foundation feature. | user note 2026-08-26, `docs/specs/astrographer-review.md` §8.3/§9.2.11 |
| **DECIDED: DOC-CHILD — a RAG object can own a subtree that CONTAINS nested subtrees owned by doc-child RAG objects (nested semantic units)** | 2026-08-26 | A nested provident node that is large enough for semantic distinctiveness (e.g. a paragraph-length `li` inside a `ul`) is its own RAG object, a doc-child of the containing RAG object. The `doc-child` edge (`{ kind: 'doc-child', source, target, order }` — `source` = the containing RAG object, `target` = the nested RAG object, `order` = the child's subtree position) expresses HIERARCHICAL nesting, distinct from the linear doc-flow edges (`doc-head`/`next-section`/`doc-end`). A RAG object's `ownedNodeIds` EXCLUDES the nodes owned by its doc-children. The traversal materializes doc-child subtrees at their `order` position within the parent's subtree. A doc-child's text = the markdown of its OWN subtree, embedded as a SEPARATE chunk. | user clarification 2026-08-26, `docs/specs/astrographer-review.md` §12 |
| **DECIDED: SOURCE-SWITCHABLE — the `RagStore` interface is the abstraction layer between the JSON store and the document load** | 2026-08-26 | `RagStore` (§5.4) is the INTERFACE the document load (Unit C traversal) and the MCP `rag`/`edit` handlers depend on — NOT the concrete JSON store. `createJsonRagStore` is the JSON-file implementation; a future `createRemoteRagStore` (pending — `docs/pending.md`) implements the SAME interface. The store is injected (constructor/options), never imported directly by the traversal/handlers, so the source is switchable without changing the document load. | user request 2026-08-26, `docs/specs/unit-a-rag-store.md` §5.3 |
| **DECIDED: STRUCTURAL-ROOT — a RAG object's subtree root can be a STRUCTURAL element type (e.g. `div`), not just a semantic element** | 2026-08-26 | A `div` (and other structural element types) is a valid `RagNodeType` root. It groups semantically-related but syntactically-unstructured text lines into ONE RAG object / one semantic chunk (e.g. a lazy/shorthand note like "S3 Bucket types / General Purpose / Vector / Directory / Table" written as plain lines with no `ul`/`ol` structure). The `div` root's children are the text lines; the whole subtree is one RAG object / one embedding. Same subtree-ownership model as `ul`/`ol` — the structural root is a grouping container. | user clarification 2026-08-26, `docs/specs/astrographer-review.md` §14 |
| **DECIDED: CROSS-DOCUMENT-SHARED — a RAG node can appear in MULTIPLE documents at the same time (shared/transcluded nodes)** | 2026-08-26 | A shared node (e.g. A's spec, called by both Class B and Class C) has MULTIPLE `parent-child` edges (one per document) and is materialized as duplicate subtrees in each document, all sharing the RAG id via the back-reference map. A text change updates all duplicates ("if the text of A changes, so do both documents"). **An edge carries a `documentIds: string[]` field (the document root node ids that OWN/USE the edge) — an edge can have MULTIPLE document owners** (a shared reference/content edge, e.g. the A→D edge, lists both B and C). A `next-section` edge's TARGET differs per document, so those are separate edges each with ONE owner. The traversal assembles each document's flow separately. Cost: a node referenced by N documents is materialized as N duplicate subtrees (the price of respecting SI-1). **Material-state nuance (2026-08-26):** the N duplicates are PER-RENDER materializations; render is transient. In the single-document view only ONE document renders at a time, so only ONE instance of a shared subtree exists at any moment (documents cannot loop in one view). The N duplicates COEXIST only if multiple documents render simultaneously — which requires UI TABS (a pending feature, `docs/pending.md`). | user clarity check 2026-08-26, `docs/specs/astrographer-review.md` §13 |
| **DECIDED: PROVIDER-AGNOSTIC — all agent/model-specific tasks (embeddings, and any future model calls) are provider/model AGNOSTIC, including remote/cloud-sourced providers, with config settings for the required inputs (model URL, API keys, etc.)** | 2026-08-27 (implemented 2026-08-27, Unit F) | The implementation of any model/agent-specific capability is NOT hardcoded to one provider/model. It is provider/model agnostic behind an interface, configurable to point at ANY provider — local (ollama `embeddinggemma`) OR remote/cloud — via config settings for the required inputs (model URL, API keys, model name, etc.). The `Embedder` interface (Unit E) is the seam; the vector embedder (Unit F) is a configurable provider, with ollama `embeddinggemma` as ONE concrete config and remote/cloud providers as others. **Implemented (Unit F, 2026-08-27):** `src/main/embeddings.ts` — the `EmbeddingProvider` abstraction + `EmbeddingProviderConfig` config shape, the ollama `embeddinggemma` concrete provider (localhost), the remote/cloud provider drop-in (OpenAI/Cohere/etc. via the SAME interface + config), and the `connect-src` CSP allowlist + API-key handling as a DESIGNED security surface. **Security implication:** supporting remote/cloud providers means network egress IS in scope — the pending.md `connect-src` CSP allowlist + API-key handling become a designed security surface (not just an open tracked item). | user clarification 2026-08-27, `docs/specs/unit-f-embeddings.md` |
| **DECIDED: CROSSLINK-EDGE-KIND — a `crosslink` RAG edge kind (`{ kind: 'crosslink', source, target, documentIds? }`) is the authoritative crosslink representation** | 2026-08-27 | A crosslink is a NEW RAG edge kind `crosslink` in the `RagEdgeKind` union — the authoritative representation. It reuses the `RagEdge` shape (Unit A §5.1), the store's edge CRUD, and the project journal. It is NOT a node property and NOT a separate link registry. The RAG store is authoritative; the traversal materializes the graph; the backlink enumeration reads the store. | `docs/specs/unit-g-crosslink-backlink.md` §4 |
| **DECIDED: CROSSLINK-LINKCONFIG — a custom `LinkConfig` with `name: 'crosslink'`, `roles: ['source', 'target']` materializes crosslinks into the provident graph** | 2026-08-27 | The custom crosslink `LinkConfig` is `{ name: 'crosslink', roles: ['source', 'target'] }` — a custom name in the engine's OPEN `LinkConfig.name` union. The traversal materializes each outgoing crosslink edge as a DISTINCT `Link` (created directly via `new Link(CROSSLINK_LINK_CONFIG)`, NOT via `createLinkHub` — the hub is the engine's same-name sharing mechanism for `component`/`placement` links and would inherit parent-child roles for a custom name). The `Link` carries a `source` anchor on the source RAG node's subtree root and a `target` anchor on the target RAG node's subtree root. | `docs/specs/unit-g-crosslink-backlink.md` §4 |
| **DECIDED: PANE-REGISTRY — a host-side `PaneRegistry` is the single authority over which sidebar panes exist and which are enabled** | 2026-08-28 | `PaneDefinition` = `{ id, title, scope: 'app-graph' | 'operator', render }`. The registry provides the 9 methods `register`/`get`/`list`/`listByScope`/`isEnabled`/`enable`/`disable`/`setEnabled`/`onChanged`. Newly registered panes are DISABLED; the host enables the panes it wants (the app-graph panes must be enabled to be MCP-visible). A PURE module (no Electron) — testable in isolation. Implemented in `src/renderer/pane-registry.ts`. | `docs/specs/unit-h-sidebar-panes.md` §4/§5.1 |
| **DECIDED: PANE-PROVIDENT-AUTHORING — every pane's content is authored as provident-ssr data and rendered through the producing graph** | 2026-08-28 | Each pane's `render(ctx)` returns a `LegacyNodeData` subtree; app-graph panes are authored into the pane-inclusive app-graph envelope (via `paneSubtreeRoot`, `src/renderer/pane-graph.ts`), operator panes into the isolated-scope envelope (`buildOperatorEnvelope`). A pane rendered outside the provident graph is a review finding (the project-wide constraint, AGENTS.md). | `docs/specs/unit-h-sidebar-panes.md` §4/§5.2 |
| **DECIDED: APP-GRAPH-PANES-MCP-VISIBLE — the document-navigation, crosslink/backlink, and search panes render in the app Runtime graph → MCP-visible** | 2026-08-28 | The `doc-nav`/`crosslinks`/`search` panes are `scope: 'app-graph'`. Their `render(ctx)` subtrees are authored into the pane-inclusive envelope the app Runtime renders, so `get_rendered_html`/`get_markdown`/`list_targets`/`get_node_state`/`provident.dispatch` see them (the §8.2 BINDING equivalence). | `docs/specs/unit-h-sidebar-panes.md` §4/§5.3/§5.6 |
| **DECIDED: OPERATOR-ISOLATED-GRAPHSCOPE — the settings pane is `scope: 'operator'` and renders in an isolated `createIsolatedScope()` GraphScope, never MCP-visible** | 2026-08-28 | The settings pane renders in its OWN `createIsolatedScope()` `GraphScope` (own `Supervisor` + own `DomAdapter` → own mount, mirroring `SecurePanels`), disjoint from the app graph. It is NOT MCP-visible and NOT exposed to the `rag`/`edit` groups — an agent cannot read it, list it as a target, dispatch on it, or mutate it. Its edits are operator edits through the IPC bridge, never the RAG `edit.*` path. | `docs/specs/unit-h-sidebar-panes.md` §4/§5.4/§5.6 |
| **DECIDED: CONTENT-WINDOW-TEMPLATE — the content-window template IS the `LegacyInitialData.template` shape; the default is the current FIXED `wiki-root` + per-zone containers** | 2026-08-28 | The content-window template is the `LegacyInitialData.template` shape (`{ root: LegacyNodeData }`), the field that defines the content-window layout (the root structure + the container zones) for rendering a retrieved document. The default (`DEFAULT_CONTENT_WINDOW_TEMPLATE`) is the current FIXED `wiki-root` + one `main` zone container (the Unit C baseline, `src/main/traversal.ts`). A customized template replaces it. | `docs/specs/unit-i-template.md` §4/§5.1 |
| **DECIDED: TEMPLATE-STORE — a host-side main-process template store persists the current `ContentWindowTemplate`, separate from the RAG store** | 2026-08-28 | A host-side main-process `createTemplateStore` persists the current `ContentWindowTemplate` (a JSON file, `node:fs` atomic temp+rename, fail-disabled boot to the default). It is SEPARATE from the RAG store — the template is NOT RAG content (it is the envelope's `template`, not a document-graph node/edge). | `docs/specs/unit-i-template.md` §4/§5.2 |
| **DECIDED: CODE-GROUP-TEMPLATE-CRUD — the template CRUD is exposed as the `code.template.*` tools in the `code` group, main-handled, default-off** | 2026-08-28 | The template CRUD is exposed as the `code.template.*` tools (`get`/`validate`/`set`/`create`/`delete`/`reset`), ALL in the `code` group (default-off), main-handled against the template store through the five-seam gate (the Unit B §5.3 pattern). Editing the template is a `code`-group op — consistent with the gate line 60 ("`code.*` CRUD") and the existing `provident.code.*` envelope-editing group. | `docs/specs/unit-i-template.md` §4/§5.3 |
| **DECIDED: TEMPLATE-PANE — a provident-rendered `scope: 'app-graph'` template-editor pane, Unit H authoring** | 2026-08-28 | A provident-rendered `scope: 'app-graph'` template-editor pane (`id: 'template-editor'`) follows the Unit H authoring convention. It renders the current template structure (the root + the zones) as editable provident content and commits edits via the template IPC (the SAME template store the `code.template.*` MCP tools reach). Being app-graph, it is MCP-visible: an agent reads it via `get_rendered_html`/`get_markdown` and dispatches its handlers via `provident.dispatch`, OR drives the `code.template.*` tools directly (§8.2 equivalence). | `docs/specs/unit-i-template.md` §4/§5.4 |
| **DECIDED: ZONE-CONSISTENCY-INVARIANT — a customized template MUST NOT drop a `container`-role producer for a zone the traversal targets** | 2026-08-28 | A customized template MUST NOT drop a `container`-role producer for a zone the traversal targets (the Unit C HARD PRECONDITION — a missing container leaves the content `unplaced`, silently not render-eligible). Enforced at TWO layers: (a) save-time validation (`validateTemplate` + the store's `set`/`create`/`delete`) REJECTS a template missing a targeted zone and forbids deleting a targeted zone; (b) the traversal (`buildTraversal`) defense-in-depth ADDS a missing `zoneName` producer (never leaves the subtree unplaced). | `docs/specs/unit-i-template.md` §4/§5.6 |
| **DECIDED: TEMPLATE-RE-DERIVE — a template change triggers a whole-graph re-derive, reusing the Unit C traversal + Unit H assembly** | 2026-08-28 | A template change (via the MCP `code.template.*` tools OR the UI template IPC) writes the template store, then main broadcasts `template-changed` → the renderer re-derives the whole graph: `buildTraversal` (with the stored template) → `assembleAppGraphEnvelope` → the app Runtime re-render. Infrequent, so the whole-graph cost is acceptable (review §4 item 9). | `docs/specs/unit-i-template.md` §4/§5.5 |
| **DECIDED: MCP-UI-EQUIVALENCE — every MCP tool with a UI IPC counterpart routes through the SAME shared handler (the §8.2 BINDING constraint)** | 2026-08-26 (pinned by Unit J 2026-08-28) | The same graph, the same rendering, and the same operations are reachable equivalently through the MCP surface and the Electron UI. Every MCP tool with a UI IPC counterpart routes through the SAME shared handler: `rag.query`/`rag-query` (the same maintained retrieval engine), `rag.backlinks`/`rag-backlinks` (the same `enumerateLinks`), `edit.set_content`/`edit-commit` (the same `setContent` op), `code.template.*`/`IPC_TEMPLATE_*` (the same `handleTemplateTool`). The equivalence surface is a first-class, equivalent access path, not an afterthought. | `docs/specs/astrographer-review.md` §8.2, `docs/specs/unit-j-mcp-security-hardening.md` §4/§5.4 |
| **DECIDED: HARDENING-VERIFICATION-CONTRACT — the Unit J hardening pass is a VERIFICATION contract over the already-implemented `rag`/`edit`/`code.template.*` surfaces** | 2026-08-28 | The hardening pass does NOT add new behavior — it audits the implemented surfaces and pins the invariants (a)–(f) in `docs/specs/unit-j-mcp-security-hardening.md` §5.2 that MUST hold: (a) every `rag.*` tool is read-only + `rag`-group + default-off; (b) every `edit.*` tool is mutating + `edit`-group + default-off; (c) editing is NEVER a `code`-group op; (d) every MCP tool with a UI IPC counterpart routes through the SAME handler; (e) the renderer switch fails closed on unknown methods; (f) `MUTATING_METHODS` covers every mutating method. A TestWriter derives every state and fail-state from §5.8/§5.9. If a future change breaks an invariant, it is a review finding. | `docs/specs/unit-j-mcp-security-hardening.md` §4/§5.2 |
| **DECIDED: IPC-SURFACE-NOT-GROUP-GATED — the renderer→main IPC channels are NOT group-gated; the `rag`/`edit`/`code` groups gate the MCP agent path only** | 2026-08-28 | The renderer→main IPC channels (`edit-commit`, `rag-query`, `rag-backlinks`, `IPC_TEMPLATE_*`) are NOT group-gated — the renderer is a trusted surface that calls main directly (`contextIsolation:true`, `nodeIntegration:false`). The `rag`/`edit`/`code` groups gate the MCP AGENT path only. This is a documented design decision, not a gap. | `docs/specs/unit-j-mcp-security-hardening.md` §4/§5.7 |
| **DECIDED: EDIT-COMMIT-RETURN-ASYMMETRY — the `edit.set_content` MCP tool returns `SetContentResult` (the full updated node) while the `edit-commit` IPC returns `EditCommitResult` (the nodeId only)** | 2026-08-28 | The `edit.set_content` MCP tool returns `SetContentResult` (the full updated node), while the `edit-commit` IPC returns `EditCommitResult` (the nodeId only). BOTH call the same `setContent` op and produce the same store state + re-traversal; the return shape differs because the IPC is the renderer's commit contract. This is a documented asymmetry, not a gap. | `docs/specs/unit-j-mcp-security-hardening.md` §4/§5.7 |
| **DECIDED: UI-MOUNT-BOOT — the renderer bootstraps the app Runtime with the pane-inclusive envelope derived from the RAG store + the stored template, replacing the `demoEnvelope()` bootstrap** | 2026-08-28 | The `SidebarPanes` host's `boot(runtime)` replaces the `demoEnvelope()` placeholder bootstrap: it fetches the RAG snapshot + the stored template, derives the document ids, runs `buildTraversal` (with the stored template), assembles the pane-inclusive envelope, and loads it into the app Runtime — so the RAG content + the app-graph panes are MCP-visible by construction. The host owns the current-document/node state (M5). | `docs/specs/unit-k-sidebar-panes-host.md` §5.1/§5.6 |
| **DECIDED: UI-MOUNT-RE-DERIVE — both `rag-store-changed` and `template-changed` trigger the SAME re-derive, guarded by the dirty-edit guard** | 2026-08-28 | A `rag-store-changed` or `template-changed` broadcast routes through the edit controller's dirty-edit guard (`requestRebuild`) → the host's `reDerive()`: fetch snapshot → `buildTraversal` (with the stored template) → `assembleAppGraphEnvelope` → re-load the pane-inclusive envelope + repopulate the backRefs map. The edit controller's `onRebuild` IS the host's `reDerive` (the SOLE subscription; the renderer-level duplicate is removed). Re-derive coalesces in-flight requests. | `docs/specs/unit-k-sidebar-panes-host.md` §5.2 |
| **DECIDED: UI-MOUNT-PANE-REGISTRATION — the four app-graph panes + the operator settings pane are registered + enabled by the host, and their handlers are bound to the IPC bridge** | 2026-08-28 | The host's `registerPanes()` registers the four app-graph panes (`doc-nav`/`crosslinks`/`search`/`template-editor`) + the operator `settings` pane and enables them; `bindHandlers()` registers the handler defs via `registerHandlerDef` (the engine resolves the bodies by name). The host installs the `window.provident.sidebar` bridge surface (M2) the compiled handler bodies call. | `docs/specs/unit-k-sidebar-panes-host.md` §5.3 |
| **DECIDED: UI-MOUNT-OPERATOR — the settings pane mounts in an isolated `createIsolatedScope()` GraphScope via `mountOperator`, never MCP-visible** | 2026-08-28 | The operator settings pane renders in its OWN isolated provident GraphScope (`createIsolatedScope()`) in the `#operator-panes` mount (NOT `#panes`, which stays SecurePanels'; M3). Because it is a separate scope from the app Runtime, the MCP endpoints (which read the app Runtime) can never see or dispatch it. The operator-settings IPC (`operatorSettings.get`/`set`) is the M9 surface; the `topK` setting feeds the search pane's `bridge.rag.query`. | `docs/specs/unit-k-sidebar-panes-host.md` §5.4 |
| **DECIDED: RICH-TEXT-EDITING-GATE — the rich-text contenteditable editing proposal passed the three-agent gate as PROCEED-WITH-AMENDMENTS, sequenced textarea-first** | 2026-08-28 | The rich-text converter is built IN-HOUSE as `src/main/rich-decompose.ts` (Unit U2, the pure `decomposeRichHtml` module — the `provident-editable@0.1.0` import plan was replaced by the in-house build), and the slice is SEQUENCED: land the plain-text textarea editing UI (Unit D §5.6, deferred per Unit D §3a H5) FIRST, then the rich-text contenteditable machinery. Resolved design: (1) `span` is NOT added to `RagNodeType` — it is a diff-matching artifact folded into the parent's `content`; inline `strong`/`em`/`a`/`img` are held by a NEW `children` field on `RagNode` (not separate RAG nodes — preserves one-chunk-per-subtree); (2) `setProps` MERGES (preserves the `data-doc-head` marker), plus new `setSubtree` (updates `children`) and `setType` (type changes, never delete+create) ops; (3) plain-text is the DEFAULT for all nodes in v1; rich-text is opt-in per-node-type (leaf-no-inline-children → rich-text; rich subtrees → textarea); no global `editingMode` field. Must-fix before the rich-text spec: batch atomicity (a real transaction, not `store.enqueue`), retrieval indexing of inline `children` text, traversal disambiguation of inline vs doc-children, store-format `children` additive + hash-source, paste-time sanitization, census 6→9. **Batch atomicity MET 2026-08-28 (Unit N)** — the `applyBatch`
  transaction primitive (a real transaction, not `store.enqueue`) has landed on
  the `RagStore` interface (see BATCH-ATOMICITY-API). **Textarea-first
  prerequisite MET 2026-08-28 (Unit L)** — the plain-text textarea editing UI has landed. **Store-format `children` additive + hash-source MET 2026-08-28 (Unit M)** — the `children` field on `RagNode` + the `RagNodeChild`/`RagNodeChildType` types + the `nodeSource` hash coverage + the additive load + the write-time validation have landed (see CHILDREN-ADDITIVE-STORE-FORMAT / CHILDREN-HASH-SOURCE). **Census 6→9 MET 2026-08-28 (Unit O)** — the edit-op count goes from 6 to 9 (the three rich-text ops `setProps`/`setSubtree`/`setType` land in `src/main/edit-ops.ts`; see RICH-TEXT-EDIT-OPS). **Retrieval indexing of inline `children` text MET 2026-08-28 (Unit Q)** — the retrieval module indexes + renders the inline `children` text via the new `nodeText(node)` helper (see NODETEXT-SPACE-JOIN / RENDER-DIRECT-CONCAT). **Traversal disambiguation of inline vs doc-children MET 2026-08-28 (Unit R)** — the traversal renders the inline `children` as child elements of the subtree root, disambiguated from doc-children by the `rag-` id prefix (see INLINE-CHILDREN-AUTHORED-ID). **Paste-time sanitization MET 2026-08-28 (Unit S)** — the pure `sanitizePastedHtml` module normalizes pasted HTML into the `RagNodeChild[]` shape (see PASTE-SANITIZATION). **All RICH-TEXT-EDITING-GATE must-fix items are now MET (2026-08-28, Units N/O/P/Q/R/S).** | proposal gate 2026-08-28 (validity VALID-WITH-AMENDMENTS, critique UNSOUND-as-written, architecture SOUND-WITH-AMENDMENTS, change-analysis PROCEED-WITH-AMENDMENTS) |
| **DECIDED: TEXTAREA-PROVIDENT-AUTHORING — the textarea is authored as provident-ssr data in the traversal (a `textarea` child of each RAG subtree root), NOT hand-written HTML/DOM** | 2026-08-28 | The textarea is authored as provident-ssr data in `buildSubtree` (`src/main/traversal.ts`) — a `textarea` child of each RAG subtree root, bound to the RAG node's content via the back-reference map. NOT hand-written HTML/DOM. A textarea rendered outside the provident graph is a review finding (the project-wide all-UI-via-provident constraint, A1). | `docs/specs/unit-l-textarea-editing-ui.md` §4/§5.1 |
| **DECIDED: TEXTAREA-BRIDGE-SURFACE — the textarea's `onInput`/`onBlur` handlers reach the edit controller through the `window.provident.sidebar` bridge surface** | 2026-08-28 | The textarea's `onInput`/`onBlur` handlers reach the edit controller through the `window.provident.sidebar` bridge surface (the Unit K §5.3 M2 pattern), extended with `textareaInput`/`textareaBlur`. A compiled function-string handler body cannot call `markDirty`/`commit` directly (they are host methods, not reachable from a `new Function` body). The handlers NEVER call an MCP tool. | `docs/specs/unit-l-textarea-editing-ui.md` §4/§5.2 |
| **DECIDED: TEXTAREA-READONLY-HOST-SET — the textarea's `readOnly` prop is set by the host at render time from `editController.isEditable(ragId)`** | 2026-08-28 | The `readOnly` prop is set by the host at render time from `editController.isEditable(ragId)` (the traversal is pure and cannot see the edit controller). The traversal emits NO `readOnly` prop (editable by default — emitting `readOnly: false` would render as the `readonly` boolean attribute and make the textarea uneditable, adversarial H1); the host sets `readOnly: true` only when `!isEditable(ragId)` and omits it otherwise (idempotent across re-assembles, H4). | `docs/specs/unit-l-textarea-editing-ui.md` §4/§5.3 |
| **DECIDED: NAME-REFERENCED-HANDLER-RESOLUTION — the app Runtime resolves name-referenced handler bodies (registered via `registerHandlerDef`) onto the node so the engine's `dispatchEvent` can fire them** | 2026-08-28 | The engine's `translateLegacy` only compiles INLINE handler bodies; a name-referenced handler (`{ name, event }`, body registered via `registerHandlerDef`) keeps `body` undefined and is skipped by dispatch. The app Runtime's `loadEnvelope`/`loadDoc` now resolves each name-referenced handler's body from the registry onto the node (`resolveNameReferencedHandlerBodies`), so the engine's `dispatchEvent` can fire it. This is the Conflict B resolution — it also fixes the pre-existing template-handler dispatch gap. The textarea's handlers are name-referenced, so this makes the textarea MCP-dispatchable (MCP/UI equivalence). | `docs/specs/unit-l-textarea-editing-ui.md` §5.8 item 12 |
| **DECIDED: TEXTAREA-RENDER-ONLY-OVERLAY — the textarea is a RENDER-ONLY editing overlay present in the DOM render view, NOT in the markdown** | 2026-08-28 | The subtree root's `content` is KEPT in the envelope (Conflict C resolution) so the markdown/line→node map (HOST-C1) still renders the text; the textarea is a RENDER-ONLY editing overlay present in the DOM render view, NOT in the markdown (the `MarkdownAdapter` does not render a `textarea`). The textarea's `value` mirrors the root's `content`. The textarea's authored id is `textarea-<ragId>` (NOT `rag-`-prefixed — `collectSubtreeIds` treats a `rag-`-prefixed child as a doc-child subtree root and would exclude the textarea from the backRefs map). | `docs/specs/unit-l-textarea-editing-ui.md` §5.1 |
| **DECIDED: CHILDREN-ADDITIVE-STORE-FORMAT — the `children` field on `RagNode` is an ADDITIVE, backward-compatible store-format change** | 2026-08-28 | The `children?: RagNodeChild[]` field on `RagNode` (the closed `RagNodeChildType` union `strong`/`em`/`a`/`img`; `span` NOT a member and NOT added to `RagNodeType`) is OPTIONAL and ADDITIVE — existing records without `children` still load and hash-verify (a missing `children` serializes identically to `children: undefined`), no migration/re-hash. `children` is deep-copied on BOTH write and read, validated at write (throw) and skipped at boot, and NOT part of `ownedNodeIds` (the inline children are held on the owning node, not separate subtree roots). | `docs/specs/unit-m-children-field.md` §4/§5.1/§5.3 |
| **DECIDED: CHILDREN-HASH-SOURCE — `nodeSource` includes `children` in the fixed field order (after `content`, before `props`), so the SHA-256 hash covers the inline children** | 2026-08-28 | `nodeSource` serializes `children` in the fixed field order (`id, type, content, children, props, ownedNodeIds, createdAt, updatedAt`), so a `children` change → a new hash; a node whose `children` was tampered (changed without a hash update) is QUARANTINED at boot (the hash-verified source discipline). A node WITHOUT `children` hashes identically to a node with `children: undefined` — the ADDITIVE guarantee. | `docs/specs/unit-m-children-field.md` §4/§5.2 |
| **DECIDED: BATCH-ATOMICITY-API — a new `applyBatch(ops)` method on the `RagStore` interface applies a batch of edit operations atomically (a real transaction, not `store.enqueue`)** | 2026-08-28 | The `applyBatch(ops: BatchOp[]): Promise<BatchResult>` method on the `RagStore` interface (`src/main/rag-store.ts`) applies a batch of edit operations ATOMICALLY — all or nothing, with snapshot/restore rollback on failure. The `BatchOp` union is CLOSED at 7 members (the 4 store primitives `putNode`/`removeNode`/`putEdge`/`removeEdge`, applied by Unit N, + the 3 forward-looking rich-text ops `setProps`/`setSubtree`/`setType`, applied by Unit O — a batch containing one is a fail-state in Unit N). A successful batch lands as a SINGLE invertible `batch` journal entry (undo/redo restores the whole batch as a unit) and persists ONCE; a failed batch rolls back the in-memory state to the pre-batch snapshot, does NOT pollute the journal, and does NOT persist. Serialized through the single-writer queue; re-entrant (the `inQueue` pattern, no deadlock). `applyBatch` NEVER throws for a domain failure — it returns the discriminated `BatchResult` (`{ ok: true, results }` / `{ ok: false, error, failedIndex }`). **The RICH-TEXT-EDITING-GATE must-fix "batch atomicity (a real transaction, not `store.enqueue`)" is now MET (2026-08-28, Unit N).** | `docs/specs/unit-n-batch-atomicity.md` §4/§5.1/§5.2/§5.3/§5.4/§5.5/§5.6 |
| **DECIDED: RICH-TEXT-EDIT-OPS — the three rich-text edit ops `setProps`/`setSubtree`/`setType` land on the edit-ops layer, landing the edit-op census 6→9** | 2026-08-28 | `setProps` MERGES props onto a node (only the named keys update; the existing props including the `data-doc-head` marker are preserved — the `setProps` edit op the user chose, Option A); `setSubtree` replaces a node's inline `children` (the Unit M `RagNodeChild[]` field) with a new array (a FULL replace, no merge/append); `setType` changes a node's `type` NEVER delete+create (id/content/children/props/ownedNodeIds preserved; only `type` changes). Each op is a single atomic edit (a single `putNode` write, or a single-op `applyBatch` from Unit N), returns the discriminated `SetPropsResult`/`SetSubtreeResult`/`SetTypeResult`, and NEVER throws for a domain failure. The edit-op census goes from 6 to 9 — the RICH-TEXT-EDITING-GATE "census 6→9" must-fix is now MET. | `docs/specs/unit-o-edit-ops.md` §4/§5.1/§5.2/§5.3/§5.4/§5.9 |
| **DECIDED: IPC-EDIT-BATCH — the `IPC_EDIT_BATCH` channel is the renderer→main batch channel for the rich-text editing machinery, applied atomically via `applyBatch`** | 2026-08-28 | The `IPC_EDIT_BATCH = 'provident:edit-batch'` channel (a renderer→main `ipcRenderer.invoke` request/reply channel, NOT a fire-and-forget `send`) carries a batch of `BatchOp` values (`EditBatchPayload { ops: BatchOp[] }`) to the RAG store, applied atomically via the `applyBatch` transaction primitive (Unit N) — all or nothing, one `batch` journal entry, one persist. The `handleEditBatch` shared handler (in `src/main/edit-ops.ts`) validates the payload and calls `ragStore.applyBatch`; the `bridge.edit.batch(ops): Promise<BatchResult>` preload method is the renderer-side invocation; the `deriveBatchBroadcast` pure helper derives the `rag-store-changed` payload (`{ kind, nodeIds, edgeIds }`) deterministically from a successful batch. A successful non-empty batch broadcasts `rag-store-changed` EXACTLY ONCE (A4); a failed batch broadcasts 0 times (A2). The channel is MCP/UI-equivalent (§8.2 BINDING) — the same batch reachable via the MCP `edit.batch` tool (forward-looking wiring) and the UI IPC, both routing through the same `applyBatch` primitive. The channel is NOT group-gated (the renderer is a trusted surface) and NOT added to the `RpcMethod` union. | `docs/specs/unit-p-ipc-edit-batch.md` §4/§5.1/§5.2/§5.3/§5.4/§5.5 |

| **DECIDED: NODETEXT-SPACE-JOIN — the retrieval `nodeText(node)` helper returns a node's FULL searchable text as content + every inline child's content, space-joined after dropping empty strings** | 2026-08-28 | The new exported `nodeText(node)` helper in `src/main/retrieval.ts` returns a node's FULL searchable text: the node's `content` + every inline child's `content`, space-joined after dropping empty strings. The three index builders (`createLexicalIndex`/`updateLexicalIndex`/`addToLexicalIndex`) tokenize `nodeText(node)` instead of `node.content`, so the inline `children` text (Unit M) is indexed for retrieval. | `docs/specs/unit-q-retrieval-children-indexing.md` §4/§5.1 |
| **DECIDED: RENDER-DIRECT-CONCAT — the retrieval renderer (`renderNode`/`renderInlineText`) renders content + inline children directly (strong → `**…**`, em → `*…*`, a → `[…](href)`, img → `![alt](src)`)** | 2026-08-28 | The retrieval renderer (`renderNode`/`renderInlineText` in `src/main/retrieval.ts`) renders a node's content + its inline children directly into the searchable text: `strong` → `**…**`, `em` → `*…*`, `a` → `[…](href)`, `img` → `![alt](src)`. Empty-content children are skipped (adversarial F1 — no `****`/`[]()` markers); a non-string `href`/`src` is coerced to string (adversarial F2). | `docs/specs/unit-q-retrieval-children-indexing.md` §4/§5.2 |
| **DECIDED: INLINE-CHILDREN-AUTHORED-ID — the traversal authors each inline child with the id `inline-<ragId>-<index>` (NOT `rag-`-prefixed), distinct from the textarea's `textarea-<ragId>`** | 2026-08-28 | `buildSubtree` (`src/main/traversal.ts`) renders each inline child as a same-type `LegacyNodeData` element (strong/em/a/img) with `content` + merged `props`, authored id `inline-<ragId>-<index>` — NOT `rag-`-prefixed (so `collectSubtreeIds` does NOT treat it as a doc-child subtree root) and distinct from the textarea's `textarea-<ragId>`. Inline children get NO `rag-` id, are NOT in `materialized`, get NO backRefs entry, get NO lineMap range; doc-children ARE separate RAG subtree roots. Ordering: [inline children, textarea overlay, doc-children]. | `docs/specs/unit-r-traversal-inline-children.md` §4/§5.1 |
| **DECIDED: PASTE-SANITIZATION — pasted HTML is sanitized by the pure `sanitizePastedHtml` module into the `RagNodeChild[]` shape, never trusted raw** | 2026-08-28 | A new PURE, node-testable module `src/main/paste-sanitize.ts` exports `sanitizePastedHtml(rawHtml: string): SanitizePasteResult` — a deterministic, TOTAL (never throws for a string input) sanitizer that removes dangerous content and normalizes the surviving content into the `RagNodeChild[]` shape. The discriminated return `{ ok: true; html; content; children } | { ok: false; error }`; the ONLY fail-state is a non-string input. Removes 79 disallowed elements + the `fe*` wildcard + `a`-in-SVG-context; strips `on*`/dangerous-key attributes; validates URLs (http(s), relative, raster-only `data:image/*` for `img`); demotes unsafe/missing-`href` `a` to text, drops unsafe/missing-`src` `img`; folds `span` into the parent's content; hoists nested inline elements to siblings. | `docs/specs/unit-s-paste-sanitization.md` §4/§5.1 |
| **DECIDED: ONE-WAY-SNAPSHOT — the RAG store is a point-in-time copy of the imported markdown corpus; edits go to the RAG store, never back to the source** | 2026-08-28 | The markdown file import (Unit T) makes the RAG store a ONE-WAY SNAPSHOT of the imported corpus (incl. the project's own `docs/*.md` tree). The source markdown files are read-only inputs; the importer performs NO write to them (A4). A subsequent change to a source file does NOT propagate to the RAG store (one-shot, no re-import). This pins the two-source-of-truth externality (amendment 1) as a one-way snapshot — the RAG store is authoritative (RAG-AUTHORITATIVE); the imported markdown is a point-in-time source, not a live feed. | `docs/specs/unit-t-markdown-import.md` §4/§5.4, `docs/specs/markdown-import-review.md` §4 amendment 1 |
| **DECIDED: MARKDOWN-EXPORT-ONLY-CARVE-OUT — initial ingestion is a DISTINCT sanctioned flow that creates RAG nodes/edges from a new corpus, NOT a markdown round-trip edit surface** | 2026-08-28 | Markdown remains EXPORT-ONLY for round-trip edits (MARKDOWN-EXPORT-ONLY). Initial ingestion (Unit T) is a DISTINCT sanctioned flow: it creates RAG nodes/edges from a new corpus and routes through the single-writer store + validation + journal. It does NOT treat the MarkdownAdapter export as an edit surface; the line→node map is a READ/assembly aid, explicitly NOT a write-back path. This is an ADDITIVE refinement of MARKDOWN-EXPORT-ONLY (not a supersession). | `docs/specs/unit-t-markdown-import.md` §4, `docs/specs/markdown-import-review.md` §4 amendment 10 |
| **DECIDED: TABLE-TYPES-ADDITIVE-STORE-FORMAT — `table`/`thead`/`tr`/`td`/`th` are added to `RagNodeType` (18→23) as an additive store-format change** | 2026-08-28 | The markdown file import (Unit T) resolves the table gap IN-SLICE: `table`/`thead`/`tr`/`td`/`th` are added to the `RagNodeType` union (18 → 23 members) and to the store's `RAG_NODE_TYPES` runtime set. The 18 existing members are UNCHANGED; the 5 new members are added. Existing records without these types still load (additive — no migration/re-hash). A GFM pipe table maps to a `table` node; the header row → a `thead` doc-child; each body row → a `tr` doc-child; each cell → a `td`/`th` doc-child. | `docs/specs/unit-t-markdown-import.md` §4/§5.2/§5.8, `docs/specs/markdown-import-review.md` §4 amendment 6 |
| **DECIDED: EDITING-MODE-SETTING — `editingMode: 'textarea' | 'contenteditable'` is an `OperatorSettings` field (default `'contenteditable'`); contenteditable is the DEFAULT editing control; a mode change broadcasts `operator-settings-changed` → the host uses the payload (authoritative) → fresh re-derive** | 2026-08-28 | `editingMode` is an `OperatorSettings` field (default `'contenteditable'`, decision D) + a `settingsContent` button-toggle (text div + button, operator isolated scope, never MCP-visible). Contenteditable is the DEFAULT editing control when `editingMode === 'contenteditable'` (a rich-eligible RAG subtree root splices to `contenteditable: true`; ineligible roots have their textarea removed in contenteditable mode — they render as plain text); textarea is the opt-in legacy control. A mode change writes the operator-settings store → main broadcasts `operator-settings-changed` with the store's result as the authoritative payload → the host uses the PAYLOAD directly (amendment A — no re-fetch, redundant + an async race with the sync `requestRebuild`) → fresh re-derive (`requestRebuild` → `reDerive`, never `refresh()` over the cached spliced envelope). Commit-on-blur, the dirty-edit guard, RAG-authoritative re-traversal, and all-UI-via-provident authoring are RETAINED. **NOTE (commit `1af5000` "Render fix"):** the default was flipped from `'textarea'` to `'contenteditable'`, and in contenteditable mode the textarea is removed for ALL rag roots (not just rich-eligible ones) — ineligible roots render as plain text with no textarea, no `contenteditable` prop, and no `rag-editor-*` handlers. **Control-fallback fix (2026-08-29):** the `settingsContent` button-toggle's `?? 'textarea'` fallback (the `lastOperatorSettings === null` boot-get-failure edge) was changed to `?? 'contenteditable'` so the control never advertises a mode that contradicts the live default; pinned by the extended F1 boot-failure test in `tests/editing-mode-broadcast-host.test.ts`. **Supersedes** FORM-CONTROL-EDITING's "NOT contenteditable (fights graph-is-authoritative; `DomAdapter.text` clobbers a live editor)" clause (line 17) — contenteditable is now the default editing control (the commit-on-blur, dirty-edit guard, and caret/focus-host-state pins remain) — **and** RICH-TEXT-EDITING-GATE's "no global `editingMode` field" clause (line 51) — a global `editingMode` operator setting now exists. **Also supersedes the Unit L `CaretState = { offset; focused }` shape (Unit L §5.4 — a SUPERSEDED decision note, 2026-08-28):** the Unit U4 discriminated `CaretState` (`{kind:'textarea'; offset; focused}` | `{kind:'rich'; ragId; anchor; focus; focused}` with the path-based `RichCaretEdge`) replaces the textarea-only `{ offset; focused }` caret state; the textarea path (`textareaBlur`) now writes `{ kind: 'textarea', offset, focused: dirty }` and the host's re-derive caret-restore loop is kind-GATED (rich caret → contenteditable root, textarea caret → textarea, mismatches DROPPED — amendment 4 / U3 F2 / ADR-8). | `docs/specs/editing-mode-toggle-review.md` §4-D + §3 amendments 1/2/4; `docs/specs/unit-u1-editing-mode-setting.md`; `docs/specs/unit-u4-contenteditable-editor.md` §1.2 |
| **DECIDED: TEMPLATE-SAVE-BUTTON-REMOVED — the dead Save button is removed from the template-editor pane (M15 refinement)** | 2026-08-28 | The template is AUTO-COMMITTED via the create/delete/reset IPC — there is no separate save IPC and no `editedTemplate` to commit. The template-editor pane's Save button was a dead control (its `template-save` handler was never registered — M15). The Save button is REMOVED from the pane (Unit I): the pane now authors only the zone list + `template-zone-input` + Add-zone + Reset. `template-save` remains NOT registered (the app-graph handler-def census stays 5). This is an ADDITIVE refinement of M15 (the handler was already dropped; now the dead control is gone too). | `docs/specs/unit-i-template.md` §5.4, `docs/specs/unit-k-sidebar-panes-host.md` M15 |
| **DECIDED: SCOPED-LOAD — the document list only finds the document heads; document rendering only walks the graph reachable from the head via the document links (PROPOSAL-GATE, PROCEED-WITH-AMENDMENTS, IMPLEMENTED — all 3 units)** | 2026-08-29 | Fixes the load bug "the app parses the entire graph and times out". The doc-nav derives documents from a lighter `rag-doc-heads` IPC (`{ documents: [{ documentId, title }] }` from the `doc-head` edges + head node content) instead of the full snapshot. `buildTraversal` walks only the reachable subgraph from the head via the document links (`doc-head` → `next-section` → `doc-end` + `doc-child` + `parent-child` for multi-parent) using new `RagStore` adjacency methods (`edgesFrom`/`edgesTo`/`edgesByKind`/`edgesForDocument`/`docHeadForDocument`) + a shared `createSnapshotStore` read-only adapter + a shared `computeDocumentSubgraph` helper (also refactors `rag.get_document`). The scoped walk shrinks `materialized` → `backRefs`/`crosslinks` drop nodes not reachable from the head (an ACCEPTED, regression-tested behavior change). The existing `RagSnapshotPayload` is preserved for `buildTraversal`. **Unit 1 (store adjacency) IMPLEMENTED (2026-08-29)** — the five `RagStore` adjacency methods (`edgesFrom`/`edgesTo`/`edgesByKind`/`edgesForDocument`/`docHeadForDocument`), the lazy O(E) index + invalidation across the 6 mutation paths, the shared PURE adjacency core (`buildAdjacencyIndex` + the 5 query helpers), the quarantine exclusion, and the read-only `createSnapshotStore(nodes, edges)` adapter have landed (the PURE core + `createSnapshotStore` in `src/main/adjacency.ts`, re-exported by `src/main/rag-store.ts`; spec `docs/specs/unit-v1-store-adjacency.md`). **Unit 2 (scoped traversal + MCP refactor) IMPLEMENTED (2026-08-29)** — the scoped `buildTraversal` walk + the shared `computeDocumentSubgraph` helper + the `rag.get_document` refactor + the `rebuildBackRefs`/`buildTraversalEnvelope` adapter replacements have landed. **Unit 3 (doc-heads doc-nav) IMPLEMENTED (2026-08-29)** — the `rag-doc-heads` IPC (`IPC_RAG_DOC_HEADS` + `RagDocHeadsPayload` + the shared `handleRagDocHeadsIpc(store)` handler + the `main.ts` handler + `bridge.rag.docHeads()`) returns `{ documents: [{ documentId, title }] }`; the doc-nav switched to `PaneContext.docHeads` (`deriveDocNavDocuments`/`docNavContent` read `ctx.docHeads`); the host gained the `lastDocHeads` cache + `selectDocument` validates against the doc-heads list (amendment 5); the `RagSnapshotPayload` is preserved for `buildTraversal` (amendment 9). Spec `docs/specs/unit-v3-doc-heads-docnav.md`; greens `docs/specs/unit-v3-doc-heads-docnav-greens.md` 36/36. **LIVE VERIFICATION (2026-08-29):** the boot now renders ONLY the current document (the localeCompare-first doc-head, matching the doc-nav's first entry) instead of the whole corpus — the full-graph render that timed out. `get_rendered_html` dropped from a 60s+ timeout to ~0.15s on the 63-document corpus. (spec `docs/specs/unit-v2-scoped-traversal-mcp.md`; see the SCOPED-WALK / SINGLE-DOCUMENT-SUBGRAPH / MATERIALIZED-SHRINK rows). | `docs/specs/load-bug-scoped-traversal-review.md` §5/§6/§7 |
| **DECIDED: ADJACENCY-INDEXED — the `RagStore` exposes the five adjacency methods backed by a lazy O(E) index, invalidated on mutation** | 2026-08-29 | The `RagStore` exposes the five adjacency methods (`edgesFrom`/`edgesTo`/`edgesByKind`/`edgesForDocument`/`docHeadForDocument`) backed by a lazy O(E) index, invalidated on mutation. The index is a pure projection of the store's edges — never a separate source of truth. | `docs/specs/unit-v1-store-adjacency.md` §4 |
| **DECIDED: SHARED-ADJACENCY-CORE — the adjacency semantics live in ONE pure implementation (`buildAdjacencyIndex` + the query helpers)** | 2026-08-29 | The adjacency semantics live in ONE pure implementation (`buildAdjacencyIndex` + the 5 query helpers). The JSON store and `createSnapshotStore` BOTH delegate to it — the renderer's traversal cannot diverge from main's (amendment 3). | `docs/specs/unit-v1-store-adjacency.md` §4 |
| **DECIDED: READ-ONLY-SNAPSHOT-ADAPTER — `createSnapshotStore(nodes, edges)` is a read-only `RagStore` adapter** | 2026-08-29 | `createSnapshotStore(nodes, edges)` is a read-only `RagStore` adapter. Its mutating methods throw (fail-closed — a read-only adapter must not silently accept a write). It is the replacement for the two inline `listNodes`/`listEdges`-only adapters (amendment 4). | `docs/specs/unit-v1-store-adjacency.md` §4 |
| **DECIDED: SCOPED-WALK — `buildTraversal` walks ONLY the reachable subgraph from each document's head via the adjacency methods, never a full-edge scan per document** | 2026-08-29 | `buildTraversal` walks ONLY the reachable subgraph from each document's head via the adjacency methods (`edgesForDocument`/`edgesFrom`/`edgesTo`/`docHeadForDocument`) — never a full-edge scan per document. The walk is O(E) index build (Unit V1) + O(adjacency) lookups. | `docs/specs/unit-v2-scoped-traversal-mcp.md` §4/§5.1 |
| **DECIDED: SINGLE-DOCUMENT-SUBGRAPH — `computeDocumentSubgraph(store, documentId)` is the SINGLE shared derivation of a document's node set, used by BOTH the scoped walk AND the `rag.get_document` MCP tool** | 2026-08-29 | `computeDocumentSubgraph(store, documentId)` is the SINGLE shared derivation of a document's node set + its scoped edges, used by BOTH the scoped `buildTraversal` walk AND the `rag.get_document` MCP tool (amendment 2) — neither re-derives the node set inline, so the traversal and the MCP contract cannot diverge. | `docs/specs/unit-v2-scoped-traversal-mcp.md` §4/§5.2 |
| **DECIDED: MATERIALIZED-SHRINK — the scoped walk's `materialized` set is the set of nodes REACHABLE from the head; nodes not reachable are NOT materialized, so `backRefs`/`crosslinks` drop them** | 2026-08-29 | The scoped walk's `materialized` set is the set of nodes REACHABLE from the head (via `doc-head` → `next-section` → `doc-end` + `doc-child` + multi-parent `parent-child`). Nodes not reachable from the head are NOT materialized, so `backRefs`/`crosslinks` drop them. This is a documented, regression-tested behavior change (amendment 1). | `docs/specs/unit-v2-scoped-traversal-mcp.md` §4/§5.4 |
| **DECIDED: DOC-HEADS-IPC — a lighter `rag-doc-heads` IPC returns `{ documents: [{ documentId, title }] }` from the `doc-head` edges + head node content** | 2026-08-29 | A lighter `rag-doc-heads` IPC returns `{ documents: [{ documentId, title }] }` from the `doc-head` edges + head node content. The doc-nav reads this, not the full snapshot. | `docs/specs/unit-v3-doc-heads-docnav.md` §4/§5.1 |
| **DECIDED: DOC-NAV-DOCHEADS — the doc-nav's `PaneContext` carries `docHeads` (the document list) instead of deriving it from `snapshot`** | 2026-08-29 | The doc-nav's `PaneContext` carries `docHeads` (the document list) instead of deriving it from `snapshot`. `deriveDocNavDocuments`/`docNavContent` read `ctx.docHeads`; `selectDocument` validates against the doc-heads list. | `docs/specs/unit-v3-doc-heads-docnav.md` §4/§5.3 |
| **DECIDED: HOST-DOCHEADS-CACHE — the host retains a `lastDocHeads` cache (the doc-heads list) alongside `lastSnapshot`, refreshed on boot/re-derive** | 2026-08-29 | The host retains a `lastDocHeads` cache (the doc-heads list) alongside `lastSnapshot`, refreshed on boot/re-derive. `buildContext()` populates `ctx.docHeads` from it. | `docs/specs/unit-v3-doc-heads-docnav.md` §4/§5.4 |
| **DECIDED: RAG-SNAPSHOT-PRESERVED — the `RagSnapshotPayload` + the `rag-snapshot` IPC are PRESERVED for `buildTraversal` (the rendering half)** | 2026-08-29 | The `RagSnapshotPayload` + the `rag-snapshot` IPC are PRESERVED for `buildTraversal` (the rendering half). The doc-nav no longer consumes the snapshot, but the rendering half still does (amendment 9). | `docs/specs/unit-v3-doc-heads-docnav.md` §4/§5.5 |
| **DECIDED: VECTOR-BOOT-BACKGROUND-PROMOTE — vector-mode boot is BOOT MODEL B: sync fail-fast warm-up gate → window+MCP born-lexical (pending) → background build → reconcile → atomic ONE-WAY embedder promotion inside the ONE shared engine** | 2026-09-05 | Vector-mode boot no longer embeds the corpus synchronously and dark (pre-W1 `main.ts:150` blocked before the window at `:392` / `mcp.start()` at `:406` — pre-W1 line numbers, shifted by the W1 rewiring — ~28 min for ~16,840 non-empty nodes at ~0.1s/embed, ANY single embed failure aborting boot). Boot model B: (1) the config-missing abort is UNCHANGED (`main.ts:153-157` post-W1; pre-W1 `147-149`; spec §5.9 #34 — failure class 1); (2) a sync warm-up gate on ONE REAL `POST /api/embed` (NOT `isOllamaAvailable` — that probes `/api/tags` and does NOT load the model): total failure → throw → the app exits BEFORE the window (failure class 2); (3) the engine is created BORN-LEXICAL (pending) and the window + MCP server come up — `rag.query`/`rag-query` serve LEXICALLY in the SAME `RetrievalResult` shape on BOTH surfaces (the DESIGNED pending state, logged; explicitly NOT a silent lexical fallback; §8.2 equivalence at every instant); (4) the vector index builds in the BACKGROUND; (5) reconcile re-embeds any node whose `updatedAt` strictly postdates its build-time embed (tie `===` → unchanged) BEFORE the swap; (6) ATOMIC ONE-WAY PROMOTION swaps the embedder inside the ONE shared `RetrievalEngine` via the NEW `setEmbedder` seam (`retrieval.ts` closed over the embedder with no setter pre-W1 — the W1 seam LANDED 2026-09-05; in-flight pre-swap queries complete coherently on the old embedder; the `onStoreChanged` hook attaches with the swap, `retrieval.ts:639` post-W1). **Unit W2 LANDED (2026-09-05 — this row's item (4) background build):** the
background build goes through the W2 batch seam — `embedBatch` on BOTH concrete
providers (interface members 5 → 6), `BATCH_CHUNK_SIZE = 64` chunked requests
with per-chunk fallback isolation, and the `Math.min(N × timeoutMs, 2147483647)`
budget clamp (spec §5.2/§5.3; the production `embedBatchFn` wiring itself is W3
per the §5.12 F-W2-3 amendment note). **Unit W3 LANDED (2026-09-05 — the flip + the `skipped` map + the batched production build are now REAL):** the embed-failure policy flip on all four index-maintenance paths (build/add/update/the `onStoreChanged` hook → `skipped.set(nodeId, 'transient')` + the re-pinned `vector index: node embed failed (transient): <nodeId> <error>` warning + the operation RESOLVES; required-arg + query-time rejections UNCHANGED), the `VectorIndex` 4-member shape (`skipped: Map<nodeId, 'empty' | 'transient'>`, members 3 → 4), `removeFromVectorIndex` clearing any `skipped` entry (the F1 unconditional hook-delete), the F3 dimension-0 latch on the maintenance paths, the F2 taxonomy (provider-channel rejections transient-classified; index-level RESOLVED-vector checks stay HARD), and the production wiring `embedBatchFn: provider.embedBatch` at controller creation (`main.ts:163`) — the background build routes through `createVectorIndex(nodes, provider.embed, opts.embedBatchFn)` (`src/main/vector-boot.ts`). 21 failure-policy tests + 5 re-pin sites; §3a W3 registrations F1–F7; doc review `archive/reviews/2026-09-05-unit-f-w3-doc-review.md`. **Unit W1 LANDED (2026-09-05):** `src/main/vector-boot.ts` + `RetrievalEngine.setEmbedder` + `VectorEmbedderOptions.index?` + the rewired `main.ts` vector branch — 33/33 + 5/5 tests green, blind-greens 28 PASS / 3 PENDING-W2 / 1 DEFERRED-STATIC, doc review `archive/reviews/2026-09-05-unit-f-w1-doc-review.md` (spec `docs/specs/unit-f-embeddings.md` §5.12, amended 2026-09-05). **Unit W4 LANDED (2026-09-05 — the cache contract is now REAL):** write-through COALESCED persistence (the exported `CACHE_WRITE_DEBOUNCE_MS = 500` single-writer queue, atomic `0o600 <path>.tmp`+rename writes, lstat regular-file guards at BOTH the cache + tmp paths, the sanitized load-failure log, non-fatal write failures), hash+tuple invalidation (the memoizing wrapper — a HIT adopts with NO HTTP call; the `cacheHits` census deduped by contentHash), and promotion pruning (drain → prune → drain before the report); the controller-creation call cited above at `main.ts:163` is now at `main.ts:168` — `createVectorBootController(ragStore, provider, { embedBatchFn: provider.embedBatch, cache: createVectorCache() })`; pinned by `tests/vector-cache.test.ts` (35 tests: 27 + the 8 RCA-3 adversarial regressions R1–R8); doc review `archive/reviews/2026-09-05-unit-f-w4-doc-review.md`. | `docs/specs/ollama-vector-boot-review.md` §2 amendments A1–A4 + §4 verdict + user go-ahead 2026-09-05 |
| **DECIDED: VECTOR-CACHE-CONTENT-HASH-KEY — the persisted embedding cache: vectors persist in userData keyed by (provider kind, model, dimension, content-hash); embed ONLY on a cache miss; hits adopt with no HTTP call** | 2026-09-05 | The user-approved "vectorize only if the vector is not found" model — the cache-inclusive variant of the vector-boot proposal (the review's parked alternative PROMOTED to IN-SCOPE core contract, superseding the `docs/pending.md` parked row). File `provident-vector-cache.json` in userData (the module-store idiom: atomic temp+rename, one main-process writer). Key = (provider kind, model, dimension, contentHash) where contentHash = lowercase-hex SHA-256 of the exact embedded text (the `nodeSource` SHA-256 discipline). Invalidation = hash mismatch (content changed) OR key mismatch (provider/model/dimension changed) → re-embed. Boot loads the cache (corrupt/unreadable → treated as EMPTY, never crashes boot); the background phase embeds ONLY misses; hits adopt with NO HTTP call; write-through after each successful embed (single-writer queued, atomic temp+rename, not awaited by the embed loop; write failure NON-FATAL); pruning at promotion (drop other-tuple + no-longer-in-store entries). Unit W4 — LANDED 2026-09-05 (spec `docs/specs/unit-f-embeddings.md` §5.13, amended 2026-09-05). **Tests that pin it (ALL LANDED 2026-09-05):** W1 → `tests/vector-boot.test.ts`; W4 → `tests/vector-cache.test.ts` (names SpecWriter-pinned 2026-09-05; W2 → `tests/embeddings-batch.test.ts` —
LANDED 2026-09-05, 25/25 green incl. `BATCH_CHUNK_SIZE = 64` + the 2147483647
budget clamp; W3 → `tests/embeddings-failure-policy.test.ts` + the `tests/embeddings.test.ts:380-386` re-pin — LANDED 2026-09-05 (21 failure-policy tests / 5 re-pin sites; the W3 flip + the skipped map are real); W4 → **LANDED 2026-09-05 — the cache contract now real: write-through coalesced persistence (the exported `CACHE_WRITE_DEBOUNCE_MS = 500` single-writer queue, atomic `0o600 <path>.tmp`+rename writes, lstat regular-file guards, the sanitized load-failure log, non-fatal failures), hash+tuple invalidation (the memoizing wrapper; the `cacheHits` census deduped by contentHash), promotion pruning (drain → prune → drain)** — `tests/vector-cache.test.ts` 35 tests (27 + the 8 RCA-3 adversarial regressions R1–R8); doc review `archive/reviews/2026-09-05-unit-f-w4-doc-review.md`). **Unit W5
LANDED (2026-09-05 — the user-directed follow-up "live upload should also
update the cache"):** the promoted embedder's live embeds route through the
SAME cache via `createVectorEmbedder`'s `cache?` seam (§5.5 amendment) —
MAINTENANCE (node-content) embeds WRITE THROUGH (`embed(text, { persist:
true })` on the cache's debounced single-writer queue); QUERY-embed misses
are adopted IN-MEMORY ONLY (session-scoped, never persisted — the disk cache
holds NODE-CONTENT vectors only; the F1/F2 Architect ruling, §5.13's W5
pass-2 amendment); the promote step passes the SAME cache instance the build
used. W5 → `tests/live-embed-cache.test.ts` (15 tests: L1–L7 + R1–R5); doc
review `archive/reviews/2026-09-05-unit-f-w5-doc-review.md`). | `docs/specs/ollama-vector-boot-review.md` §4a (user go-ahead, cache-inclusive variant, 2026-09-05) |
| **DECIDED: MULTI-STORE-REGISTRY — a server-side `provident-rag-stores.json` registry (NOT OperatorSettings) owned by a new pure module `src/main/rag-store-registry.ts`; the implicit `{ name: 'main', default: true }` entry is SYNTHESIZED AT BOOT mapping to the legacy `provident-rag.json` (zero-config byte-equal)** | 2026-09-05 | Home: a NEW server-side file `provident-rag-stores.json` in userData, module-store idiom, sibling of `security-store.ts`/`operator-settings-store.ts` — NOT an `OperatorSettings` field (that store's corrupt→defaults fallback `operator-settings-store.ts:53-63` would silently drop the registry). Shape `{ version: 1, stores: RagStoreConfig[] }`, `RagStoreConfig = { name, default?, persistenceFile?, corpusRoot?, embedder? }`; name charset `/^[a-z0-9][a-z0-9_-]{0,63}$/`. Validation (pure, node-testable): unique non-empty names, exactly one `default: true`, no two stores resolving to the same persistence file (derived ∪ explicit), absolute `corpusRoot`/`persistenceFile` when present, `embedder` REJECTED in Phase 1 (fail-loud "per-store embedder config is not supported yet"). Boot split: corrupt/unreadable registry file ⇒ fail-soft (implicit main entry + pinned log); valid-JSON-but-invalid-registry ⇒ fail-LOUD abort before the window. MIGRATION-LEGACY-PATH: the implicit entry is synthesized, never written back; name `main` without `persistenceFile` derives the legacy `provident-rag.json` (not `provident-rag-main.json`) so the first registry write preserves all existing data. Registry mutation is BOOT-TIME-ONLY (D8 — edit the JSON + restart; a mid-run registry change is unobserved until restart, pinned by test). **LANDED (2026-09-05, Unit U-MS2 — spec `docs/specs/unit-ms2-store-wiring.md`, greens 43/0/3, `tests/unit-ms2-store-wiring.test.ts` 62 pass / 1 staged red):** the landed wiring consumes this registry (`main.ts` loads it once at boot; `buildRagStoreDirectory` constructs the N stores + N engines), and validation now ALSO rejects the REGISTRY-FILE self-collision — F14 (F-MS2-1: an entry, derived or explicit, whose persistence path resolves onto the registry file itself fails loud; tests 58–59). **The spec §4 sub-pins recorded WITHIN this row (2026-09-05 doc-review reconciliation — NOT four separate decision rows):** REGISTRY-BOOT-SPLIT (the boot split above), REGISTRY-DERIVED-PATHS (the derived paths + the name-`main` carve-out above), REGISTRY-NO-WRITE (the never-written-back synthesis + boot-time-only mutation + the module's structurally write-free import set), REGISTRY-CWD-TRANSPARENCY (an omitted `corpusRoot` stays OMITTED on the resolved store — the module NEVER fills `process.cwd()` in; the importer's cwd default applies at import time). **LANDED 2026-09-05, Unit U-MS1** — spec `docs/specs/unit-ms1-store-registry.md`, blind greens 35/35 (`docs/specs/unit-ms1-store-registry-greens.md`, the F-BLIND-MS1-1 BigInt expectation reconciled), `tests/unit-ms1-store-registry.test.ts` 63 tests (63/63 green; the §3a adversarial fix batch = tests 47–63; doc-review pass 2026-09-05). | `docs/specs/multi-document-store-config-review.md` §2 D2/D3/D8 (four-agent gate, user go-ahead 2026-09-05) |
| **DECIDED: SINGLE-WRITER-STORE-PER-STORE — the SINGLE-WRITER-STORE lock-point property is PER-STORE; each `createJsonRagStore` instance keeps its own single-writer queue; no cross-store write transactions** | 2026-08-26 (refined 2026-09-05 for the multi-store slice) | Refinement of SINGLE-WRITER-STORE (`docs/decisions.md` line 21 — "THE store is the lock point"): with N stores, EACH `createJsonRagStore` instance serializes its own writes through its own closure queue (`rag-store.ts:661-680`); the main process owns all writes to every store; MCP and UI both route through the addressed store. Per-store single-writer is sufficient for the Phase-1 read-only-per-store query surface; NO cross-store write transactions exist (`applyBatch` is per-store — BATCH-ATOMICITY-API), and scratch-store promotion (a cross-store write) is out of scope until an atomicity story exists. **LANDED (2026-09-05, Unit U-MS2 — spec `docs/specs/unit-ms2-store-wiring.md`, greens 43/0/3, `tests/unit-ms2-store-wiring.test.ts` 62 pass / 1 staged red):** the landed directory construction builds the N DISTINCT instances (`buildRagStoreDirectory` rule 1 — each `createJsonRagStore` keeps its own closure queue); greens S08 verified `main.store !== other.store` + cross-store write isolation. | `docs/specs/multi-document-store-config-review.md` §2 D12 (the refinement the feature request itself asked to be recorded) |
| **DECIDED: ENGINE-PER-STORE — the retrieval ENGINE is created ONCE PER STORE at boot (a `Map<name, RetrievalEngine>`), maintained incrementally per store; the Unit E F1 "created ONCE in main" letter is refined per store** | 2026-08-27 (refined 2026-09-05 for the multi-store slice) | Refinement of Unit E F1 (`docs/specs/unit-e-rag-index.md:93-109`) + §5.12's "ONE shared RetrievalEngine" letter: with N stores there are N `createRetrieval` engines, ONE per loaded store, created ONCE at boot and maintained incrementally per store (the F1 no-per-call-rebuild property holds per store). `edit.*` success and the IPC edit paths resolve the ADDRESSED store's engine (today's closures bind one engine: `mcp-server.ts:1131`, `main.ts:246,283,307`). Phase-1 non-default stores are LEXICAL-ONLY (A6 — only the default store takes the `embedderKind==='vector'` branch, `main.ts:154-170`, keeping the §5.12 failure-class-2 warm-up abort unreachable for non-default stores). **LANDED (2026-09-05, Unit U-MS2 — spec `docs/specs/unit-ms2-store-wiring.md`, greens 43/0/3, `tests/unit-ms2-store-wiring.test.ts` 62 pass / 1 staged red):** the per-store engine map is REALIZED as the directory's per-name entries (`RagStoreDirectory.entries` — N engines created ONCE at boot by `buildRagStoreDirectory`); `edit.*` reconciles the ADDRESSED store's engine PER CALL (`ragStores.entries.get(storeName)?.engine`); non-defaults are lexical-only (A6/R10). | `docs/specs/multi-document-store-config-review.md` §2 D5 (four-agent gate, user go-ahead 2026-09-05) |
| **DECIDED: STORE-QUALIFIED-BROADCAST — `RagStoreChangedPayload` gains a REQUIRED `store: string`; all four emission sites qualify it; the host DROPS foreign-store broadcasts before the renderer subscription; `rag-snapshot` gains `store`** | 2026-09-05 | The `rag-store-changed` broadcast payload (`shared/types.ts:359-363`) gains a REQUIRED `store: string`; all FOUR emission sites (`main.ts:249,286,308` — the UI commit/batch/rich paths — + `mcp-server.ts:1134` — the MCP edit path) qualify it in the same unit (A3). The payload type is declared structurally in THREE places (`preload.ts:22-26`, `mcp-server.ts:349`, `sidebar-panes.ts:57`) — collapse to ONE shared declaration or pin in sync by test. `IPC_RAG_SNAPSHOT`'s `RagSnapshotPayload` gains `store` (`main.ts:377-380`). The host records its boot store from the snapshot and the re-derive guard DROPS foreign-store broadcasts — a foreign-store ingest must NOT trigger a re-derive storm of the unchanged default graph (closes the critique's B5). **LANDED (2026-09-05, Unit U-MS3 — spec `docs/specs/unit-ms3-store-qualified-broadcast.md`, greens 23/0/4-relegated, `tests/unit-ms3-store-qualified-broadcast.test.ts` 23/23):** the ONE shared `RagStoreChangedPayload` (`shared/types.ts:375-381`) with REQUIRED `store`; the three structural copies DELETED (preload.ts + mcp-server.ts keep compat re-exports); the four emission sites stamp `store` (main.ts sites 1–3 at `:273,:311,:335` + the snapshot handler `:412` — all `plan.defaultName`; the MCP path via `handleEditTool`'s seven construction points `emit`ing `store: storeName`); the host `lastStore` capture + the guard (the malformed/`null` fail-closed `warn` + the silent foreign drop) in `sidebar-panes.ts`; the two derive helpers narrowed to `Omit<…,'store'>` (edit-ops.ts `:537,:651`). | `docs/specs/multi-document-store-config-review.md` §2 D5 (four-agent gate, user go-ahead 2026-09-05) |
| **DECIDED: STORE-ID-PREFIX — non-default-store imports mint `<name>:`-prefixed documentIds at the import minting seam; the default store's output is byte-equal to today; the prefix namespace is reserved (A1)** | 2026-09-05 | `importMarkdownCorpus` prefixes the documentId passed to `parseMarkdown` with `<name>:` for NON-default stores (`markdown-import.ts:249-286`); the default store stays unprefixed (byte-equal). Safe because `sanitizeDocumentId` strips `:` (`markdown-import.ts:69-76`) and the registry name charset excludes it — no default-store documentId can contain `:`. A1 (binding): the prefix namespace is reserved at the NODE-id level too — a default-store document whose sanitized basename EQUALS a registered non-default store name mints prefix-shaped node ids (`markdown-parse.ts:444,565` — `` `${documentId}:${type}:${n}` ``); the spec pins one of (a) the default store's import seam rejects such a documentId, or (b) id-based resolution derives the store from a recognized prefix and fails loud on ambiguity. A foreign-store id passed to an id-based tool without `store` MISSES in the default store (the existing `node not found` fail-state) instead of silently mutating a same-named node (closes the critique's B3 mutation hazard). `edit.create_node` mints `n-${randomUUID()}` — already globally unique. **LANDED + A1-RESOLUTION-(a) (2026-09-05, Unit U-MS4 — spec `docs/specs/unit-ms4-id-prefixing.md`, greens 41/0/2, `tests/unit-ms4-id-prefixing.test.ts` 40/40 + `unit-ms4-id-prefixing-adversarial.test.ts` 9/9):** the `<name>:` prefix mint (§5.2 step 4g, `markdown-import.ts:279`) + the A1 default-seam REJECTION (resolution (a), §5.4 — the byte-pinned `documentId collides with a registered store name` message, A1-S7 erratum `includes(base)` take-as-given) + the SC1–SC5 invalid-store-context battery + the foreign-id-miss guarantee (INV-4) are all LANDED.** | `docs/specs/multi-document-store-config-review.md` §2 D4 + §8 A1 (four-agent gate, user go-ahead 2026-09-05) |
| **DECIDED: IMPORT-ROOT-PER-STORE — `edit.import_markdown` uses the ADDRESSED store's `corpusRoot` as the containment root; relative `files` resolve against it; the tool schema stays files-only (ADV-1); the description is corrected** | 2026-09-05 | The `edit.import_markdown` tool uses the addressed store's configured `corpusRoot` as the containment root; relative `files` paths resolve against the STORE's root (`resolve(corpusRoot, file)` — `markdown-import.ts:203` changes from `resolve(file)`; byte-identical for the default store when `corpusRoot === process.cwd()`, A5). The TOOL schema stays `files`-only (ADV-1 preserved, `unit-t-markdown-import.md:183`) — the seam is fed server-side from the registry, never by the caller, and the `store` argument cannot influence any path (adversarial-pinned). The tool description at `mcp-server.ts:1078` is CORRECTED in the same unit ("fixed server-side per store: the addressed store's configured corpus root; the default store's root is its configured corpus root (the project root when unconfigured)"). **Amended 2026-09-05 (F-MS2-6, the U-MS2 adversarial batch):** the row originally quoted the pre-amendment tail — "the default store's root is the project root" — which was factually wrong for a default entry with a CONFIGURED `corpusRoot`; the byte-pinned description string was amended to "…the default store's root is its configured corpus root (the project root when unconfigured)" (the rest byte-identical; spec `docs/specs/unit-ms2-store-wiring.md` §5.2's F-MS2-6 amendment — the code string and the sanctioned test-20 re-pin carry the amended wording). ONE-WAY-SNAPSHOT holds per store (each store's import is its own one-shot batch journal entry). The realpath/TOCTOU discipline (`markdown-import.ts:207-248`) is reused UNCHANGED per store root. **LANDED importer half (2026-09-05, Unit U-MS4 — spec `docs/specs/unit-ms4-id-prefixing.md`, greens 41/0/2, `tests/unit-ms4-id-prefixing.test.ts` 40/40 + `unit-ms4-id-prefixing-adversarial.test.ts` 9/9):** the importer's internal re-point (`markdown-import.ts:203`) + the per-store containment/TOCTOU reuse (`:207-248`) + the F-MS4-3 corpusRoot guard are LANDED; the wiring half stays U-MS2's (`mcp-server.ts:534-541` — the 3-arg pass-through feeding the store's `corpusRoot` + the `ImportStoreContext`).** | `docs/specs/multi-document-store-config-review.md` §2 D11 (four-agent gate, user go-ahead 2026-09-05) |
| **DECIDED: VECTOR-TOPOLOGY-PER-STORE — Phase-2 seams pinned NOW: per-store vector cache FILES, N boot controllers, per-store > global > lexical embedder precedence, non-default warm-up failure ⇒ that store stays lexical-pending permanently, registry embedder shape bans plaintext apiKeys** | 2026-09-05 (Phase-2-effective) | Pinned in the Phase-1 review so Phase 2 needs no rework (NOT implemented in Phase 1 — the parked row is `docs/pending.md`): (a) cache topology = PER-STORE files, one `createVectorCache({ path })` per store (`vector-cache.ts:170-174`), default `provident-vector-cache-<name>.json` with name `main` → the legacy `provident-vector-cache.json`; each store's §5.13 prune stays store-local — a shared single file would let one store's promotion prune other stores' entries (the critique's B1). (b) N `createVectorBootController(store, …)` instances (already store-scoped, `vector-boot.ts:110-114`). (c) Precedence: per-store `embedder` config > global env/args config (`main.ts:53-83`) > lexical; a store with no embedder config in global vector mode INHERITS the global config. (d) Warm-up: only the DEFAULT store's warm-up is sync-before-window (class 2 unchanged, `main.ts:159`); non-default stores warm up CONCURRENTLY in the background — a non-default warm-up failure ⇒ that store stays lexical-pending permanently (logged once, never retried), voiding the N-sequential-dark-boot cost. (e) Registry-file embedder shape: `Omit<EmbeddingProviderConfig,'apiKey'> & { apiKeyEnv?: string }` (`embeddings.ts:36-47` base) — a raw apiKey in the registry file is REJECTED by validation; secrets stay env-indirect. | `docs/specs/multi-document-store-config-review.md` §2 D6 (four-agent gate, user go-ahead 2026-09-05) |
| **DECIDED: UI-SELECTOR-DEFERRED — Phase 1 ships a read-only settings-pane store listing (provident-authored, operator-isolated scope, NOT MCP-visible) and NO active-store switcher; `RagQueryPayload` gains the SAME optional `store?: string` for MCP/UI mechanical symmetry; NO store-census MCP tool** | 2026-09-05 | The Phase-1 UI surface is a READ-ONLY store listing appended to the operator settings pane, authored as provident-ssr data in the isolated operator scope (PANE-PROVIDENT-AUTHORING + OPERATOR-ISOLATED-GRAPHSCOPE hold — a pane rendered outside the graph is a review finding), fed by a NEW read-only IPC channel + preload bridge method returning the registry's presentation view (name, default flag, persistence file name, corpus root, per-store loaded/corrupt status); NOT group-gated (IPC-SURFACE-NOT-GROUP-GATED, `decisions.md:45`) and NEVER MCP-visible. NO active-store switcher: the renderer boots/edits ONE store (the default) — "the UI's active store" is out of scope. MCP-UI-EQUIVALENCE resolution: `RagQueryPayload` gains the SAME optional `store?: string` (`types.ts:406-409`) forwarded by `handleRagQueryIpc` so the surfaces stay mechanically symmetric, while the pane's own query path passes nothing (a documented asymmetry row, sibling of EDIT-COMMIT-RETURN-ASYMMETRY); non-default search results are display-only (A2). NO store-census MCP tool (an agent must not enumerate store names — B9; the census is the operator UI); unknown-store errors echo only the caller-supplied name (A9). The new IPC channel is NOT a five-seam gate seam — the spec must state that explicitly so a later agent does not "fix" the census gap with an MCP `rag.list_stores` tool. **LANDED HALF (2026-09-05, Unit U-MS5 — spec `docs/specs/unit-ms5-settings-listing.md`, 41 pass / 3 skip, full suite 2415 pass / 41 skip):** the LISTING half of this row landed — the read-only `provident:rag-store-listing` IPC + the `bridge.rag.stores()` method + the provident-authored `operator-rag-stores` settings-pane section + the `RagQueryPayload.store?: string` passthrough. The DEFERRAL — the active-store SWITCHER (a UI control that changes the rendered store) — remains DEFERRED (Phase 1 still boots/edits ONE store, the default; "the UI's active store" stays a declared non-concept). The five refinement rows below (STORE-LISTING-IPC through RAG-QUERY-STORE-DISPLAY-ASYMMETRY) are this row's landed sub-decisions. | `docs/specs/multi-document-store-config-review.md` §2 D9/D10 (four-agent gate, user go-ahead 2026-09-05); landed-by U-MS5 2026-09-05 |
| **DECIDED: STORE-LISTING-IPC — a NEW read-only renderer→main IPC channel `provident:rag-store-listing` returns the registry's presentation view** | 2026-09-05 (LANDED, Unit U-MS5) | The `IPC_RAG_STORE_LISTING = 'provident:rag-store-listing'` channel (`shared/types.ts`) + the `handleRagStoreListingIpc` shared handler (`mcp-server.ts`, exported for direct unit testing) + the `ipcMain.handle` registration (`main.ts`) + the `bridge.rag.stores()` preload method — the registry's presentation view (one `RagStoreListingEntry` per configured store: name, default flag, persistence file NAME (basename — F7), corpus root, per-store load status). Manual-UI ONLY — NEVER MCP-visible (an agent cannot enumerate the configured store names, B9/A9). NOT group-gated (IPC-SURFACE-NOT-GROUP-GATED); NOT a five-seam gate seam (no `RpcMethod`, no `TOOL_GROUPS` row, no `ALL_TOOLS` row, no `MUTATING_METHODS` member, no renderer-switch method). The `RagQueryPayload.store?: string` passthrough rides this unit. | `docs/specs/unit-ms5-settings-listing.md` §4/§5.1/§5.2 (the §5.11-row that lands with the unit) |
| **DECIDED: STORE-LISTING-PROVIDENT-AUTHORED — the settings-pane listing is authored as provident-ssr data inside `settingsContent()`'s section** | 2026-09-05 (LANDED, Unit U-MS5) | The listing (`operator-rag-stores` container + `h3: 'RAG stores'` + one entry `div` per store or a placeholder `p`) is authored as provident-ssr data inside `settingsContent()`'s `section` (`sidebar-panes.ts`) and mounts in the isolated operator scope (PANE-PROVIDENT-AUTHORING + OPERATOR-ISOLATED-GRAPHSCOPE + UI-MOUNT-OPERATOR). NO handlers on any listing node (read-only; NO switcher); NO new `registerHandlerDef` (the handler-def census stays 12). A listing rendered outside the provident graph or inside the app graph is a review finding. | `docs/specs/unit-ms5-settings-listing.md` §4/§5.5 |
| **DECIDED: STORE-LISTING-BOOT-CACHED — the host fetches the listing ONCE at boot into `lastStoreListing` and re-renders it from the cache** | 2026-09-05 (LANDED, Unit U-MS5) | `boot()` fetches the listing once (`bridge.rag.stores()`) into the host's `lastStoreListing` cache BEFORE `mountOperator()`; the channel handler projects the IN-MEMORY boot-loaded registry form, never the registry file (D8 — a mid-run registry edit is observed only after restart). `reDerive()`/`refresh()` do NOT re-fetch (a red test pins the `stores()` call count stays 1 across a re-derive). A boot fetch bridge error does NOT abort boot (a display-only surface; `null` → the '(stores unavailable)' placeholder; deliberately DIVERGES from the snapshot/docHeads/template abort discipline — the F-MS5-3 awaited-inline availability note). | `docs/specs/unit-ms5-settings-listing.md` §4/§5.5 |
| **DECIDED: STORE-LISTING-STATUS-SHARED — `RagStoreLoadStatus` is declared ONCE in `shared/types.ts`; its three members are exactly the D7 failed-store matrix** | 2026-09-05 (LANDED, Unit U-MS5) | `RagStoreLoadStatus = 'loaded' | 'failed-corrupt' | 'failed-missing'` is the SINGLE shared declaration (`shared/types.ts`), coordinated with U-MS2's per-store `storeLoadStatus` derivation (`rag-store-directory.ts`) — a divergent re-declaration is a review finding (the A3/RCA-6 drift class, applied proactively). | `docs/specs/unit-ms5-settings-listing.md` §4/§5.1 |
| **DECIDED: RAG-QUERY-STORE-DISPLAY-ASYMMETRY — the MCP `rag.query` tool and the `rag-query` IPC are mechanically symmetric on the OPTIONAL `store` field, while the renderer's OWN query path never passes one** | 2026-09-05 (LANDED, Unit U-MS5) | `RagQueryPayload.store?: string` is forwarded by `handleRagQueryIpc` (`store: payload?.store` — the conditional-spread preload `query(query, topK?, store?)` idiom, byte-equal); the MCP tool + the IPC see the identical args shape (MCP-UI-EQUIVALENCE). The renderer's OWN query path (`submitQuery`) NEVER passes a store (no switcher; two-arg call) — a non-default result, if ever rendered in the search pane, is DISPLAY-ONLY: the search rows carry NO handler, and a `<name>:`-prefixed id does not resolve in the default store's document view. No UI control produces a non-default store in Phase 1. Sibling of EDIT-COMMIT-RETURN-ASYMMETRY (`decisions.md:46`). | `docs/specs/unit-ms5-settings-listing.md` §4/§5.6 (risk R9) |
| **DECIDED: RESULT-LEVEL-PROVENANCE — every `rag.query`/`rag-stream` result carries `citations` (the deduplicated grounding set `[{documentId, nodeId}]`) and `trace` (per-mode); a result without a `trace` is not a valid `RagResult`** | 2026-09-08 | The `rag.query`/`rag-stream` result is extended with `citations` + `trace` (flat mode = `{mode:'flat', engine, topK, source}`; graph mode = the ordered `reference`→`fact` path walked as `GraphTraceEntry[]`), plus the contract's `results` array and `blockedBy?`. The existing `ranked`/`context`/`markdown`/`lineMap`/`k`/`store` fields are PRESERVED (a backward-compatible additive extension). A result without a `trace` fails with `TraceUnavailable` (FS-16). | `docs/specs/unit-x-rag-provenance-traversal.md` §4 |
| **DECIDED: QUERY-AUDIT-LOG — every `ragQuery`/`ragStream` call is recorded in a bounded in-memory audit log (`getQueryAuditLog` → `[{query, filters, mode, resultCount, timestamp, requester}]`), exposed via BOTH the MCP `get_query_audit_log` tool AND the GUI audit panel (D4 parity)** | 2026-09-08 | A NEW pure module `src/main/query-audit.ts` hosts a bounded ring-buffer audit log (default 1000 entries; `record`/`list`/`clear`; never throws on a full log — the OLDEST entry is dropped). The log is created ONCE in main and shared by the MCP `rag.query`/`rag-stream` handlers + the `rag-query` IPC + the `get_query_audit_log` tool (the `handleRagTool` optional `auditLog?` param, threaded through `handleRagQueryIpc`). The GUI audit panel reads the SAME log via a `rag-query-audit` IPC (MCP/UI equivalence, §8.2). | `docs/specs/unit-x-rag-provenance-traversal.md` §4 |
| **DECIDED: GRAPH-MODE-WALK — `mode: 'graph'` is a deterministic, hop-limited local walk over the `reference`→`fact` graph (GAP-7, re-scoped to Astrographer — NOT an Incanter change)** | 2026-09-08 | `walkReferenceGraph` walks the `crosslink`-kind edges whose target is a `reference` OR `fact` node (a `content` target is NON-traversable), resolving through `FRESH`/`RESOLVED` and surfacing `BROKEN`/`STALE` in the trace (never silently traverses a stale embed). A `reference` target is a WAYPOINT (not a resolved target). A traversal that resolves no target returns an empty result with `blockedBy` (a valid state, not an error). `HopLimitExceeded` (FS-17) when the walk exceeds `maxHops` without resolving a target; `CycleDetected` (FS-18) on a `reference`→`fact` cycle. | `docs/specs/unit-x-rag-provenance-traversal.md` §4 |
| **DECIDED: PARENT-CONTEXT-EXPAND — `expand: 'parent'` returns the parent Document/node-cluster for a retrieved child, capped by `maxParentContext` (default 5) and stale-propagating** | 2026-09-08 | `expandParentContext` returns the top `maxParentContext` result items (by result order) with a `parent` (the owning Document's `title` + a `snippet`); items beyond the cap are returned WITHOUT a `parent`. A child with no owning document omits the `parent`. Stale-propagating: a child's `parent.stale` is `true` if ANY incoming `reference`→`fact` edge to the child has `state: 'STALE'`. Deterministic. | `docs/specs/unit-x-rag-provenance-traversal.md` §4 |
| **DECIDED: REFERENCE-GRAPH-ADDITIVE-FIELDS — the `reference`→`fact` graph is realized on the existing `RagStore` substrate via ADDITIVE fields (`RagNode.nodeKind?`, `RagEdge.edgeType?`, `RagEdge.state?`)** | 2026-09-08 | The `reference`→`fact` graph is the subgraph of `crosslink`-kind edges whose source is a `reference` node and target is a `fact` node, realized on the existing `RagStore` substrate via ADDITIVE fields: `RagNode.nodeKind?: 'content'|'fact'|'reference'` (default `'content'`), `RagEdge.edgeType?: 'link'|'embed'` (default `'link'`), and `RagEdge.state?: 'FRESH'|'RESOLVED'|'STALE'|'BROKEN'` (default `'RESOLVED'` for `link`, `'FRESH'` for `embed`). These follow the CHILDREN-ADDITIVE-STORE-FORMAT precedent: additive, hash-covered (in `nodeSource`/`edgeSource`), no migration/re-hash; validated at write (the byte-pinned `rag putNode: nodeKind required/invalid` / `rag putEdge: edgeType/state required/invalid`), skipped at boot. The runtime sets (`RAG_NODE_KINDS`/`RAG_EDGE_TYPES`/`RAG_REFERENCE_STATES`) live in `rag-store.ts`, not `adjacency.ts`. | `docs/specs/unit-x-rag-provenance-traversal.md` §4 |
| **DECIDED: QUALIFY-RESULT — the merged fan-out result is store-qualified by a PURE builder `qualifyStoreResult(merged, stores, { qualified })`; `opts.qualified` is the A-F1 gate (`true` stamps `store`/builds `storeContexts`; `false` returns `merged` byte-equal)** | 2026-09-08 (LANDED, Unit U-F2) | U-F2's pure builder (`src/main/retrieval.ts`) — PURE + DETERMINISTIC, no Electron/I/O, never mutates `merged` or the per-store results (`qualified:true` spread-copies items). `qualified:false` returns `merged` UNCHANGED (object-/byte-equal, `stores` not read) — the A-F1 gate that keeps the Phase-1 A4 single-store `ragQuery` provably byte-equal. `qualified:true` attributes each merged item to its producing store by REFERENCE IDENTITY against `stores[].result.results` (U-F1's `mergeStoreResults` interleaves by reference), re-derives `citations` with the first-appearance store, and builds `storeContexts`. Fail-states pinned (§5.6/§5.9): `qualifyStoreResult: merged result required` / `opts required` / `qualified must be a boolean` / `stores required when qualified` / `store entry required` / `store name must be a non-empty string` / `store result must have context, markdown and lineMap` (F-F2-1 adds `store results must be an array`) / `unattributable result item`. | `docs/specs/unit-f2-result-qualification.md` §4/§5 |
| **DECIDED: STORECONTEXTS-BLOCKS — the qualified result carries an ADDITIVE `storeContexts` array (one block per store with a VALID result, in registry-insertion order, default block FIRST; failed/null-result stores contribute NO block)** | 2026-09-08 (LANDED, Unit U-F2) | `StoreContextBlock = { store: string; context: RagNode[]; markdown: string; lineMap: LineNodeMap }` (D2). Each block carries that store's OWN assembled context/markdown/lineMap; the TOP-LEVEL `context`/`markdown`/`lineMap` on the `RagResult` stay the DEFAULT store's block (U-F1's merged block, UNCHANGED). `storeContexts` is absent (`undefined`) when NOT qualified (A-F1). A store with a VALID result but an EMPTY `results` array still contributes a (possibly empty) block — NOT an error; a null/undefined-result store (failed, D6) is SKIPPED. | `docs/specs/unit-f2-result-qualification.md` §4/§5.4/§5.5 |
| **DECIDED: STORE-FIELD-NAMING — every `store` value (per-item/per-entry/block) is the producing store's REGISTRY NAME, bare (no `<name>:` id prefix); the default store's `store` is its registry name (e.g. `'main'`), NEVER `''`** | 2026-09-08 (LANDED, Unit U-F2) | D3 — the `store` on every `RagResultItem`/`citations` entry/`storeContexts` block names the producing store's registry `name` WITHOUT the `<name>:` prefix (the prefix is a secondary signal only — a `'wiki'` store whose ids are `wiki:<id>` stamps `store:'wiki'`, NOT `'wiki:'`). A fan-out always runs under a registry (A-F3), so the default store's `store` is its registry name (`'main'`), NEVER `''` — the legacy `''` sentinel is UNREACHABLE in a qualified result. The additive `store?` on `GraphTraceEntry`/`BlockedByEntry` is TYPE-SHAPE UNIFORMITY ONLY — NEVER stamped by the qualified FLAT builder (the fan-out is FLAT-only, D4). | `docs/specs/unit-f2-result-qualification.md` §4/§5.4 |
| **DECIDED: FANOUT-INTERLEAVE-MERGE — the cross-store `stores:"all"` fan-out merges per-store top-k results by a pure, rank-based interleave (NEVER score-compare), skipping failed/empty stores, deterministic in registry-insertion order; the additive result-shape changes are A-F1-gated to U-F2** | 2026-09-08 | The cross-store fan-out slice is a 3-unit decomposition (D8: U-F1 pure merge → U-F2 store-qualification → U-F3 fan-out wiring). **D1 (merge policy, U-F1):** the merged `results` is a per-store top-k round-robin by rank (`mergeStoreResults(stores, { topK })`, `src/main/merge-store-results.ts`); NEVER a cross-store score comparison — a per-store `score > 0` floor is applied upstream and NOT re-applied, making mixed embedders SAFE; merged length ≤ N×topK. **D6 (skip-failed/empty, U-F1):** an empty-`results` store contributes zero; a NON-default store whose `ragQuery` threw is passed as `{ name, result: null }` and SKIPPED (never a loud fail); a FAILED DEFAULT store (first entry) FAILS LOUD (`mergeStoreResults: default store result required`) — the deliberate D6 default-exemption, since the top-level `ranked`/`context`/`markdown`/`lineMap`/`k` block is the default store's. **D7 (determinism, U-F1):** the merge consumes stores in canonical (registry insert order = `RagStoreDirectory.entries` `Map` key order); deterministic (same input → same output). **A-F1 (gating):** the `storeContexts`/per-item `store` shape changes are U-F2's, gated to `stores:"all"` ONLY; U-F1 keeps the top-level fields as the default store's block and does NOT touch `RagResult`/`RagResultItem` (Phase-1 A4 single-store byte-equality untouched). **LANDED (2026-09-08, Unit U-F1 — spec `docs/specs/unit-f1-merge-store-results.md` §3a/§4/§5, greens 23 PASS / 0 FAIL, `tests/unit-f1-merge-store-results.test.ts` 31/31):** the pure module exported `mergeStoreResults` + `StoreResultInput` (`src/main/merge-store-results.ts`); adversarial F-F1-1..F-F1-4 all HOST — F-F1-2/F-F1-3 FIXED + regression-tested (the default-store block-field assertion with the byte-pinned `…must carry ranked/context/markdown/lineMap/k`, and the sparse-array/`null`-element interleave skip), F-F1-1/F-F1-4 INFO (documented, no fix — string-coercion dedup key + by-reference item copy, per spec §5.2). U-F2 (`docs/specs/unit-f2-result-qualification.md`) + U-F3 (`docs/specs/unit-f3-stores-all-schema.md`) NOT yet landed. | `docs/specs/multi-store-fanout-review.md` §2 D1-D8 + §3 A-F1 (four-agent gate, user go-ahead given 2026-09-08) |

| **DECIDED: STORES-ALL-SCHEMA — `stores?: 'all'` (a single-member string enum) is added to the `rag.query`/`rag-stream` inputSchemas + the `rag-query` IPC `RagQueryPayload`, ADDITIVE alongside (never replacing) the existing optional `store`** | 2026-09-08 (LANDED, Unit U-F3) | D5/A-F4 — `rag.query`/`rag-stream`: `stores: z.enum(['all']).optional()`; `RagQueryPayload.stores?: 'all'`. Omission keeps the single-store path byte-equal. Mutually exclusive with `store` (A-F3). A direct caller passing a non-`'all'` `stores` value fails the defensive pin `rag.query: stores must be "all"` (mirrors the zod enum). | `docs/specs/unit-f3-stores-all-schema.md` §4/§5.1 |
| **DECIDED: REGISTRY-REQUIRED — `stores:"all"` with `dir == null`/undefined (no registry to fan across) FAILS LOUD before any engine call (A-F3)** | 2026-09-08 (LANDED, Unit U-F3) | The legacy directory-less single-store sentinel has no registry to fan out to. `{ stores:'all' }` with a null/omitted `dir` throws `rag.query: stores:"all" requires a configured store registry` (rejects identically on the `rag-query` IPC). Precedes the engine call. | `docs/specs/unit-f3-stores-all-schema.md` §4/§5.2/§5.9 |
| **DECIDED: MUTUAL-EXCLUSION — a request carrying BOTH `store` AND `stores` is rejected before any engine call (A-F3); the guard fires BEFORE `resolveStoreArg`'s unknown-store M2 throw** | 2026-09-08 (LANDED, Unit U-F3) | Guard precedence (arbitration): `{ store: anything, stores }` → `rag.query: store and stores are mutually exclusive`, never the M2 unknown-store throw. In `rag-stream` it surfaces as the `[{type:'error',error}]` chunk. Byte-pinned message. | `docs/specs/unit-f3-stores-all-schema.md` §4/§5.2/§5.9 |
| **DECIDED: FLAT-ONLY-GUARD — `stores:"all"` with `mode:'graph'` FAILS LOUD (byte-pinned `rag.query: stores:"all" is only valid in flat mode`)** | 2026-09-08 (LANDED, Unit U-F3) | Consumed from D4: the fan-out is FLAT-mode only. `mode:'graph'` WITHOUT `stores:"all"` is unchanged (a valid single-store graph query). The guard is the last of the four §5.2 `stores:"all"` guards (after mutual-exclusion, stores-value, dir==null). | `docs/specs/unit-f3-stores-all-schema.md` §4/§5.2/§5.9 |
| **DECIDED: FAN-OUT-WIRING — for `stores:"all"`, `handleRagTool` iterates `dir.entries` in canonical (registry insertion) order (D7), queries each entry's engine with the same per-store topK (D1), SKIPS a throwing non-default store (`{name, result:null}`, D6), REORDERS default-first, then `mergeStoreResults` (U-F1) + `qualifyStoreResult(merged, stores, { qualified:true })` (U-F2); the merged result is stamped with the default entry's registry name** | 2026-09-08 (LANDED, Unit U-F3) | A-F4 — wired in BOTH the `rag.query` (returns `{ ...qualified, store: defaultName }`) and `rag-stream` (returns `[{type:'result',result:qualified},{type:'done'}]` WITHOUT a top-level `store`) cases. The default store's failure surfaces as U-F1's `mergeStoreResults: default store result required` (no special-case). Deterministic. | `docs/specs/unit-f3-stores-all-schema.md` §4/§5.3/§5.5/§5.8 |
| **DECIDED: MERGED-AUDIT — for `stores:"all"` the fan-out records ONE audit entry with `resultCount` = the MERGED count (post-merge, unchanged by qualification) + an OPTIONAL `stores: string[]` (the canonical-order queried store names, A-F2)** | 2026-09-08 (LANDED, Unit U-F3) | D4/A-F2 — `auditLog.record({ ..., mode:'flat', resultCount: merged.results.length, stores: canonicalNames, requester:'mcp' })`. `stores` present ONLY for the fan-out; single-store entries stay byte-equal (no `stores` key). Recording skipped when `auditLog` null (no throw). Flows through `list()` verbatim. | `docs/specs/unit-f3-stores-all-schema.md` §4/§5.4 |
| **DECIDED: REGISTRY-WRITE-MODULE — the registry WRITE seam is a SEPARATE pure module `src/main/rag-store-registry-write.ts`; the loader `src/main/rag-store-registry.ts` is byte-unchanged, read-only, and PURE** | 2026-09-08 (LANDED, Unit U-H1) | The loader's `REGISTRY-NO-WRITE` pin is REFINED to be per-module (A-P2-3): it governs the LOADER'S import set only, so `docs/decisions.md`'s `unit-ms1` banned-export census (tests 45–46) stays green because the loader's export surface + source are untouched. The write module's ONLY loader seam is `resolveRegistry` (+ `loadRagStoreRegistry` for the final step-5 re-load) + the types + the consts it needs. The write module's import set holds the WRITE/rename/fsync primitives (`writeFileSync`/`renameSync`/`mkdirSync`/`existsSync`/`readFileSync`/`statSync`/`openSync`/`fsyncSync`/`closeSync`) but NO removal/truncate primitive (`unlink`/`unlinkSync`/`rm`/`rmSync`/`rmdirSync`/`truncateSync`) — the structural D3 no-REMOVE pin closing the orphan guarantee at the source level. NO Electron, NO store/engine import, NO IPC/MCP (D6). | `docs/specs/unit-h1-registry-write.md` §4/§5.1 |
| **DECIDED: REGISTRY-ATOMIC-WRITE — every registry persist is atomic (`mkdirSync(recursive)` → `writeFileSync(path+'.tmp')` → fsync the temp → `renameSync`); a failed write leaves the ORIGINAL file intact** | 2026-09-08 (LANDED, Unit U-H1) | Persisted form is `JSON.stringify({ version: 1, stores }, null, 2)`. A failed write leaves the original file intact; a crash between the temp write and the rename leaves the original plus a stale `.tmp` (harmless — overwritten by the next write, ignored by the loader). A written registry IS re-readable by `loadRagStoreRegistry` (round-trip, R2). | `docs/specs/unit-h1-registry-write.md` §4/§5.4 |
| **DECIDED: REGISTRY-RELOAD-ON-WRITE — the combined write entry re-reads the CURRENT disk registry first (fail-loud on corrupt, never the implicit form on a hot write), mutates, persists atomically, then RE-LOADS and returns the fresh loaded form + the delta (D2: persisted-and-live never diverge)** | 2026-09-08 (LANDED, Unit U-H1) | D2 — the disk file IS re-read + re-applied. SUPERSEDES the boot-time-only "registry change observed only after restart" assertion for this module and — by A-P2-3 designation — `tests/unit-ms5-settings-listing.test.ts` Red 18 (the DESIGNATION pins here; the mechanical refresh-on-apply wiring + the Red 18 re-write land in U-H2 as the runtime controller's job). The U-MS5 §5.9 fail-state 10 + the `STORE-LISTING-BOOT-CACHED` row keep holding for the PRE-H2 wiring. | `docs/specs/unit-h1-registry-write.md` §4/§5.10 |
| **DECIDED: REGISTRY-WRITE-FAILS-LOUD — a persist/write failure THROWS (the native fs Error), deliberately diverging from the sibling stores' catch-and-swallow** | 2026-09-08 (LANDED, Unit U-H1) | An unobservable hot-write failure would let persisted-and-live diverge silently (violating D2). mkdir/writeFileSync/fsync/renameSync throwing ⇒ the native fs Error PROPAGATES (EACCES/EROFS/ENOENT/EISDIR/ENOTEMPTY/ENOSPC/…) — the throw path is pinned, the message is the native one (vs the siblings' `catch {}` at `module-store.ts:139-141`/`template-store.ts:108-110`, NOT copied). The write module emits ZERO console.* log output (failures are thrown, not logged; the controller decides how to surface them). | `docs/specs/unit-h1-registry-write.md` §4/§5.4/§5.5 |
| **DECIDED: REGISTRY-DELTA-RETURN — the write module returns the new registry state AND the delta `{ added | removed | renamed }`; the delta is a pure structural computation, exactly ONE non-empty member per mutation** | 2026-09-08 (LANDED, Unit U-H1) | Applying the delta to the live runtime — teardown, engine construct, re-bind of the const-captured closures, the `teardown()` primitives — is the RUNTIME controller's (U-H2) explicitly NOT this unit's job. `add` → `added:[name]`; `remove` (orphan, D3 — the entry only, the persistence file + journal NEVER touched) → `removed:[name]`; `rename` (NON-default only, D4/D5; the no-persisted-ids half is a documented U-H2 caller precondition) → `renamed:[{from,to}]`. | `docs/specs/unit-h1-registry-write.md` §4/§5.4/§5.6 |
| **DECIDED: RUNTIME-CONTROLLER — a NEW node-testable module `src/main/rag-store-runtime.ts` exports `createRagStoreRuntimeController(opts)`, a controller owning the mutable live directory + the single hot-apply orchestration seam** | 2026-09-08 (spec authored, Unit U-H2) | The controller is CONSTRUCTED ONCE at boot AROUND the boot `RagStoreDirectory` (the SAME object `buildRagStoreDirectory` built) — construction is side-effect-free: NO rebuild, NO fs, NO re-read (presence is byte-equal to today's boot). The factory + 3 types (`RagStoreRuntimeOptions`/`HotApplyResult`/`RagStoreRuntimeController`) + 10 controller methods (`getDirectory`/`getDefaultEntry`/`getDefaultName`/`getDefaultStore`/`getDefaultEngine`/`getVectorBoot`/`getRegistryPath`/`currentStores`/`statusOf`/`hotApply`). | `docs/specs/unit-h2-runtime-controller.md` §4/§5.1–§5.4 |
| **DECIDED: MUTABLE-LIVE-DIRECTORY — the controller holds the boot `RagStoreDirectory` and mutates its `entries` Map IN PLACE (never replacing the Map or the directory object)** | 2026-09-08 (spec authored, Unit U-H2) | `entries` stays typed `ReadonlyMap` (consumers cannot write) but the runtime object is a real `Map`; the controller is the ONLY writer. Because `handleRagTool`/`handleEditTool`/`handleRagStoreListingIpc`/`IPC_RAG_QUERY`/`IPC_RAG_SNAPSHOT` read `entries` per call through the SAME captured directory object, an in-place mutation makes every already-wired closure see new stores WITHOUT re-pointing (the D1 "no re-registration" basis). `defaultName` is stable. Preserves the U-MS1 F-MS1-15(c) Map-keyed pin. | `docs/specs/unit-h2-runtime-controller.md` §4/§5.4 |
| **DECIDED: DEFAULT-STABLE-APPLY + REBUILD-ALL-OF-NON-DEFAULT — U-H2's reachable mutations never change the default; a hot-apply REBUILDS the non-default entries from the freshly loaded registry, ORPHANING the old ones WITHOUT teardown** | 2026-09-08 (spec authored, Unit U-H2) | Reachable-mutation set = add/remove/rename NON-default only (U-H1's W-rename-default + F12 guards). Rebuilt non-default entries are always-lexical (`createJsonRagStore` + lexical `createRetrieval`, A6/R10); the DEFAULT entry's store/engine object identities AND the `vectorBoot` controller carry across unchanged (never re-warmed) — keeping the default-serving path byte-equal and the apply teardown-free before U-H5. Old non-default entries are ORPHANED (dropped from the live Map + closures; NO `teardown()` — U-H5/A-P2-8). The `delta` is threaded in `HotApplyResult` so U-H3/U-H4/U-H6 can later substitute a teardown-aware incremental apply. | `docs/specs/unit-h2-runtime-controller.md` §4/§5.4–§5.7 |
| **DECIDED: HOT-APPLY-TRIGGER-RESOLVED — the single runtime trigger is the controller's `hotApply(mutation)` orchestration seam (a synchronous in-process method consuming the U-H1 `writeRegistryMutation`), never an MCP tool and never a U-H2-added IPC channel** | 2026-09-08 (Architect-confirmed, Unit U-H2) | The gate (D6) forbade a new MCP tool + new IPC channel but did NOT pin the runtime trigger; the Architect CONFIRMED the `hotApply(mutation)` seam (single mutation per apply, synchronous, returns `{ loaded, delta }`). It is invoked by the LATER U-H8 operator-UI IPC handler (which may add the IPC channel in U-H8, per D6) and by U-H2's node tests. Consistent with D6: U-H2 adds NO MCP tool and NO IPC channel — the seam is an in-process method. | `docs/specs/unit-h2-runtime-controller.md` §4/§7 |
| **DECIDED: NO-OP-BYTE-EQUAL — constructing the controller + a NO-OP run leave the boot byte-identical; the normal boot path still reads the registry EXACTLY ONCE per boot (D8)** | 2026-09-08 (spec authored, Unit U-H2) | (i) constructing the runtime controller does NOT rebuild/re-read/touch anything; (ii) `writeRegistryMutation`'s re-read happens ONLY inside `hotApply` — an explicit operator-triggered re-read, the "controlled re-read" the D8 pin allows (the boot path stays read-once); (iii) every reachable `hotApply` (non-default mutations) leaves the default-serving path byte-identical. | `docs/specs/unit-h2-runtime-controller.md` §4/§5.4 |
| **DECIDED: ATOMIC-APPLY — `hotApply` order (D7): validate → (rename) D4 no-ids inspection → `writeRegistryMutation` (the only disk touch, atomic; a throw leaves disk+live untouched) → CONSTRUCT-fully-then-INSERT into a STAGING map (if any construct throws, the live Map is NOT mutated) → swap in ONE synchronous pass → return `{ loaded, delta }`** | 2026-09-08 (spec authored, Unit U-H2) | A successful write ALWAYS completes the swap for U-H2's reachable set (loader-validated ⇒ non-throwing constructs), preserving D2 (persisted-and-live never diverge); the (unreachable) construct-throw case is surfaced by the throw, not silent divergence. | `docs/specs/unit-h2-runtime-controller.md` §4/§5.4 |
| **DECIDED: D4-NO-IDS-CALLER-PRECONDITION — for a `kind:'rename'`, `hotApply` inspects the CURRENT store's data for any node id starting with `<from>:` BEFORE any write and THROWS `R-rename-ids-present` (disk NOT written)** | 2026-09-08 (Architect-confirmed home: Unit U-H2) | This is the U-H1 §5.3.2-declared "U-H2 caller precondition" (U-H1 cannot verify it — it needs store DATA). Id-migration for populated non-default stores is a SEPARATE unit (D4). The Architect confirmed U-H2 (not U-H6) is the home for the store-data inspection, so the TestWriter writes the F10 row. | `docs/specs/unit-h2-runtime-controller.md` §4/§5.4/§7 |
| **DECIDED: A-P2-1-CLOSURE-REWIRING (U-H2b) — every default/directory-bound closure in `main.ts` and `mcp-server.ts` reads the runtime's accessors PER CALL (the runtime seam is injected); the legacy const fallback stays byte-equal for the directory-less/single-store path** | 2026-09-08 (spec authored, Unit U-H2b) | 13 main.ts closure bind sites rewired (B1–B13, source-anchored) + mcp-server.ts changes (1 new server option, 1 stored field, 2 call-site forwards, 1 signature change, 2 handler-closure resolution changes, M1–M4). MCP-UI-EQUIVALENCE preserved (both surfaces read the same runtime accessors per call). | `docs/specs/unit-h2-runtime-controller.md` §4/§5.8–§5.9/§5.11 |
| **DECIDED: REFRESH-ON-APPLY (A-P2-3) — `IPC_RAG_STORE_LISTING` reads the runtime's CURRENT resolved projection (`currentStores()` + `statusOf`), NOT the boot `registry` const** | 2026-09-08 (spec authored, Unit U-H2b) | A pull after an apply shows the new state. NO renderer/pane node change is part of U-H2 (U-MS5's surface survives; U-H8 owns the operator editor). Mechanical supersession of `tests/unit-ms5-settings-listing.test.ts` Red 18 (per the U-H1 REGISTRY-RELOAD-ON-WRITE designation) lands here. | `docs/specs/unit-h2-runtime-controller.md` §4/§5.8–§5.10 |

## SUPERSEDED

| Decision | Date | What it pinned | Replaced by |
| --- | --- | --- | --- |
| **DECIDED: CONTENT-GRAPH-AUTHORITATIVE (prior reshape §3a)** | 2026-08-26 | The document content graph is authoritative; the RAG index is a materialized projection. | **RAG-AUTHORITATIVE** — the RAG layer is authoritative; the provident graph is the materialization (user clarification §8.1). |
| **DECIDED: 1:1-BACK-REFERENCE (prior reshape §9.2.2)** | 2026-08-26 | The back-reference is `Map<nodeId, ragNodeId>` (1:1). | **SUBTREE-OWNERSHIP** — the back-reference is `Map<ragNodeId, nodeId[]>` (many-to-one; a RAG object owns a subtree). |
