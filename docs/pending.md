# Astrographer — Pending / Parked / Speculative

Maintained by the document-archival loop (AGENTS.md item 6). Three kinds of
rows: (a) UPSTREAM — constraints owned by the `provident-ssr` project that this
project must respect (imported from `../Provident-Electron/docs/` and
`../Preempt-Providence/docs/`); (b) DEFERRED — lower-value gaps parked until a
use case surfaces; (c) SPECULATIVE — future features with their revisit
conditions.

Retired PARKED and SPECULATIVE rows are archived in the gitignored `archive/`
dir and do not ship in a fork (see `docs/FORKER.md` §1/§5).

## UPSTREAM (imported constraints)

| Constraint | Source | What this project does |
| --- | --- | --- |
| NEVER edit `node_modules/provident-ssr/` or `../Preempt-Providence/` source | AGENTS.md (foundation) | All engine gaps recorded in `docs/defects.md` → `docs/HANDOFF.md` (feature requests to the provident dev agent) |
| ALL non-shell UI must be rendered with the provident framework (graph → DomAdapter/SSRFragmentAdapter/MarkdownAdapter), never hand-written HTML/DOM | AGENTS.md (foundation) | The wiki UI (content window, sidebar panes, settings) is authored as provident-ssr data; a UI element outside the graph is a review finding |
| `MarkdownAdapter` drops `css:*` (D5) and `data:*`/`data-node-id` (D7) | upstream adapters.md §4.7 | Formatting that must survive markdown is encoded as element TYPES; the line→node map is host-side (markdown is export-only). **ENG-GAP-1 (markdown node-identity preservation) is SHELVED 2026-08-26** — markdown is export-only, comment IDs are fragile, and markdown-parsing-to-storage will use text-match diffing instead (see SPECULATIVE below) |
| `state-slice` mutation targeting a placement zone is HARD-BLOCKED (`placement-target-blocked`, FS-10) | upstream node.md §7.1 | Editing writes back to the RAG store → re-traversal, NOT a zone-targeted state-slice |
| A `contentNodes`-owned content root is family-'in-tree' but DROPPED from compile until attached (P3 §2.4, F-13) | upstream node.md / translate.md | Every RAG subtree root must be placement-attached to render; "owned ⇒ rendered" is false |
| No network egress yet (the `connect-src` CSP allowlist for a declared network is an open tracked item) | foundation pending.md M-r12 | Retrieval is local-first (lexical BM25/tf-idf); the vector embedder (Unit F) is now implemented behind the `Embedder` interface. **PROVIDER-AGNOSTIC (2026-08-27):** the vector embedder (Unit F) is provider/model agnostic — local (ollama `embeddinggemma`) AND remote/cloud providers are in scope, so the `connect-src` CSP allowlist + API-key handling become a DESIGNED security surface (not just an open tracked item). A localhost ollama call is LOCAL (no external egress); a remote/cloud provider requires the CSP allowlist + API-key config. |

## DEFERRED (lower-value / parked)

| Item | Date | Constraint / revisit condition |
| --- | --- | --- |
| **Vector embeddings** | 2026-08-26 | Deferred behind the interface-swappable `Embedder` (Unit F). **IMPLEMENTED (Unit F, 2026-08-27)** — the vector embedder is a drop-in behind the same interface (`createRetrieval` uses the passed embedder; `rag.query`/`rag-query` share the maintained engine). `src/main/embeddings.ts` implements the `EmbeddingProvider` abstraction + `EmbeddingProviderConfig` config shape, the ollama `embeddinggemma` concrete provider (localhost), the remote/cloud provider drop-in, the vector index, cosine similarity scoring, the vector embedder behind the async-amended `Embedder` interface, the deterministic mock embedder, and the real-ollama integration path. Selected via `retrieval.embedder: 'lexical' | 'vector'` in `main.ts`. Lexical-first (BM25/tf-idf) remains the default; the vector embedder is opt-in. |
| **contenteditable rich inline editing** | 2026-08-26 | Rejected for v1 (fights graph-is-authoritative; `DomAdapter.text` clobbers a live editor). Form-control editing (commit-on-blur) is the v1 path. **Focused validity check 2026-08-26: FEASIBLE-WITH-CONSTRAINTS** — per-element graph-authority suspension is cleanly implementable via a `DomAdapter` decorator wrapper (skip `setProp(wire,'text',…)` for a `suspendedWires` set; forward `.wires`/`beginBatch`/`endBatch`); the write-back model (on blur → RAG store → re-traversal) is untouched; no engine gap. Constraints: (1) the suspension only intercepts `set` text ops — it does NOT prevent a full re-traversal from re-creating the edited element (the dirty-edit guard is the protection for that case); (2) HTML→plain-text conversion on blur is a real project-specific gap for rich subtrees (`ul`+`li`, `strong`, `em`) — the contenteditable produces HTML that must be decomposed back into the RAG store's per-node plain-text `content` + element `type` tree; (3) the current `CaretState` (`{ offset, focused }`) does not capture a rich contenteditable's Selection/Range. **PROPOSAL GATE 2026-08-28: PROCEED-WITH-AMENDMENTS, sequenced textarea-first.** The `provident-editable@0.1.0` package (HTML→provident-tree converter + diff) is adopted, but the slice is SEQUENCED: land the plain-text textarea editing UI (Unit D §5.6, deferred per Unit D §3a H5) FIRST, then the rich-text contenteditable machinery. Resolved design (see `docs/decisions.md` RICH-TEXT-EDITING-GATE): `span` is NOT added to `RagNodeType` (a diff-matching artifact folded into the parent's `content`); inline `strong`/`em`/`a`/`img` are held by a NEW `children` field on `RagNode`; `setProps` MERGES (preserves `data-doc-head`) + new `setSubtree`/`setType` ops; plain-text is the DEFAULT for all nodes in v1, rich-text opt-in per-node-type. **Batch atomicity MET 2026-08-28 (Unit N)** — the `applyBatch` transaction primitive (a real transaction, not `store.enqueue`) has landed. **IPC_EDIT_BATCH MET 2026-08-28 (Unit P)** — the `IPC_EDIT_BATCH` channel + the `handleEditBatch` shared handler + the `bridge.edit.batch` bridge + the `deriveBatchBroadcast` helper have landed (see `docs/decisions.md` IPC-EDIT-BATCH). on the `RagStore` interface (see `docs/decisions.md` BATCH-ATOMICITY-API). Must-fix before the rich-text spec: retrieval indexing of inline `children`, traversal disambiguation, paste-time sanitization, census 6→9. **Store-format `children` additive + hash-source MET 2026-08-28 (Unit M)** — the `children` field on `RagNode` + the `RagNodeChild`/`RagNodeChildType` types + the `nodeSource` hash coverage + the additive load + the write-time validation have landed (see `docs/decisions.md` CHILDREN-ADDITIVE-STORE-FORMAT / CHILDREN-HASH-SOURCE). **Census 6→9 MET 2026-08-28 (Unit O)** — the edit-op count goes from 6 to 9 (the three rich-text ops `setProps`/`setSubtree`/`setType` land in `src/main/edit-ops.ts`). **Textarea-first prerequisite MET 2026-08-28 (Unit L)** — the plain-text textarea editing UI has landed. **Retrieval indexing of inline `children` text MET 2026-08-28 (Unit Q)** — the retrieval module indexes + renders the inline `children` text via the new `nodeText(node)` helper (see `docs/decisions.md` NODETEXT-SPACE-JOIN / RENDER-DIRECT-CONCAT). **Traversal disambiguation of inline vs doc-children MET 2026-08-28 (Unit R)** — the traversal renders the inline `children` as child elements of the subtree root, disambiguated from doc-children by the `rag-` id prefix (see `docs/decisions.md` INLINE-CHILDREN-AUTHORED-ID). **Paste-time sanitization MET 2026-08-28 (Unit S)** — the pure `sanitizePastedHtml` module normalizes pasted HTML into the `RagNodeChild[]` shape (see `docs/decisions.md` PASTE-SANITIZATION). **All RICH-TEXT-EDITING-GATE must-fix items are now MET (2026-08-28, Units N/O/P/Q/R/S)** — the rich-text contenteditable editing slice is COMPLETE (see `docs/next-steps.md`). Revisit the rich-text machinery after the textarea UI lands. |
| **Stored document-flow edges as a separate engine mechanism** | 2026-08-26 | Rejected — doc-flow edges are authoritative in the RAG store; the traversal maps them to family order + a doc-head marker prop. No new engine roles (closed `Role` union). **DECISION RECORD (not a future candidate):** this is a final rejection, not a parked option — the engine's `Role` union is closed (`'parent' | 'child' | 'source' | …`), so a native doc-flow mechanism would require opening the union (an engine change, a handoff item, never a host change). The chosen DERIVED-DOC-FLOW design (store-authoritative `doc-head`/`next-section`/`doc-end`/`doc-child` edges → traversal maps them to family child-anchor order + the `data-doc-head` marker prop) is the only correct home given the closed union. Revisit only if the upstream engine opens its `Role` union. |

## SPECULATIVE (future features)

| Item | Date | Constraint / revisit condition |
| --- | --- | --- |
| **Markdown-vs-source diffing to detect changes** | 2026-08-26 | User decision: markdown is EXPORT-ONLY; agent changes go through direct MCP updates. Diffing markdown vs. generated source to detect changes is a FUTURE SPECULATIVE feature. Revisit when a use case needs change-detection from the export surface. |
| **Remote embedding provider** | 2026-08-26 | Deferred behind the `Embedder` interface. **IMPLEMENTED (Unit F, 2026-08-27)** — the remote/cloud provider (`createRemoteEmbedProvider`) is a drop-in behind the SAME `EmbeddingProvider` interface + config, with the `connect-src` CSP allowlist + API-key handling as a DESIGNED security surface (REMOTE-SECURITY-POSTURE). Tested via a MOCKED HTTP layer (no live remote call in the test suite — no network egress in CI). |
| **API access to a remote DB (remote RAG store)** | 2026-08-26 | A `createRemoteRagStore` implementing the SAME `RagStore` interface as `createJsonRagStore` (Unit A §5.3 — the abstraction layer). The document load (traversal) and MCP `rag`/`edit` handlers depend on the `RagStore` interface only, so a remote DB is a drop-in replacement. Requires network egress + auth + a new security surface. Revisit when a declared network is allowed. |
| **RAG object versioning by journal timestamps** | 2026-08-26 | The project journal (Unit A §5.6) already records invertible entries with timestamps. Versioning would reconstruct a RAG object's state at a given journal timestamp (point-in-time snapshot) — e.g. preserve the documentation for a long-term-stable (LTS) build while development continues. Revisit when the project journal lands and a point-in-time reconstruction use case surfaces. |
| **RBAC to selective versions** | 2026-08-26 | Role-based access control to SPECIFIC versions of RAG objects — e.g. test/users have READ-ONLY access to the LTS version, while developers can edit the dev version. Builds on RAG-object versioning (above) + the foundation's security model (tool groups, `security.ts`). Revisit when versioning lands and a multi-role access use case surfaces. |
| **Per-leaf citation from markdown** | 2026-08-26 | The line→node map is COARSE (whole subtree → one RAG object), so per-leaf citation is impossible by design. **ENG-GAP-1 (markdown node-identity preservation) is SHELVED 2026-08-26** — per-leaf citation stays impossible by design. Revisit only if a markdown-as-input flow becomes supported. |
| **Markdown parsing to storage via text-match diffing** | 2026-08-26 | A future feature to ingest markdown (e.g. an external editor's export) back into the RAG store. **Will rely on TEXT-MATCH DIFFING, NOT node-identity comments** — comment IDs are fragile (an external editor could delete/alter them) and markdown is export-only today. **PROPOSAL GATE 2026-08-28 (round-trip-diffing framing): DO-NOT-PROCEED** — the revisit condition was UNMET and the text-match diffing mechanism is incoherent (undefined diff surface, unrecoverable content↔children boundary, coarse READ-aid line map, no diffing engine). **PROPOSAL GATE 2026-08-28 (initial-ingestion framing, re-run with the user's concrete use case — migrating an existing document store into the RAG engine): PROCEED-WITH-AMENDMENTS** — parsing existing markdown into RAG nodes/edges from scratch is a distinct sanctioned flow (NOT a contradiction of MARKDOWN-EXPORT-ONLY) and a minimal first slice is buildable. **ADJUSTED SCOPE (user decision 2026-08-28):** allow the `docs/` corpus (one-way snapshot), add table support (additive `RagNodeType` change), and multi-document corpus ingestion. See `docs/specs/markdown-import-review.md`. **Revisit condition (re-pinned):** proceed to the spec gate with the user's go-ahead; the first slice must pin the two-source-of-truth externality (one-way snapshot), the parser grammar, the importer's chunking rule, the document-root/document-id convention, the inline-children parse, the table types, idempotency (one-shot), and the security surface. **IMPLEMENTED (Unit T, 2026-08-28)** — the initial-ingestion framing landed as `docs/specs/unit-t-markdown-import.md` (the markdown file import slice: multi-document corpus ingestion incl. the `docs/` tree as a one-way snapshot, heading→section chunking, table support via the additive `RagNodeType` 18→23 change, one-shot, default-off `edit.import_markdown` tool routed through `applyBatch` as one atomic batch journal entry). The round-trip-diffing framing remains SHELVED (DO-NOT-PROCEED). |
| **Shared-node edit notification + save prompt** | 2026-08-26 | When a user/agent edits a section (RAG node) that is incorporated into MORE THAN ONE document (a CROSS-DOCUMENT-SHARED node, review §13), NOTIFY them that the node is shared across N documents. When the document would save, PROMPT whether the text should be changed for ALL owners (update all duplicates) or whether SOME should be preserved on a CLONE of the original (fork the node for one document, leaving the others unchanged). **Revisit condition MET 2026-08-27 — the editing path (Unit D) has landed** (the cross-document shared-node model is implemented in Unit C; Unit D implements the edit write-back + re-traversal). Still SPECULATIVE — not scheduled; a candidate for a future unit. |
| **Edge/node staleness flagging (bidirectional)** | 2026-08-26 | A staleness-tracking aid for the RAG layer. When an edge's text/content is updated, the connected nodes (its source/target) are FLAGGED for possible staleness — their content may reference the old edge content (e.g. the A→D reference edge's explanation, which a node's text may quote). Likewise, when a node's content is updated, the connected edges are FLAGGED for possible staleness — their content may reference the old node content. The flag is a REVIEW AID only: it does not change the authoritative store, does not block edits, and does not auto-update anything — it surfaces "this connected element may now be inconsistent" for the user/agent to review. Revisit when the editing path (Unit D) lands and edge-contained content (e.g. the A→D reference edge's explanation) is editable. |
| **Document tabs (multi-document simultaneous render)** | 2026-08-26 | The CROSS-DOCUMENT-SHARED duplicates are PER-RENDER materializations; render is transient. In the current single-document view, only ONE document renders at a time, so only ONE instance of a shared subtree exists at any moment (documents cannot loop in one view). The N duplicates COEXIST only if multiple documents render simultaneously — which requires UI TABS. Add document tabs to render several documents at once (then the "update all duplicates" concern becomes live). Revisit when a multi-document view is wanted (the rendering spine, Unit C, has landed). |
| **Manual document segmentation (override default chunking)** | 2026-08-27 | A user or agent should be able to MANUALLY chop a document into RAG node/edge segments, OVERRIDING the default chunking. Today the RAG node/edge boundaries are produced by the default chunking (the document model + traversal — Units B/C). This feature lets the user/agent explicitly define the RAG node/edge boundaries (e.g. select a text range → "make this a RAG node", "split here", "merge these", "mark this as a doc-head/next-section edge"), overriding the default segmentation. The manual boundaries are stored in the RAG store (the authoritative layer) and the traversal materializes them. Revisit when the editing path (Unit D) lands and a manual-segmentation use case surfaces (a candidate for a future unit). |
| **Binary-condense first-pass vector search** | 2026-08-27 | A binary condense of the embedding vectors as a FIRST-PASS search to NARROW DOWN the subset used for a FULL vector search. Each node's embedding vector is condensed to a compact binary code (e.g. sign-binarization / binary quantization of the vector, or a learned hash); a query's binary code is compared (e.g. Hamming distance) against all nodes' codes to cheaply select a CANDIDATE SUBSET, then the full-precision cosine similarity is computed ONLY over that narrowed subset (re-ranking). This is a coarse-to-fine / approximate-nearest-neighbor (ANN) optimization: the binary pass is fast and memory-light, the full vector pass is accurate but only on the candidates. Builds on the Unit F vector embedder + vector index (the binary codes are derived from the same embeddings). **Revisit condition PARTIALLY MET 2026-08-27 — the vector embedder (Unit F) has landed** (`src/main/embeddings.ts`); the remaining condition is the corpus being large enough that a full vector scan over every node is too slow. |
| **Crosslink hover-preview pane** | 2026-08-27 | For CROSS-LINKS, load the LINKED provident graph node into a DISPLAY PANE visible on MOUSEOVER on the link. When the user hovers over a crosslink (a `crosslink` edge — Unit G), a preview pane shows the linked node's rendered content (the linked provident graph node), without navigating away from the current document. This is a hover-preview / link-preview UX: the pane is transient (visible on mouseover, dismissed on mouseout), and it renders the linked node via the provident graph (the same render path — the linked node's subtree). Builds on the Unit G crosslink mechanism (the `crosslinks: CrosslinkWiring[]` output + the backlink enumeration) and the Unit H display-pane infrastructure. **Revisit condition MET 2026-08-28 — the crosslink mechanism (Unit G), the Unit H display-pane infrastructure (the pure pane registry + pane-inclusive envelope assembly, `src/renderer/pane-graph.ts`), AND the renderer display-pane MOUNT (the `SidebarPanes` host / `mountOperator` isolated-GraphScope render, landed with the UI mount — Unit K) have all landed.** Still SPECULATIVE — not scheduled. |
| **Click-drag moveable pane layout** | 2026-08-27 | CLICK-DRAG MOVEABLE layout for the sidebar registered panes, SNAPPING to the side/top/bottom of the window. The user can drag a pane (via the pane header) to move it and snap it to any window edge (left/right/top/bottom), instead of the fixed `[sidebar]` placement. The pane positions/layout are the user's preference (persisted, per-pane) and OVERRIDE the default `[sidebar]` zone placement from the pane registry (Unit H). This changes the placement from the fixed `SIDEBAR_ZONE` to a user-defined snapped zone. Builds on the Unit H pane registry + pane-inclusive envelope assembly (the pane content is still authored as provident content and rendered through the producing graph; only the PLACEMENT becomes user-moveable). **Revisit condition MET 2026-08-28 — the pane infrastructure (Unit H) has landed** (the pure pane registry + pane-inclusive envelope assembly). Still SPECULATIVE — not scheduled; the governing condition is now a customizable pane layout being wanted. |
| **Scoped snapshot for rendering (main-side traversal or a scoped `rag-doc-subgraph` IPC)** | 2026-08-29 | The SCOPED-LOAD fix (see `docs/specs/load-bug-scoped-traversal-review.md`) reduces the WALK cost but the boot/re-derive STILL fetches the full `RagSnapshotPayload` for `buildTraversal` — the snapshot IPC still serializes the whole store to the renderer. For a very large graph the transfer remains a full-graph cost. Follow-up: a main-side traversal (return the full `TraversalResult` over IPC) or a scoped `rag-doc-subgraph` IPC (the renderer fetches only the reachable subgraph). Revisit when profiling shows the snapshot IPC serialization is itself a bottleneck. |
| **Editing-mode config (textarea vs rich-text contenteditable)** | 2026-08-28 | There is NO user-facing config to toggle the document editing mode. The plain-text **textarea** editing UI (Unit L) is the only live editor; the rich-text contenteditable machinery is COMPLETE (Units N/O/P/Q/R/S — see `docs/next-steps.md` / the rich-text row above) but there is no switch to use it, and no `editingMode` operator setting exists (the operator-settings store exposes `enabledPanes`/`defaultDocumentId`/`topK` only). The user requested a toggle between "the demo textarea and rich text document editing". **PROPOSAL GATE 2026-08-28: PROCEED-WITH-AMENDMENTS** (validity VALID-WITH-AMENDMENTS, critique UNSOUND-as-written, architecture resolved decisions A–I, change-analysis PROCEED-WITH-AMENDMENTS — see `docs/specs/editing-mode-toggle-review.md`). Approved as a **5-unit slice (execution order U2→U3→U1→U5→U4)**: U2 pure `decomposeRichHtml`; U3 `isRichEditableRoot` + host `applyEditingMode` splice + snapshot `children` field; U1 `editingMode` operator-settings field + Settings control + `operator-settings-changed` broadcast + supersession; U5 `setRichText` atomic op + IPC/bridge; U4 contenteditable handlers + bridge + discriminated `CaretState` + IME + re-derive restore. textarea stays the DEFAULT. **LANDED / COMPLETE 2026-08-28 — the full 5-unit slice is code-complete (U2, U3, U1, U5, and U4 all DONE), AWAITING the user's commit**: — the pure `decomposeRichHtml` module (`src/main/rich-decompose.ts`) + the additive exports on `src/main/paste-sanitize.ts` (Unit U2); the pure `isRichEditableRoot`/`EDITABLE_TYPES` gate (`src/renderer/rich-eligibility.ts`) + the host `applyEditingMode` splice + the additive snapshot `children?` field + `EditingMode` type (`src/renderer/sidebar-panes.ts`, `src/shared/types.ts`) (Unit U3); the `editingMode` operator-settings field + Settings control + `operator-settings-changed` broadcast + supersession (Unit U1); and the `setRichText` atomic op + the `IPC_EDIT_RICH_COMMIT` channel + the preload `edit.commitRich` bridge + the `handleRichCommit`/`handleRichCommitIpc` handlers (Unit U5) have all landed (see the Unit U2 + Unit U3 + Unit U1 + Unit U5 DONE rows in `docs/next-steps.md`). **U4 is pending** — the contenteditable handlers + bridge + discriminated `CaretState` + IME + re-derive restore. |
